# SunDO Progress

## 현재 버전
v0.17.0

## 완료한 작업
- [x] W-01 프로젝트 스캐폴딩
- [x] W-02 디자인 토큰 이식 (PRD §7.1~7.3)
- [x] W-03A 기반 레이어 + AppShell + Field + Switch (DoD 24개 전부 통과)
- [x] W-03B 표면 컴포넌트 6종 + Dock + Footer (DoD 28개 전부 통과)
- [x] W-03C Toast · BottomSheet · ConfirmModal (DoD 28개 전부 통과) — **W-03 3분할 완료**
- [x] W-04 Firebase 초기화 + Google Auth 컨텍스트 (DoD 28개 전부 통과, 2단계 분할 수행)
- [x] W-05 라우팅 + 역할 가드 (DoD 27개 전부 통과) — **스토리 페이지가 사라지고 앱이 됐다**
- [x] W-06 S1 로그인 화면 (DoD 30개 전부 통과) — **첫 실물 화면.** S2·S2-1은 W-07로 분리됐다
- [x] W-07 S2-1 승인 대기 + `refresh()` 계약 확장 (DoD 30개 전부 통과, 27번은 §8 단서로 해당 없음)
      **S2는 W-08로 분리됐다** — OP-01이 프로파일 A에서 성립하지 않는다
- [x] W-08 S2 가입 신청 (DoD 31개 전부 통과) — **OP-01 충돌이 안 1+안 2로 해소됐다.**
      `inviteCodes` 단건 `get` + `useCount` 증가를 승인 시점으로 이동. 규칙 요청서는 `database_ToDo/W-08.md`
- [x] W-09 오프라인 배너 슬롯 — 여백 계약 변경 (DoD 22개 전부 통과)
      **배너가 오버레이에서 흐름으로 내려와 콘텐츠를 56px 밀어낸다.** 배너가 없을 때 6화면의
      computed 값은 한 칸도 안 바뀌었다. 착수 전 확인 2건을 받아 DoD 15·9의 검증 기준을 고쳤다.
      시드 요청서는 `database_ToDo/W-09.md`
- [x] W-10 S3 홈 · 학년 선택 (+ CountUp · sparkle · 당겨서 새로고침) — **DoD 34개 전부 통과**
      **자리표시자가 사라지고 첫 데이터 화면이 됐다.** 신규 부품 5종:
      `useCountUp` · `dateKeys`(순수 함수) · `stats`(판별 유니온) · `usePullToRefresh`+`ScrollRootContext` · `sparkle`.
      `AppShell`의 여백 계산은 **한 줄도 바뀌지 않았다**(들여쓰기 재정렬만).
      보고서 `reports/W-10.md` · 인덱스 요청서 `database_ToDo/W-10.md`
      **DoD 4(인덱스 실측)는 W-11 착수 시점에 st4R 환경에서 닫혔다** — S3의 세 질의는 인덱스 없이
      통과한다(6행 전문은 보고서 §1-3). 같은 실행에서 **S5의 `orderBy` 질의가 `failed-precondition`**임이 드러났다
- [x] W-11 S4 반 선택 · S5 학생 목록 (+ `roster` · `CenterNotice` · `.skel`/`.back` 승격) — **DoD 33개 전부 통과**
      **명부 화면 두 장이 섰다.** 신규 부품 2종(`lib/roster.ts` 판별 유니온 · `CenterNotice`) +
      아이콘 2종. 🔴 **착수 전 확인 1건(QA-11-01)** — `1f` 원문이 `sticky`와 양립하지 않아 멈추고 물었고
      **유리 헤더 띠**로 확정됐다. `AppShell` **diff 0줄** · 기존 화면 변경은 클래스 이름 승격뿐(회귀 0칸).
      보고서 `reports/W-11.md` · 인덱스 요청서 `database_ToDo/W-11.md`
- [x] W-12 S6 기록 작성 시트 + 저장 로직 — **DoD 35개 중 33개 통과 · 2개 미측정**
      🔴 **앱이 처음으로 데이터를 만든다.** 지금까지는 전부 읽기였다.
      신규 3종(`RecordSheet` · `Segmented`(공용) · `lib/records.ts`) + 확장 4종
      (`dateKeys.toMonthKey`/`formatDateTimeKst` · `spawnSparkle(color?)` · `Field(counter?)` ·
      `BottomSheet(onClosed?, 아래 스와이프)`). `AppShell` **diff 0줄** · 기존 화면 변경은 `ClassStudents.tsx` 하나뿐.
      **의존성 추가 0**(UUID는 `crypto.randomUUID()`) · 실 Firestore 쓰기 **0건** · 콘솔 조작 **0건**.
      착수 전 확인 2건(`Field` 카운터 API 형태 · 아래 스와이프 소유자) — 둘 다 승인.
      **미측정 2건**: ER-17(UI 도달 불가 — §4-8) · 「인덱스 없이 실제로 동작」(실 접속 0 제약 — §1-4).
      보고서 `reports/W-12.md` · 콘솔 요청서 `database_ToDo/W-12.md`
- [x] W-13 S7 기록 조회 (+ 실시간 구독 · 무한 스크롤 · 카운터 3종) — **DoD 36개 중 35개 통과 · 1개 부분 측정**
      🔴 **앱이 처음으로 `onSnapshot` 실시간 구독을 켠다.** 지금까지 모든 조회는 단발 `getDoc`/`getDocs`였다.
      `Records.tsx` 자리표시자가 실물이 됐고, `records.ts`에 조회 계층 5종
      (`subscribeRecords` · `fetchRecordPage` · `fetchRecordCounts` · `fetchWithdrawnAuthors` · `clearRecordsCache`)이 붙었다.
      `dateKeys.ts` 2종(`formatTimeKst` · `formatDateKeyLabel`) · 아이콘 2종 · CSS 25종 추가.
      `AppShell` **diff 0줄** · `stats.ts`·`router.tsx`·`usePullToRefresh` **전부 diff 0줄** · 의존성 추가 **0** ·
      실 Firestore 접속 **0건** · 콘솔 조작 **0건** · 린트 경고 **18개(기준선 그대로, 신규 0)**.
      **착수 전 질문 0건** — §9의 5조건에 걸리는 항목이 없었다(구독 형태 성립 · 스크롤 root는 이미 컨텍스트가 있다).
      **부분 측정 1건**: 동기화 칩 「정상」의 실 SDK `fromCache` 플립(서버 왕복이 필요 — 접속 0 제약).
      보고서 `reports/W-13.md` · 콘솔 요청서 `database_ToDo/W-13.md`
- [x] W-14 S9 선도 일정 (조회) — **DoD 32개 전부 통과 · 미측정 0건**
      **읽기 전용 3블록짜리 가벼운 화면인데 규격 공백이 4건 나왔다.** 신규 1종(`lib/duty.ts`) +
      확장 3종(`dateKeys` 4종 · 아이콘 1종 · CSS 16종). `AppShell`·`router.tsx`·`stats.ts`·
      `records.ts`·`roster.ts`·기존 화면 5종 **전부 diff 0줄** · 의존성 추가 **0** ·
      실 Firestore 접속 **0건** · 린트 경고 **18개(기준선 그대로)**.
      🔴 **`onSnapshot`을 켜지 않았다** — 문서 1~2건이고 MVP에서 이 문서를 바꾸는 주체가 앱에 없다.
      🔴 **`getDoc`은 `onSnapshot`과 실패 방식이 반대**임을 실측했다(§1-3) — EM-06에 추가 조건이 필요 없다.
      🔴 **BR-48 주차 공백**을 해석으로 고정하고 경계 14케이스를 순수 함수로 실측했다.
      **착수 전 질문 0건** — §9의 5조건이 전부 실측·기존 규칙으로 갈렸다.
      OV-06 — design `1j`의 `舊 version.` 3종(독 4탭 · 템플릿 바인딩 5개)을 현행화했다.
      보고서 `reports/W-14.md` · 콘솔 요청서 `database_ToDo/W-14.md`
- [x] W-15A S8 관리 ③ 가입 승인 + ① 부서 가입 코드 — **DoD 34개 전부 통과 · 미측정 0건**
      🔴 **앱이 처음으로 남의 계정 상태를 바꾼다.** 배치 쓰기 · 감사 로그 · `inviteCodes` 생성이
      전부 처음이고 셋이 한 트랜잭션에 묶인다 — **부분 성공이 없다**를 실측으로 잠갔다
      (3번째 연산에 실패 주입 → 쓰기 **0건** · 네 문서 전부 원복).
      신규 1종(`lib/admin.ts` — 6번째 `lib`) + 확장 3종(`inviteCode.generateInviteCode` ·
      `Pill(disabled, ariaLabel)` · CSS 21종). `AppShell`·`router.tsx`·`stats.ts`·`records.ts`·
      `roster.ts`·`duty.ts`·기존 화면 6종 **전부 diff 0줄** · 의존성 추가 **0** ·
      실 Firestore 접속 **0건** · 린트 경고 **18개(기준선 그대로)**.
      🔴 **② 부원·권한 양도는 만들지 않았다**(W-15B) — 섹션 자리 자체가 없다.
      🔴 규격 공백 3건을 닫았다 — 거절 사유 UI 부재 / `activeInviteCodeId` `null` 상태 / BR-17↔§9.3.3 충돌.
      🔴 코드 생성기 10,000회 실측 — 형식 위반 0 · 금지 문자 0 · 충돌 0 · `Math.random` 0건.
      **착수 전 질문 0건** — §9의 ④(오프라인 배치)는 타임아웃을 발명하지 않고 실측해 오프라인 회차로 넘겼다.
      보고서 `reports/W-15A.md` · 콘솔 요청서 `database_ToDo/W-15A.md`
- [x] W-15B S8 관리 ② 부원 목록 + 권한 양도 (+ S8 보정 3건) — **DoD 33개 전부 통과 · 미측정 0건**
      🔴 **S8이 MVP 범위에서 완성됐다.** 앱이 처음으로 `users`를 **목록(`list`)으로** 읽고,
      처음으로 남의 `role`을 **두 건 동시에** 바꾼다. 양도는 이 앱에서 **유일하게 되돌릴 수 없는 조작**이라
      빗장(ref) · 확인 모달 · 대상 검증(커밋 전 `getDoc` 3회) 세 겹을 전부 걸었다.
      신규 2종(`admin.fetchMembers`/`transferHead`) + CSS 3종(`.adm-ro`·`.mrow-mem`·`.mrow-av`) +
      토큰 1개(`--text-rowsm`). `AppShell`·`router.tsx`·`stats.ts`·`records.ts`·`roster.ts`·`duty.ts`·
      `RequireAuth.tsx`·기존 화면 6종 **전부 diff 0줄** · 의존성 추가 **0** ·
      실 Firestore 접속 **0건** · 린트 경고 **18개(기준선 그대로)**.
      🔴 **강등 대상을 `departments.headUid`로 잡아 head 경로와 Dev 경로를 한 형태로 만들었다**(결정 2).
      🔴 **성공 직후 TS-12 「접근 권한이 없습니다」가 실제로 떴고 세 번 실패한 뒤 잡았다** — 전문은 보고서 §4-2.
      🔴 규격 공백 3건을 닫았다 — MD-02 Dev 변형 / 정렬 규칙 / EM-05 조건.
      **착수 전 질문 2건** — `14px` 토큰 이름(`--text-rowsm`)과 리터럴 치환 범위(5곳 전부)를 확정받았다.
      보고서 `reports/W-15B.md` · 🔴 **콘솔 요청서 `database_ToDo/W-15B.md`가 규칙 회차의 단일 입력이다**

- [x] W-16 규칙 회차 — Security Rules + 인덱스 + 배포 체계 — **DoD 32개 전부 통과 · 미측정 0건**
      🔴 **앱을 떠받치던 W-04 임시 catch-all을 걷어낼 규칙 파일이 섰다. 단 아직 배포하지 않았다.**
      산출은 「배포된 규칙」이 아니라 **「배포해도 안전하다는 증거를 갖춘 규칙 파일」**이다.
      신규 파일 5종(`firestore.rules` 358줄 · `firestore.indexes.json` · `firebase.json` · `.firebaserc` ·
      롤백용 `firestore.rules.catchall`) + 규칙 테스트 8파일(`tests/rules/`).
      **규칙 테스트 152건 전량 통과**(통과기대 59 · 거부기대 93 — 지시서 요구 42건의 상위 집합) ·
      🔴 **역검증 18변형**(조건을 일부러 열어 「빨개지는지」 확인 — 17개는 빨개졌고 **1개는 빨개지지 않았다**).
      앱 코드 변경은 `src/lib/records.ts` **한 곳뿐**(`recordCount` A안 ② — `setDoc` 1건 → 배치 2연산,
      `void`·`await` 안 함 계약은 그대로). `AppShell`·`router.tsx`·화면 8종·`lib` 5종 **전부 diff 0줄**.
      **`dependencies` diff 0줄**(에뮬레이터 2종은 `devDependencies`) · 번들 JS **−71B** · CSS **해시까지 동일** ·
      🔴 **실 Firestore 접속 0건**(테스트 projectId `demo-sundo-rules`) · **`firebase deploy` 실행 0회** ·
      린트 경고 **18개(기준선 그대로)**.
      🔴 **Q-1~Q-5 실측 5건이 규칙 구조를 결정했다** — 보고서 §2. 특히 **Q-5는 지시서에 없던 측정**이고
      「`$` 앵커가 `attacker@dshs.kr.evil.com`을 막는다」는 PRD 문장이 **사실이 아님**을 잡았다.
      보고서 `reports/W-16.md` · 🔴 **배포 실행 지시서 `database_ToDo/W-16.md`**
- [x] W-16D 배포·시딩 실행 회차 — **스모크 8/8 통과 · 🔴 롤백 0회 · `src/` diff 0줄**
      🔴 **실 Firestore를 처음 건드린 회차다.** W-16까지 접속 0건이었고 검증은 전부 에뮬레이터였다.
      되돌리기 비싼 조작 셋이 전부 성공했다 — ① 규칙 전량 교체(catch-all 소멸 `00:36:15Z`)
      ② 학생 **1,033건** 임포트(실패 0 · 2.7초) ③ 부장 권한 양도 + **Dev 복구 경로**.
      🔴 **배포 전에 IX-08 누락을 잡았다** — `firestore.indexes.json`이 `records` 2건뿐이었고
      `roster.ts:fetchStudents`가 등식 3개 + `orderBy('number')`라 **그대로 배포했으면 S5가 ER-03**이었다.
      원인은 `W-16.md` §8 표가 **W11-2(S5 orderBy)와 W11-3(S4 카운트)을 한 칸에 뭉갠 것**.
      🔴 **콘솔에 파일에 없는 인덱스 5건이 더 있었다**(인벤토리는 「없음」이었다) — 스모크 후 삭제해
      정확히 3건. 소비자 9개 질의 실측 `INDEX-REQ` **0건**.
      신규 2종(`scripts/seed-students.mjs` — `--inspect`/`--dry-run`/`--commit`/`--prune`,
      `scripts/inspect-firestore.mjs` — **읽기 전용**) · `dependencies` diff **0줄**(`firebase-admin`은 devDeps).
      🔴 **실측 4건이 W-17의 전제를 바꿨다** — `memberCount`는 증가도 감소도 되지 않고 읽는 소비자도
      없다(`src/` 참조 **0건**) · 고아 활성 코드 `GSFH-4487`(BR-17 정합 규정 공백) ·
      `W-11.md` §2-2 생성 링크의 필드 순서 오류 · S6 시트가 **개발 서버에서만** 자기 자신을 닫는다
      (`BottomSheet` N-02 효과에 cleanup 없음 + StrictMode — **프로덕션은 정상**).
      🔴 **규칙을 넓히지 않았다** — 5-6이 실패로 보였을 때 정적 근거 셋으로 앱 문제임을 가려냈다.
      보고서 `reports/W-16D.md` · W-17 입력 `database_ToDo/W-17-input.md`

- [x] W-17 S10 설정 + 계정 탈퇴 (+ W-17F 보정 2건) — **DoD 20개 중 18 통과 · 2건은 근거 기록**
      🔴 **실규칙 위에서 만들어진 첫 화면이다.** S1~S9는 W-04 catch-all 위에서 만들어졌고
      규칙이 나중에 따라왔지만, S10은 `selfWithdraw()`가 **이미 서 있는 상태**에서 그 첫 소비자로 섰다.
      🔴 **`firestore.rules` diff 0줄 · 재배포 0회** — 코드를 규칙에 맞췄다.
      신규 2파일(`lib/account.ts` · `screens/Settings.tsx`) + 아이콘 2종 + `.set-*` CSS +
      🔴 **버전 주입**(`vite.config.ts`가 `package.json`을 읽어 `__APP_VERSION__` — GT-07 단일 출처).
      `AppShell`·`router.tsx`·화면 8종·`lib` 기존 5종 **전부 diff 0줄** ·
      🔴 **`/settings` 라우트와 독 `설정` 탭이 이미 있어 「1줄 등록」조차 필요 없었다** ·
      의존성 추가 **0** · 린트 **18(기준선)** · 규칙 테스트 **159/159**(152 → **+7**) ·
      역검증 **+1**(`selfwithdraw-keys-open`).
      🔴 **탈퇴는 배치 2연산이다** — BR-57의 4연산 중 `memberCount` 감소는 **실행 불가능**하고
      (`B-17`이 증명 · 탈퇴자는 `head`가 아니라 `departments` update에 막힌다) 결정 1이 폐기를 확정했다.
      🔴 **실기기 탈퇴 1회 성공 + 되돌리기 완료** — 정확히 3필드만 바뀌었고 **`recordCount`가 1로 보존**(C7)됐다.
      🔬 **측정으로 얻은 것 4건** — ① `diff().affectedKeys()`는 「쓴 키」가 아니라 **「값이 바뀐 키」**다(`B-20`)
      ② 규칙의 `auditLogs` create는 **`action`을 검사하지 않는다**(`B-18`) — 그래서 재배포가 필요 없었다
      ③ 🔴 **CSS 402줄이 `@media (max-width:380px)`에 갇혔다** — 삽입 앵커를 `@layer`의 닫는 괄호로
      착각했고 **소스·빌드·린트가 전부 통과해 번들을 봐야만 드러났다**(보고서 §4.1 · 규약 승격)
      ④ 🔴 **시크릿 창은 창이 아니라 「세션」 단위로 격리된다** — 계정을 바꾸려면 전부 닫아야 하고,
      캐시는 신원뿐 아니라 **계정 상태**도 가린다(보고서 §6.1 · §6.4).
      🔴 **지시서 결정 3(부장 차단)을 철회했다** — design `20b`·§8.11.4 T-06·§10.10 R-07·BR-56
      **네 곳이 이미 일치**했고 결정만 달랐다. 신규 문구 0건.
      보고서 `reports/W-17.md` · 콘솔 요청서 `database_ToDo/W-17.md` · W-17F 지시서 `prompts/W-17F.md`

- [x] W-18 정책 화면 3종 + Footer 연결 — **DoD 14개 중 12 통과 · 8번 부분 실측 · 11번 승인된 이탈**
      🔴 **이 회차의 산출은 코드가 아니라 「사실」이다.** §2 구현 사실 인벤토리 12항과
      §3 사실 대조표(문장 ↔ 근거)가 정본이고 화면은 그것을 담는 그릇이다.
      신규 2파일(`lib/policy.ts` 문안 정본 · `components/PolicyScreen.tsx` 공용 셸) +
      화면 3종은 **각 한 줄** + CSS **218줄 추가·기존 0줄 변경**.
      🔴 **`firestore.rules`·`AppShell`·`router.tsx`·기존 화면 8종·`lib` 기존 전부 diff 0줄** ·
      의존성 **0** · 린트 **18(기준선)** · 규칙 테스트 **159/159**(W-17과 동수).
      🔴 **design 본문 6문장을 사실로 교체했다**(DS-05 → 사용자 확정) — 시각 규격은 한 칸도
      바꾸지 않고 **문장만** 고쳤다. 「탈퇴 시 파기」·「기간 경과 후 삭제」·「언제든지 탈퇴」가
      전부 실측과 어긋났다(W-17 §5.1의 17필드 불변 · 파기 코드 0줄 · BR-56).
      🔴 **금지 문장 6종 0건을 `grep`이 아니라 「렌더되는 문자열 82개」로 증명했다** —
      「쓰지 마라」라고 적은 주석이 `grep`에 걸리는 거짓 양성을 없앤 검사다.
      🔴 **지시서 §0.5·§3.6을 이탈해 S2에 약관 동의 확인을 만들었다** — 같은 문서 §9-2가
      「동의 시점이 규격에 없으면 멈추고 물어라」로 요구했고 규격 공백(코드 0줄 · PRD 조항 없음 ·
      design 0건)을 확인한 뒤 사용자가 신설로 확정했다. **Firestore 필드는 0개**다.
      🔬 **측정으로 얻은 것 3건** — ① **이 하네스에서는 node가 Chrome을 spawn하면 조용히 죽는다**
      (종료 코드 127 · 예외 0 · 샌드박스 무관) → **PowerShell이 띄우고 node는 붙기만** 한다
      ② 🔴 **`min-height:100%`는 높이를 확정하지 않는다** — 카드가 1,731px로 늘고 내부 스크롤이
      0이 됐다. `height:100%`로 바꿔야 `flex:1`이 분배할 「남는 공간」이 생긴다
      ③ 자기 결함 1건 — 음수 여백 `-10px`이 간격을 12px이 아니라 **2px**로 만들었다(실측이 잡았다).
      번들 증가 **JS +11.66 kB(gzip +5.02) · CSS +1.93 kB(gzip +0.30)** ·
      🔬 **A/B — 같은 문안을 JSX로 박으면 셸을 빼고도 +5.34 kB 크다**(데이터 모듈 채택 근거).
      보고서 `reports/W-18.md` · 콘솔 확인 요청 `database_ToDo/W-18.md`(**리전 확인 1건**)
- [x] W-19 PWA(manifest · 아이콘 · 최소 SW · 설치) + **Hosting 첫 배포** — DoD 19개 중 15 통과 · 3 부분 · 1 승인된 이탈
      🔴 **앱이 처음으로 인터넷에 올라갔다** — `https://sundo-dev-8ef38.web.app` (⚠ **공유 금지**).
      신규 6종: `public/manifest.webmanifest` · `public/sw.js` · `public/icons/` 4장 ·
      `lib/pwa.ts` · `UpdateBanner`(design `10c`) · `InstallBanner`(design `10d`).
      🔬 **설치 가능 요건을 대조군 5개로 실측했다** — `fetch` 핸들러도 **서비스 워커 자체도 요건이 아니고**
      binding한 것은 **manifest**뿐이다(보고서 §1.3). 이 결과가 W-20의 전제를 바꾼다.
      **의존성 추가 0** — 아이콘은 헤드리스 Chrome `canvas`로 뽑았다.
      `AppShell` **diff 0줄**(배너는 `DockLayout`의 `createPortal`) · `router.tsx` **0줄**.
      🔴 **계약 4-2 이탈 1건** — `reapply` `hasOnly`에 2키(결정 4). 규칙은 **앱보다 먼저** 재배포했다.
      보고서 `reports/W-19.md` · 콘솔 요청 `database_ToDo/W-19.md`(🔴 **SW 해제 절차 · 코드 내리기**)
- [x] W-20 오프라인(캐시 전략 · 갱신 강제 · 전송 대기 배지) — DoD 18개 중 14 통과 · 2 부분 · 2 미측정
      🔴 **서비스 워커가 처음으로 캐시를 갖는다.** W-19 §8-2의 「낡은 워커가 남아도 증상이 없다」가
      오늘부터 거짓이 됐고, 그 대가를 **내비게이션 network-first**로 막았다(보고서 §3.2).
      🔬 **오프라인에서 앱과 정책 3종이 전부 열린다** — 배포본에서 본문 길이가 온라인과 같다
      (CP-02 · EC-44). 🔬 **자기 제거 워커가 캐시가 붙은 뒤에도 유효**함을 실측(DoD 11).
      신규: BR-41 `전송 대기 {n}건` 배지 + NT-07 플러시 토스트(사용자 결정) ·
      precache **빌드 생성**(`vite.config.ts`) · `lib/records.ts` 대기 큐 계층.
      **의존성 추가 0**(Workbox 미도입 — 실코드 59줄) · `firestore.rules` **0줄** ·
      `AppShell`·`router.tsx` **0줄** · 화면 변경은 **S7 하나**.
      🔴 **실기기 17건은 전량 미측정이다** — 기기가 없다. 체크리스트는 `database_ToDo/W-20.md` §2.
      보고서 `reports/W-20.md` · 콘솔·검증 절차 `database_ToDo/W-20.md`
- [x] W-21 실기기 검증 + 버그·디자인 패치 (패치 9건 + 결정 3 전부 완료 · DoD 18개 중 15 통과 · 3 부분)
      🔴 **P-0을 코드 0줄로 고쳤다.** standalone 로그인 무한 루프의 원인은 앱이 아니라
      **`authDomain`이 manifest `scope` 밖**이라는 배포 설정이었다 — iOS가 OAuth를 Safari로
      내보내 크리덴셜이 **다른 브라우징 컨텍스트**에서 확정됐다. `…firebaseapp.com` →
      **`…web.app`**으로 바꿔 전 과정을 동일 출처로 만들었다(`.env.local` 1줄 · GCP 승인 URI 선행).
      ⇒ **`signInWithRedirect` 경로는 이 앱에서 한 번도 성립한 적이 없었다**(W-19 §5-3 미측정분).
      나머지 8건: P-1 S5 리스트 내부 스크롤(사용자 승인) · P-2 `.shine`을 `background-clip:text`로 ·
      P-5 `.pill` 가로 13→22 · P-6 로고 2자리 · P-7 아바타 5자리 정적 이미지 ·
      P-8 토스트 1줄 + 모달/안내 마침표 분할 · P-10 footer 최하단·22px · P-11 체크박스 테두리 + 미체크 안내.
      `firestore.rules` **0줄** · `rules:test` **162/162** · `AppShell`·`router.tsx` **0줄** ·
      의존성 **0줄** · 린트 **18**. 신규 파일은 `src/lib/sentences.ts` 하나(순수 함수).
      🔴 **실기기 16/17 통과 · 1 미측정**(1번 — Android 기기 없음). **§0.4 이월 9건 중 8건이
      네 회차 만에 닫혔다.** 14~16번이 W-20의 network-first 판단을 실기기에서 증명했다.
      🔴 **신규 결함 2건은 고치지 않고 W-21B로 넘겼다** — ⓐ `getCountFromServer`가 오프라인
      캐시를 못 읽어 PRD §8.3.5·§8.4.5가 구조적으로 미충족(SDK 원문 확정) ⓑ 오프라인 S5
      명부 ER-03(원인 미확정 — 디버거를 붙일 기기가 없다). 지시서 §2.2의 「W-22 직전에
      큰 수술을 하지 않는다」를 지켰다.
      보고서 `reports/W-21.md` · 콘솔 요청서 `database_ToDo/W-21.md`
- [x] W-21B 기록 수정·삭제 + 코드 발급 권한 (**규칙 회차** · DoD 18개 중 14 통과 · 2 미측정 · 2 W-21C 이관)
      🔴 **첫 산출은 「나누기로 한 결정」이다.** 지시서는 네 건(기능 3·4·9·12)을 한 회차에
      담았고 §0.1이 「안 들어가면 코드를 더 쓰지 말고 분할을 먼저 제안하라」고 못 박았다.
      조사를 마친 시점에 **코드 0줄에서 멈추고 물었고 사용자가 2분할을 골랐다.**
      **W-21B = 기능 9 + 12(규칙)** / 🔴 **W-21C(`v0.16.0`) = 기능 3 + 4(검색·데이터 모델)**.
      🔴 **둘째 산출은 지시서의 사실 하나를 뒤집은 것이다** — §3.4의 「`inviteCodes`
      create/update → `isDev()`」를 **그대로 쓰면 부장의 가입 승인이 죽는다**(승인 배치가
      같은 규칙으로 `useCount`를 +1 한다 · `admin.ts:383` · B-1). **필드로 갈라** 만료·생성은
      `dev`, `useCount`만 `head`에게 남겼다(C-10이 급소를 지킨다).
      `firestore.rules` **+103 −8**(W-16D 이후 처음 열었다) · `rules:test` **162 → 213건 전량 통과** ·
      역검증 **18 → 33변형 전부 1:1** · 새 배치 **2종**을 실제 `writeBatch`로 통과 확인.
      🔴 **의도적으로 뒤집은 기존 케이스 4건**(C-1 · C-3 · B-3 · RU-2) — 전부 보고서 §4.4에 1:1.
      권한은 **작성자 본인 · 차장 · 부장 · Dev** 넷이다(착수 확인 3 — 결정 4에 차장을 남겼다).
      🔴 **`recordCount` 감소는 넣지 않았다** — 소비자가 화면에 0곳이고, 삭제자≠작성자일 때
      「남의 카운터를 바꾸는 새 문」을 열어야 한다(B-30이 실측). W-16 §7의 v1.1 예약은 **안 닫힌다.**
      🔴 **삭제 되돌리기(`deleted → active`)도 열지 않았다** — 콘솔이 한다(`database_ToDo/W-21B.md` §5).
      `AppShell`·`router.tsx`·`firestore.indexes.json`·`package-lock.json` **전부 0줄** · 린트 **18** ·
      CDP 실측 **가로 스크롤 15칸 전부 0** · 번들 **+8,824 B**.
      🔴 **실기기는 미측정 — 배포 전이다**(DoD 14·15는 배포 후에만 닫힌다).
      보고서 `reports/W-21B.md` · 콘솔 요청서 `database_ToDo/W-21B.md`
- [x] W-21C 순찰 일정 편집(중식/석식) + 학생 검색 + 이월 결함 2건
      (**v1.0.0 전 마지막 코드 회차** · DoD 19개 중 17 통과 · 2 미측정)
      🔴 **최대 산출은 W-21의 판정을 뒤집은 실측이다.** W-21 §6.1은 결함 3(오프라인 집계)을
      **「설계 회차 규모」**로 봤다 — 집계를 `getDocs`로 바꾸면 S3마다 학생 1,033건이라는
      이유였다. 🔬 **소비자를 세어 보니 S3 학년 버튼도 S4 반 버튼도 인원 수치를 안 그린다**
      (`isEmptyGrade`·`isEmptyClass`의 `> 0`뿐). 필요한 것은 **존재 여부**뿐이라 `limit(1)`이면
      학년당 **1건**이고, `(오프라인)` 표기는 **이미 구현돼 있었다**(`Home.tsx:42`).
      ⇒ **설계 회차가 아니라 46줄이었다. 온라인 과금 증가 0**(집계가 먼저 서고 실패해야 `getDocs`).
      🔴 **마이그레이션 실행 완료** — `dutySchedules` 2건에 `*ByMeal` 4필드 **추가만**.
      옛 4필드는 **보존**(되돌리기 근거) · 멱등 · `--revert --commit`으로 되돌린다.
      🔴 **같은 이름 교체(안 B)를 배제한 근거를 코드에서 확인했다** — 옛 앱의 `toNameMap`이
      `if (!Array.isArray(value)) continue`라 **요일 행이 조용히 빈다**(거짓말).
      규칙 `dutySchedules` update/create를 **`isHead()`로 좁혔다**(결정 2). 🔴 좁히기 전에
      **write 경로를 코드에서 셌고 0건이었다**(규약 4-2) — 그래서 규칙을 앱보다 먼저 배포해도 된다.
      `rules:test` **213 → 231건 전량 통과** · 역검증 **33 → 39변형 전부 1:1**.
      ⚠ **결함 4는 곁가지만 고쳤다**(사용자 확정) — 캐시에 없는 반을 오프라인에서 열면
      EM-01 「학생이 없습니다」라는 **거짓말**이 뜨던 것을 ER-03으로. **원인은 v1.1**(기기 없음).
      `AppShell`·`router.tsx`·`firestore.indexes.json`·`package-lock.json` **전부 0줄** ·
      린트 **18**(🔴 20까지 늘었다가 규칙을 끄지 않고 되돌렸다) · CDP **가로 스크롤 20칸 전부 0** ·
      번들 **+20,337 B**.
      🔴 **실기기 미측정 — 배포 전이다.**
      보고서 `reports/W-21C.md` · 콘솔 요청서 `database_ToDo/W-21C.md`

## 진행 중
- 🔴 **W-22B 완료 — 커밋 `SunDO v0.17.0`.** 남은 것은 **PM의 콘솔 5건과 W-22A 스모크**다.

  ### ✅ W-22B가 닫은 것
  - 🔴 **App Check ① SDK 부착** — `initializeApp` 직후·`getAuth`보다 **먼저**(48 → 77 → 88행).
    🔴 **키가 없으면 건너뛰고 경고 1줄만 남긴다**(W-04 §6의 `throw` 계약을 **일부러** 따르지 않았다 —
    6개는 **기능**이고 사이트 키는 **방어**다). `try`로 감쌌다(EC-01).
    🔬 **3형태 실측 — 전부 앱이 그려졌다**: 키 없음 / 가짜 키(400) / 도메인 전면 차단 / 예외 주입.
    **예외 주입에서 `catch`가 실제로 잡혔다.**
  - 🔴 **BR-30 카운터** — `approvalRequests`를 **등식 1개**로 세고 24시간은 **클라이언트 필터**.
    **새 컬렉션 0 · `users` 신규 필드 0 · `localStorage` 0 · 인덱스 0건.**
    판별 유니온 `ok(n) | unknown`이고 🔴 **`unknown`은 막지 않는다**(§3.5 — 가입을 잘못 막는 것이 더 비싸다).
    S2는 **진입 1회 + 제출 직전 1회** 센다. E-1009 `가입 신청은 24시간에 3회까지 가능합니다`.
  - 🔴 **`firestore.rules` 1줄을 열었다** — `approvalRequests` `allow list`에 「본인」 절.
    ⚠ **§0.4 이탈이고 PM 승인을 받았다.** 근거는 아래 「가장 큰 산출」.
  - 🔴 **규칙 테스트 231 → 236건 전량 통과** · 역검증 **41 → 43변형**(신규 3건 전부 1:1).
  - 🔴 **`CLAUDE.md`에 「검증 규약」 절 신설** — 4-3 교체 · 4-4·4-5·4-6 승격 · **4-8·4-9 신설**.
  - 🔴 **§14.5 14항목 대조표** — ✅ 10 · 🔶 2(의도된 부분) · 🔴 2 열림(11번 시행 · 14번 도메인).
    🔴 **11번을 ✅로 적지 않았다 — 시행하지 않은 App Check는 아무것도 거부하지 않는다.**

  ### 🔴 W-22B의 가장 큰 산출 — 지시서대로 짰으면 **막는 척만 하는 코드**가 될 뻔했다
  `allow list: if isVice() || isTeacher();` 였고 **신청자는 둘 다 아니다.**
  🔬 저장소 원본 규칙을 에뮬레이터에 싣고 재 보니 `noProfile`·`pending`·`withdrawn`·`rejected`가
  **전부 `permission-denied`**였다. §3.5가 「조회 실패는 통과」이므로 두 규정이 겹치면
  **카운터가 항상 통과** — BR-30이 통째로 무동작이 된다.
  🔬 **덤으로 규칙 주석 하나가 틀렸다는 것이 드러났다** — 「`list`에서는 「본인」을 증명할 수 없다」는
  거짓이고, Firestore는 `list`를 **문서마다** 평가한다. 본인 질의는 통과 · `where` 없는 질의와
  남의 uid 질의는 **거부**됨을 양방향으로 확인한 뒤에 고쳤다.

  ### 🔴 아직 열려 있는 것
  1. 🔴 **W-22A 스모크 18건 · 되돌리기 3건 · M-01 · 콘솔 육안 3건** — `database_ToDo/W-22A.md`
  2. 🔴 **콘솔 5건** — App Check 등록·디버그 토큰 · 시행 절차 · 보호책임자 연락처 ·
     승인 도메인 · 잔재 정리 7행 — `database_ToDo/W-22B.md`
  3. 🔴 **PRD 조항 개정 3건(§17.1 S9 편집 · 기록 수정·삭제 · `dutySchedules` 시딩)을 쓰지 못했다** —
     근거가 스모크 관측인데 **전량 미측정**이다. **안 도는 것을 조항에 적을 수 없다.**
     나머지 5건(Lighthouse · §18.2 · M-01 측정방법 · §18.1 dev/prod · E-1009)은 **완성 문장으로 제출**했다.
  4. 🔴 **DoD 미측정 4건**(9·14·15·19) — `src/lib/signup.ts`가 **Vite 전용 모듈**이라 node에서 못 부르고,
     `vite preview` + 헤드리스에서 **서비스 워커가 페이지를 제어하지 않았다.**
     **배포본에서 실계정으로 신청을 3회 만들면 전부 닫힌다.**

  보고서 `reports/W-22B.md` · 콘솔 요청서 `database_ToDo/W-22B.md`

---

## 지난 회차 (W-22A)
- ⚠ **W-22A 부분 완료** — 배포·관문·Lighthouse·§8(커밋 `v0.16.1`·재배포)까지.
**재개하는 세션은 이 항목과 `database_ToDo/W-22A.md`만 읽으면 문맥이 복원된다.**

  ### ✅ 이미 끝난 것 (다시 하지 마라)
  - 🔴 **배포가 나갔다.** `2026-08-25` KST · 3단계 전부 §3.3 순서대로:
    ① 넓히는 규칙(임시본) `22:35:21~42` → ② 앱 `v0.16.0` `22:36:32~45`(live 릴리스 `22:36:44`)
    → ③ 좁히는 규칙(저장소 원본) `22:37:14~25`. **②③ 창 = 29초.**
    🔴 **배포본은 이제 `v0.16.0`이다** — W-21B·W-21C가 **한 번에** 나갔다.
  - 🔴 **§2 관문 5건 전부 통과** — 문자열 3건 있음 · `rules:test` **231/231** ·
    테스트 **뒤** `firestore.rules` diff **0줄**(SHA `c8d6430…` 불변). **규칙은 사라지지 않았다.**
  - 🔴 **배포된 것이 무엇인지 실선에서 확인했다** — 자산 파일명이 로컬 빌드와 일치 ·
    SW 캐시 `sundo-shell-v0.16.0-afd174ac` · 번들에 W-21B/W-21C 기능 문자열 8종.
  - ✅ **Lighthouse PWA 측정 완료 — 점수 100 · 자동 감사 6건 전부 1.0 · 실패 0건.**
  - 🔴 **임시본은 커밋되지 않았다**(`git checkout --`로 지웠다) · `git status` 깨끗 ·
    **소스 diff 0줄** · **커밋 0회** · 인덱스 배포 0건 · 마이그레이션 재실행 0건.

  ### 🔴 남은 것 — 전부 PM 실행 대기 (`database_ToDo/W-22A.md`가 절차 정본)
  1. 🔴 **콘솔 육안 확인 3건**(§3) — 게시된 규칙 **본문**. 🔴 CLI로는 잴 수 없다(§9 ②).
  2. 🔴 **스모크 18건 전량 미측정**(§4) — 계정 로그인·실기기가 필요하다.
     🔴 **8번(부장의 가입 승인)이 급소**이고 실패하면 §7 롤백을 **PM이 판단**한다.
     🔴 **18번(Dev의 `일정 편집`)은 기대를 비워 두고 잰다.**
  3. 🔴 **되돌리기 3건**(§5) — 삭제 기록 `status` 복구 · `inviteCodes` 재발급 왕복 ·
     `htm0303`의 `role`을 `member`로. **하나라도 실패하면 혼자 손대지 말고 물어라.**
  4. 🔴 **M-01 값**(§4.6) — 🔬 **정의는 닫혔다. 짓지 마라.** PRD §3.1이 이미
     「학생 행 탭 → 저장 성공 토스트」로 규정했고 기기는 §17.1이 iPhone Safari로 못 박았다.
     PM이 채울 것은 **회수·측정자·값**뿐이다. ⚠ 계측 이벤트가 코드에 **0줄**이라 수동 계측이다.

  ### ✅ §8 — **스모크보다 먼저 닫았다** (PM 결정 · `reports/W-22A.md` §12.1)
  🔴 **지시서 §8은 「§2~§6이 전부 끝난 뒤에만」이었으나 PM이 순서를 바꿨다.**
  근거 — **커밋 경계를 회차별로 유지**하기 위해서다. W-22B가 착수되는데 W-22A가 미커밋이면
  두 회차가 한 커밋에 섞이고 `v0.16.1`이 저장소에서 사라진다.
  버전 3곳(`package.json` · 이 파일 · S10 표시=`__APP_VERSION__`) → **`0.16.1`** ·
  커밋 1회 `SunDO v0.16.1` · **hosting만** 재배포(🔴 규칙은 다시 배포하지 않았다).

  ⚠ 🔴 **그 대가 — 스모크가 재는 번들이 `v0.16.0`이 아니라 `v0.16.1`이 된다.**
  두 번들의 차이는 **버전 문자열 하나뿐**이고(소스 diff 0줄), 오히려 **S10이 표시하는 버전과
  실제 배포본이 일치한 상태에서** 재게 된다. 🔴 **스모크 16(두 버전 건너뛴 갱신)은
  `v0.14.0` → `v0.16.1`로 세 칸이 되어 조건이 더 세진다** — 무효가 아니라 강해진다.

  🔬 **재배포 실측**(`23:16:54~23:17:31` KST) — `sw.js VERSION='0.16.1'` ·
  캐시 `sundo-shell-v0.16.1-4f4a9d20` · 번들에 `0.16.1` 3회 · **`0.16.0` 잔존 0회** ·
  🔴 **CSS 해시가 `index-CAouJt3E.css`로 불변** — 스타일 diff 0줄의 실측 증거다.
  ⚠ **§8 안에서 배포를 커밋보다 먼저 돌렸다** — 그래야 「재배포 확인」이 커밋되는 보고서에
  들어간다. 🔴 **로직이 바뀌는 회차에서는 이 순서를 쓰지 마라**(`reports/W-22A.md` §12.3).

  보고서 `reports/W-22A.md` · 실행 기록 `database_ToDo/W-22A.md`

### 🔴 W-22B(`v1.0.0`)가 받는 것 — **마지막 인수인계**
0. 🔴 **`reports/W-21C.md` §10이 정본이다.** 특히 **§10.2 — `v1.0.0`이 §17.1 MVP 완료
   조건을 채우는지 항목별 대조표.** 🔴 **채우지 못하는 것 셋(M-01 중앙값 · Lighthouse ·
   §14.5 14항목 대조표)은 전부 「측정」이고, 조항 개정이 필요한 것 셋(S9 편집 · 기록
   수정·삭제 · `dutySchedules` 수동 시딩)은 전부 PM 결정이다.**
1. 🔴 **App Check + BR-30 레이트 리밋은 짝이다** — 하나만 하면 의미가 없다.
2. 🔴 **승인 도메인 정리 · `localhost` 제거 · 프로덕션 프로젝트(§18.1) · Hosting 채널 ·
   커스텀 도메인(`D-19`)**.
3. 🔴 **개인정보 보호책임자 연락처가 유일하게 열린 콘솔 항목이다.**
   ⚠ **`reports/W-21B.md` §8.4의 「리전이 열려 있다」는 틀렸다** — `reports/W-19.md` §0이
   CLI로 `asia-northeast3`(서울)를 확인해 **닫았다**. 정정한다.
4. ⚠ **스모크·되돌리기는 W-22B가 아니라 W-22A 안에서 닫힌다** — 위 「진행 중」을 보라.
   `database_ToDo/W-22A.md` §3~§5가 절차 정본이고 18건 결과표가 거기 있다.
5. 🔴 **배포 순서를 실사용 시작 뒤에 다시 판단해야 한다**(W-21B §1.3).
   🔴 **결정 2로 이 프로젝트가 그대로 운영이 되므로, ②③ 창을 「무해하다」고 넘긴 것은
   W-22A가 마지막이다.**
6. 🔴 **W-22A가 새로 넘기는 것 3건**(`reports/W-22A.md` §10.2가 전문):
   ① 🔴 **규약 4-3의 확인 수단을 바꿔야 한다** — 지시서의 `diff firestore.rules /tmp/head.rules`가
   이 저장소(`core.autocrlf=true`)에서 **항상 494줄 전부 불일치**를 낸다. 거짓 경보가 반복되면
   **관문을 무시하게 되고 그때 그물이 삭는다.** ⇒ `git diff --numstat` + `git hash-object`로 교체.
   ② 🔴 **PRD §17.1 DoD·§18.2의 「Lighthouse PWA 항목 통과(설치 가능, 오프라인 응답)」 개정** —
   최신 Lighthouse(13.4.1)에는 **PWA 카테고리가 없고**(v11 고정으로 측정했다), 「오프라인 응답」은
   그 카테고리가 **애초에 재지 않는다.** 오프라인 근거는 W-20의 CP-02·EC-44다.
   ③ 🔴 **게시된 Rules 본문을 자동으로 읽을 수단이 없다** — 서비스 계정이 필요하고
   「키 커밋 금지」와 충돌한다. **판단이 필요하다.**

### 🔴 W-21에서 열린 채로 남은 것 — 🔴 **결함 3은 W-21C가 닫았다**
1. ✅ **결함 A(오프라인 집계) — W-21C가 닫았다.** 🔴 **「읽기 1,033건을 함께 풀어야 한다」는
   전제가 틀렸다** — S3 학년 버튼도 S4 반 버튼도 **인원 수치를 안 그리므로** 필요한 것은
   존재 여부뿐이고 `limit(1)`이면 학년당 1건이다. `(오프라인)` 표기도 이미 있었다.
   ⇒ `getCountFromServer` 먼저 → 실패하면 `getDocs` 폴백. **온라인 과금 증가 0.**
   ⚠ **오프라인 실기기 재검증은 아직이다**(`database_ToDo/W-21C.md` §4 15번).
2. ⚠ **결함 B(오프라인 S5) — 곁가지만 고쳤다. 원인은 v1.1.**
   W-21C가 고친 것: `getDocs`가 오프라인에서 빈 결과를 조용히 돌려줘 **EM-01 「이 반에
   등록된 학생이 없습니다」라는 거짓말**이 뜨던 것 → `fromCache`를 보고 ER-03으로.
   🔴 **관측된 ER-03의 진짜 원인은 여전히 미확정이다** — 기기가 필요하다(사용자 확정).
   ⚠ **v1.1에서 진단할 때 이번에 만든 `firestore/students-offline`과 원래의 `unavailable`을
   구분하라** — 곁가지 수정이 본체를 가릴 수 있다.
3. **실기기 1번**(Android 설치 → standalone) — 🔴 **기기가 없어서** 미측정. 앱 문제가 아니다.
4. 🔴 **체크리스트 8번의 합격 기준을 갱신해야 한다** — P-11 ②가 「미체크 시 비활성」을
   「활성 + 탭하면 안내」로 뒤집었다. `database_ToDo/W-20.md` §2 표가 낡았다.

### 🔴 P-0 — 다시 만나면 여기부터 읽어라
**`authDomain`은 앱이 실제로 서비스되는 출처와 같아야 한다.** 다르면 iOS 홈 화면 앱에서
로그인이 무한 루프에 빠진다 — `signInWithRedirect`가 manifest `scope` 밖으로 나가면서
iOS가 Safari를 따로 열고, OAuth가 **앱과 다른 브라우징 컨텍스트**에서 끝나
`getRedirectResult()`가 `null`을 돌려준다.
- 🔬 Hosting 사이트(`{site}.web.app`)에도 `/__/auth/handler`·`/__/auth/iframe`이 **그대로 산다**
- 값은 **`.env.local`**(커밋 대상 아님). 커밋되는 것은 `.env.example`의 주의 문단뿐
- ⚠ 바꾸기 전에 **GCP 승인된 리디렉션 URI**를 먼저 추가해야 한다 — 안 하면
  `redirect_uri_mismatch`로 **브라우저 탭 로그인까지** 막힌다
- ⚠ **`public/sw.js`는 이 문제와 무관하다**(가설 1 기각 — 보고서 §4)

### 🔴 서비스 워커가 캐시를 갖는다 — 되돌리는 법을 먼저 읽어라
`sundo-shell-v0.14.0-{목록 해시}`에 `/index.html` + `/assets/**`가 설치 시 담기고,
글꼴·아이콘은 쓸 때 담긴다. **낡은 워커가 남으면 낡은 셸을 붙들 수 있다.**
- ⚠ 다만 **내비게이션이 network-first**라 온라인이면 낡은 워커라도 새 셸을 받는다(보고서 §3.2)
- 해제 절차(캐시 포함) · 🔴 **자기 제거 워커 전문**: `database_ToDo/W-20.md` §1
- 🔴 **`/sw.js`를 404로 만들지 마라** · **`hosting:rollback`은 워커를 지우지 못한다**
- 🔴 **`chrome://serviceworker-internals`의 `Unregister`만으로는 캐시가 남는다** — 사이트 데이터도 지워야 한다

### W-21이 남긴 사실 (다음 회차가 재현하지 않아도 되는 것)

- 🔴 **`.blur-in`이 높이 사슬을 끊는다 — 이제 세 곳에서 이어 붙였다.**
  `ScreenTransition`이 `<div class="blur-in">`으로 화면을 감싸는데 그것이 `height:auto`라
  화면 루트의 `min-h-full`/`h-full`이 **무효가 된다**(W-18 §6.2).
  `.blur-in:has(> .pol)`(정책) · `.blur-in:has(.ft)`(Footer 3화면) ·
  `.blur-in:has(> [data-screen='S5'])`(S5 내부 스크롤). **`.blur-in` 본체는 한 번도 안 건드렸다.**
  ⚠ S1의 제목 5글자·부제도 `.blur-in`을 쓴다 — 선택자를 넓히면 그것들이 함께 걸린다.
- 🔴 **`min-height:100%`로는 내부 스크롤이 서지 않는다.** 사슬 **세 칸**이 전부 확정이어야 한다:
  `.blur-in{height:100%}` → `main{height:100%}` → `.list{flex:1;**min-height:0**;overflow-y:auto}`.
  `min-height:0`이 없으면 flex 항목의 기본 최소 크기가 콘텐츠 높이를 바닥으로 삼아 **안 줄어든다.**
- 🔴 **`.ft`는 이제 좌우 패딩이 없다.** 여백 소유자는 `AppShell`(좌우 22px)이다 —
  Footer가 자기 22를 또 주면 44가 된다. **여백을 더해 눈으로 맞추지 말고 소유자를 보라.**
- 🔴 **`.toast`는 `left:50%`만으로는 반쪽 폭이었다.** shrink-to-fit 가용 폭이
  `430 − 215 = 215px`로 잘려 **모든 토스트가 문구와 무관하게 2줄**이었다.
  `width:max-content` + `max-width:calc(100% - 28px)`로 고쳤다. **`nowrap`은 안 썼다** —
  사전이 늘어난 날 화면을 뚫는다.
- 🔴 **흰 글자에 광택을 걸려면 기저를 낮춰야 한다.** `background-clip:text`는 칠해진 곳만
  글자로 만들므로 기저 정지점이 투명이면 **글자가 사라진다.** `.shine`은
  기저 `rgba(255,255,255,0.82)`(대비 4.80:1) · 띠 `#fff`(6.24:1)이고
  두 선언은 `@supports` 안에 **함께** 갇혀 있다 — 지원하지 않으면 옛 거동으로 떨어진다.
- **`.skel`에는 애니메이션이 없다**(정지 그라디언트). shimmer로 오해하지 마라.
- **`.pill` 소비자는 `screens/Admin.tsx` 3곳뿐이다**(양도·승인·거절). 「공용이라 회귀가 넓다」가
  이 저장소에서는 성립하지 않는다. 지금 값은 `min-height:44px; padding:0 22px`(65.17×44).
- **S5는 `usePullToRefresh` 소비자가 아니다**(소비자 = S3·S7·S8·S9). 그래서 S5만
  스크롤 주체를 바꿔도 당겨서 새로고침이 안 깨진다.
- **아바타 자리는 5곳이다** — `.pend-avatar`(S2-1) · `.s2-avatar`(S2) · `.rs-avatar`(S6) ·
  `.mrow-av`(S8) · `.set-avatar`(S10). 🔴 **「일반 학생」 자리는 `.rs-avatar` 하나뿐**이고
  나머지 넷은 전부 가입 계정이다. S5 학생 목록에는 아바타가 없다.
- **긴 문구를 마침표로 나누는 순수 함수는 `src/lib/sentences.ts`다.**
  🔴 **마침표 뒤에 공백이 있을 때만** 나눈다 — 소수·판본·도메인·URL은 정의상 안 걸린다.
  소비자는 `ConfirmModal`과 S2 재가입 안내 2곳. **토스트·정책 문서에는 걸지 마라.**
- 🔴 **AC-01 위반이 아직 2곳 남아 있다** — `.dock-label`·`.s3-grade-sub`.
  결정 3이 「네 곳만」이라 손대지 않았다(보고서 §6 ⑦).
- 🔴 **`.gbtn`의 고정 높이 54px이 루트 32px에서 라벨을 넘친다**(실측). 목록 밖이라 안 고쳤다.

### W-21 측정 함정 (신규)

- 🔴 **Git Bash가 인자의 `/path`를 Windows 경로로 바꾼다.** `node x.mjs /login`이
  CDP에서 `Cannot navigate to invalid URL`이 된다 → **`MSYS_NO_PATHCONV=1`**을 앞에 붙여라.
- 🔴 **`vite preview`는 `127.0.0.1`이 아니라 `::1`에 붙는다.** 포트는 `Listen`인데 curl이 실패한다
  → **`http://localhost:4173`**으로 붙어라.
- 🔴 **인증 뒤 화면(S5·S8·S10)은 헤드리스로 열 수 없다.** 실제 `AppShell` 안의 `.blur-in`에
  **화면 구조를 주입**해서 재라 — 같은 스타일시트·같은 스테이지라 사슬이 그대로 재현된다.
- 🔴 **W-20 §1.2의 「SW 해제 → 재설치 → 로그인」 판별 실험은 이 코드베이스에서 오염된다.**
  `registerServiceWorker()`가 `load`에서 등록하고 `activate`가 `clients.claim()`을 부르므로
  **첫 실행에서 워커가 즉시 페이지를 잡는다.** 워커를 실험에서 빼려면 자기 제거 워커를 배포해야 한다.
- 🔴 **`git checkout -- <파일>`은 「임시 변경」만 되돌리지 않는다.** 아직 커밋하지 않은 회차에서는
  **그 회차의 정상 변경까지 HEAD로 돌린다.** W-21에서 검증용 `0.14.1`을 원복하려다
  `package.json`이 `0.13.0`으로 돌아간 채 배포됐다. **값을 직접 되돌려라.**
  W-20 §4가 `git checkout -- public/sw.js`를 쓴 것은 그 파일이 이미 커밋될 상태였기 때문이다 —
  **전제가 다르면 같은 명령이 다른 일을 한다.** 배포 뒤 `sw.js`의 `VERSION`을 눈으로 확인하라.
- 🔴 **`getCountFromServer`는 오프라인 캐시를 읽지 않는다.** SDK가 RPC로 직행한다(원문 확인).
  소비자 6곳(`stats.ts` 2 · `roster.ts` 1 · `records.ts` 3)이 전부 오프라인에서 죽는다.
- 🔴 **`getDoc`과 `getDocs`는 오프라인에서 정반대다.** `getDoc`은 캐시 미스 시 **reject**하고,
  `getDocs`는 `source:'server'`가 아닌 한 **항상 resolve**한다(빈 목록이라도).
  ⇒ **질의 실패는 예외가 아니라 「빈 결과」로 온다.** 「학생이 없습니다」로 오역하지 마라.
- 🔴 **16번(배너 무시)은 `sw.js` 주석만으로 잴 수 없다.** 자산 해시가 그대로면 화면이
  바이트 동일이라 「최신인지」를 눈으로 구분할 수단이 없다. **`package.json` 버전을 임시로
  올려 S10 표시를 마커로 쓴다** — 배너·자산 해시·눈에 보이는 증거를 한 번에 얻는다.

## 다음 작업
로드맵: W-21 완료 → **W-21B**(신규 기능 4건 · `v0.15.0`) → **W-22 배포**(`v1.0.0`).

- 🔴 **W-21B — 신규 기능 4건**(W-21 결정 1로 분리됨) · 지시서 대기 · `v0.15.0` 예상
  전제 조사는 **`reports/W-21.md` §7**에 끝나 있다. 요약:
  - **3 일정 편집 + 중식/석식** — `dutySchedules` update 규칙 + 🔴 **스키마 확장 →
    기존 2건(`2026-W34`·`W35`) 마이그레이션**
  - **4 학생 검색** — 🔴 **Firestore는 부분 문자열 검색을 못 한다.** 세 안(클라이언트 필터 1,033건 /
    prefix 질의 / 학년·반 선택 후 그 안에서) 중 **세 번째는 새 질의를 요구하지 않을 수 있다** —
    착수 시 먼저 확인하라
  - **9 기록 수정·삭제** — 🔴 `records` update/delete 신설 + **`recordCount` 감소**.
    `rules:test`의 **RC-3이 지금 「거부 기대」로 서 있어 함께 뒤집힌다.**
    `RECORD_UPDATE`/`RECORD_DELETE` 감사 `action`은 §9.3.8에 있으나 **호출부 0건** · IX-12
  - **12 코드 발급을 Dev 전용으로** — `isHead()` → `isDev()`.
    🔴 **Dev 부재 시 신입을 받을 수 없다**는 사실을 문서에 명시할 것(의도된 통제)
  - ⚠ **3·9는 PRD가 v1.1로 예약한 것**이다(§17.1). W-21B가 v1.0.0으로 끌어올린다 — st4R 항목
- **W-22 배포**(`v1.0.0`) — App Check · BR-30 레이트 리밋 · 프로덕션 프로젝트(`sundo-prod`) ·
  승인 도메인 정리 · Hosting 채널 분리. 🔴 **`authDomain` 규칙이 프로덕션에도 그대로 적용된다** —
  `sundo-prod`의 앱 출처와 `authDomain`을 반드시 일치시켜라(W-21 P-0).
- **접근성 회차는 범위에서 빠졌다**(W-21 결정 5) — §15·§17.1 개정은 **PM 소유**다.
  스크린리더 낭독 순서 · 포커스 이동 · W-03C 이월(토스트 수동 닫기)은 **v1.1**.

## 확정된 결정
- 🔴 **W-20 결정 1 — Workbox를 도입하지 않는다. 의존성 추가 0 유지.** (사용자 확정)
  결과: `public/sw.js` **실코드 59줄**(§9-1의 40줄 신호를 넘었다 — 보고서 §3.1에 수치와 근거).
  precache 목록은 **손으로 적지 않는다** — `vite.config.ts`의 `collectShell()`이 `dist`를 훑는다.
  🔴 **캐시 이름 = 버전 + precache 목록 해시.** 버전만 넣으면 같은 버전 재빌드에서 옛 캐시가 안 지워진다.
- 🔴 **W-20 결정 2 — 내비게이션은 network-first(3s 타임아웃), 자산은 cache-first.**
  「배너를 무시한 기기」의 답이 이것이다 — **온라인이면 매 로드마다 새 셸을 받으므로
  낡은 채로 남는 것은 워커 코드뿐이다.** §18.2의 업데이트 배너는 그대로 산다.
  🔴 **W-15A의 「임의의 타임아웃 금지」와 갈리는 근거**: 배치는 틀리면 기록이 사라지고,
  내비게이션은 틀려도 **자기 자산과 한 벌로 캐시된 직전 셸**을 그린다. 안전한 쪽으로 실패한다.
- 🔴 **W-20 결정 3 — BR-41 배지 + NT-07 플러시 토스트를 만든다.** (사용자 확정)
  🔴 **SDK가 대기 쓰기 건수를 노출하지 않는다** — 세션 카운터(필터 무관·재실행에 0)와
  스냅샷 `hasPendingWrites`(재실행에 남음·필터 안에서만)의 **최댓값**을 쓴다.
  통로를 `watchPendingCount`/`watchPendingFlush` **둘로 나눴다** — React가 자식 effect를
  먼저 돌려 S7이 `DockLayout`의 토스트를 가로채기 때문이다.
- 🔴 **W-20 결정 4 — 오프라인 쓰기 결론 2건은 「코드 0줄」이다.** (측정 결과)
  ① **배치 타임아웃**: W-15A의 판단을 **유지**한다(유지가 결론이다).
  ② 🔴 **탈퇴 + `signOut`**: SDK 원문 실측 — 큐는 **삭제되지 않지만 그 사용자의 큐로 남아
  다시 로그인하기 전에는 전송되지 않는다**(`localStoreHandleUserChange` · `IndexedDbMutationQueue.Kr(user)`).
  탈퇴자는 다시 로그인하지 않으므로 **오프라인 탈퇴는 사용자에게 거짓말이 된다.**
  ⇒ §8.11.5의 오프라인 비활성 가드는 **하중을 받는다.**
  ⚠ **PRD §14의 `clearIndexedDbPersistence()`는 구현돼 있지 않고, 그것이 옳다** — 부르면 큐가 진짜로 삭제된다.
- 🔴 **W-19 결정 1 — 앱 아이콘 원본은 `public/DSHS.png`(대신 학생자치회 엠블럼)다.** (사용자 확정)
  착수 시점 파일은 124×124 **학교 교표**였고 지시서 §9-4가 「배포하지 마라」로 규정해 멈추고 물었다.
  사용자가 390×390 자치회 엠블럼으로 교체하며 「쓴다」로 답했다.
  ⚠ **자치회/학교의 명시적 사용 승인 기록은 저장소에 없다** — `reports/W-19.md` §7-8.
  `public/favicon.ico`(64×64)는 **임시** favicon이다. `favicon.svg`(#863bff)는 팔레트 밖이라 참조하지 않는다.
- 🔴 **W-19 결정 2 — 글꼴은 공식 Subset woff2를 self-host한다.** (사용자 확정 · 파일 교체도 사용자가 했다)
  🔴 **오늘 CDN이 주던 것과 같은 파일이다**(바이트 완전 일치) — 그래서 글리프 커버리지·시각 변화가 **정의상 0**.
  `@font-face`를 **5개 다** 선언한 것도 그 1:1을 지키기 위해서다. 3개로 줄이지 마라 — 그 문장이 거짓이 된다.
  브라우저는 쓰이는 face만 받으므로 전송량 비용은 0이다(400/500/700 `loaded` · 100/300 `unloaded` 실측).
  🔬 **한글 11,172자 전량 커버**(`뷁`·`똠` 확인). 한자만 없다.
- 🔴 **W-19 결정 3 — design `10c`·`10d` 배너 2종을 만든다.** (사용자 확정)
  위치 **하단**(design 원문) — EC-24의 「상단」과 충돌하며 **design이 이겼다**.
  설치 배너는 **iOS 비-standalone에서만** — Android 문구가 §8.10에 없어 발명하지 않았다.
  **독이 있는 화면만**(`DockLayout` 1곳) — 좌표가 독 높이를 전제한다.
  🔴 **§18.2 「대기 후 탭」과 지시서 §3.3-3 「즉시 활성화」를 양립시켰다** — `install`에서
  `skipWaiting`을 자동으로 부르지 않고, 그 대기를 푸는 **경로**를 `SKIP_WAITING` 메시지로 확보했다.
- 🔴 **W-19 결정 4 — `reapply` `hasOnly`에 `agreedAt`·`agreedPolicyVersion` 2키를 넓혔다.** (사용자 확정)
  **계약 4-2 이탈**이다. `users` **create**는 `hasOnly`를 안 쓰지만 **재가입 update는 쓴다** —
  넓히지 않으면 재신청 배치가 통째로 죽고, 신규 가입에만 기록하면 재가입자 문서에
  **오늘 동의한 적 없는 옛 시각·옛 버전**이 남는다. 증명하려고 만든 필드가 반대를 증명한다.
  규칙은 **앱 배포보다 먼저** 재배포했다(§18.4). 에뮬레이터 **162/162**(UC-6·RE-7·RE-8 신규) +
  역검증 변형 `reapply-no-agreement-keys`가 RE-0·RE-0b·RE-7 정확히 3건을 빨갛게 만든다.
  ⚠ **소급하지 않았다.** 기존 계정 3개에는 두 필드가 **없다** — 그것이 사실이다.
- 🔴 **W-18 결정 1 — 문서는 구현에 맞춘다. 미구현 기능을 약속하지 않는다.** (사용자 확정)
  「~할 예정입니다」도 쓰지 않는다. 🔴 **design `20i`·`20g`의 본문 6문장이 실측과 어긋나
  사실로 교체됐다** — 시각 규격·섹션 제목·칩은 design 그대로다(DS-05를 보고한 뒤 확정).
  🔴 **처리방침은 「지금 사실」을 적고 있다.** §14.3 익명화(v1.2)가 구현되면
  「탈퇴해도 계정 정보가 남습니다」가 거짓이 되므로 **그 회차가 문서를 함께 고쳐야 한다.**
- 🔴 **W-18 결정 2 — OSS 목록은 `dependencies` 6개 + 폰트 1개다.** (사용자 확정)
  `devDependencies`(vite·tailwindcss·oxlint …)는 배포되지 않으므로 넣지 않는다.
  Spoqa Han Sans Neo는 **CDN으로 실제 배포되므로** 넣는다. Workbox는 **저장소에 없어** 뺐다.
  전이 의존성 55개는 열거하지 않는다(전량 허용형 · 카피레프트 0).
- 🔴 **W-18 결정 3 — 법적 검토는 PM 소유다.** 이 회차는 사실에 근거한 초안을 만들었다.
  🔴 **PM 확인이 필요한 문장 11건이 `reports/W-18.md` §8에 있다** — 국외 이전 · 미성년자 ·
  동의 사실 미보존 · 보관 기간 · 학생 정보주체의 권리 행사 경로 · 폰트 라이선스 원문 대조.
- 🔴 **W-18 결정 4 — 가입(S2)에 약관 동의 확인을 신설한다.** (사용자 확정)
  지시서 §0.5·§3.6이 금지한 범위였으나 **같은 문서 §9-2가 「동의 시점이 규격에 없으면
  멈추고 물어라」로 요구**했고, 규격 공백(코드 0줄 · PRD 조항 없음 · design 0건)을 확인한 뒤
  사용자가 신설로 확정했다. 🔴 **Firestore 필드는 만들지 않았다** — 화면이 막을 뿐이라
  「동의를 받았다」가 서버에 남지 않는다(보고서 §8-2). 문구 신규 1건:
  `개인정보 처리방침과 서비스 이용약관에 동의합니다`.
- 🔴 **W-18 결정 5 — Firestore 리전은 확인 전까지 문서에 적지 않는다.** (사용자 확정)
  PRD §9.1과 W-04 콘솔 지시서는 `asia-northeast3`(서울)를 말하지만 **실제 생성값의 확인
  기록이 저장소에 없다.** 「지시했다」와 「그렇게 만들어졌다」는 다른 문장이다.
  처리방침은 위치를 적지 않고 「Google이 운영하는 Firebase에 저장됩니다」까지만 쓴다.
  확인 요청은 `database_ToDo/W-18.md` §1이고, **국외면 이전 고지 문단이 별도로 필요하다.**
- 🔴 **~~W-17 결정 3 — 부장 탈퇴 차단을 「비활성 버튼 + 신규 문구 + 항상 표시」로 한다~~**
  → 🔴 **W-17 §2에서 철회됐다**(사용자 확정). 지우지 않고 이력으로 남긴다.
  **철회 근거**: design `20b`(채택 시안) · PRD §8.11.4 T-06 · §10.10 R-07 · BR-56 **네 곳이
  이미 서로 일치**했고 지시서의 결정만 달랐다. 채택본은 **`부장 권한을 먼저 양도해 주세요`** ·
  버튼은 **눌린다**(비활성이 아니다) · 문구는 **탭한 뒤 0.2s 펼침**이다.
  결정 3의 의도(다음 행동 안내 · 「눌렀는데 아무 일도 없다」 방지 · 버튼을 없애지 않음)는
  채택본이 이미 전부 충족한다. **신규 문구 0건**이라 §8.10 코드 부여도 필요 없다.
- 🔴 **Dev는 모든 권한을 소유한다 — 양도도 할 수 있다.** (W-15B 결정 2 — 사용자 확정)
  §4.1이 「Dev: 전 기능 접근 가능하나 **모든 행위가 감사 로그에 기록됨**」으로 규정한다.
  → `양도` pill은 `head`와 `dev` **양쪽에** 렌더된다. ⚠ R-02·BR-19의 문언(「본인 → member」)이
  Dev 경로에서 성립하지 않는다 — `reports/W-15B.md` §7 신규 1번.
- 🔴 **양도의 강등 대상은 「본인」이 아니라 `departments/{id}.headUid`가 가리키는 사람이다.**
  (W-15B §4-1 · 실측) 그래야 head 경로와 Dev 경로가 **한 형태**가 된다 —
  head 실행이면 `headUid == 본인`이라 R-02의 문언과 결과가 같고, Dev 실행이면 현 부장이 강등된다.
  🔴 **본인 uid로 추정하면 Dev 경로에서 강등 연산이 통째로 빠져 부장이 2명이 된다.**
  현 부장 문서가 없거나 `role !== 'head'`면 **그 연산만 빼고 승격 3연산으로 성공**한다
  (BR-23의 부장 0명 복구 경로가 실제로 그 상태다 — 거기서 막히면 복구할 방법이 사라진다).
- 🔴 **자기 행위로 강등될 때의 `refresh()`는 「이동 뒤」가 아니라 「언마운트 뒤」에 부른다.**
  (W-15B §4-2 · 실측 — **세 번 실패한 뒤 잡았다**)
  `navigate`의 라우터 상태 갱신은 `startTransition`이라 커밋이 미뤄지고, 그 사이 `refresh()`가
  `profile.role`을 바꾸면 **동기 갱신이 전환을 앞질러** 아직 커밋돼 있는 `/admin` 트리를 다시 그린다.
  그 렌더에서 `RequireRole`이 `denied`가 되고, 토스트는 큐가 없어 **TS-09가 TS-12
  「접근 권한이 없습니다」로 즉시 교체된다** — 사용자에게는 성공이 실패로 읽힌다.
  🔴 **다음 셋은 전부 실패했다** — ① `navigate` 뒤에 `refresh()` ② 바깥을 `ReactDOM.flushSync`로 감싸기
  ③ `await navigate(…, { flushSync: true })`. → **언마운트 정리 함수에서 부른다**
  (`refreshOnUnmountRef` + `useEffect(() => () => …)`). 그 시점에는 `RequireRole`도 사라진 뒤다.
- 🔴 **클라이언트 배치는 「부장 정확히 1명」(BR-18)을 보장하지 못한다.** (W-15B §4-3)
  네 연산은 원자적이고 커밋 전에 대상(BR-20)·현 부장을 `getDoc`으로 확인하지만,
  **확인과 커밋 사이의 창은 닫을 수 없다** — 같은 순간 다른 부장·Dev가 양도하면 부장이 2명이 된다.
  실질 보장자는 **Rules(§9.6 필수 조건 3)와 BR-23의 Dev 복구 경로**다 — `database_ToDo/W-15B.md` §4.
  ⚠ **BR-21(본인 role 변경 금지)을 규칙에 그대로 쓰면 양도가 죽는다** — head 실행 시 강등 대상이 본인이다.
- 🔴 **`14px`이 §7.2 스케일에 편입됐다 — `--text-rowsm`(`0.875rem`).** (W-15B 결정 3 ① — 사용자 확정)
  소비자 **5곳**(S4 `반` · 공용 모달 본문 · S6 일시 · S9 요일 행 · S8 행 이름)의 리터럴을 전부 옮겼다.
  `grep "font-size: 14px" src/index.css` → **0건**. 🔴 **`src/lib/cn.ts`의 `classGroups`에도 등록했다.**
  루트 16px에서 computed는 다섯 곳 모두 `14px`로 **불변**이고, 루트 32px에서는 `28px`로 스케일한다 —
  리터럴은 사용자 글자 크기 설정을 따르지 않았다. **회귀가 아니라 AC-05가 의도한 거동이다.**
- 🔴 **섹션 카드 `rise` 스태거는 「n번째 카드 = `0.05 × n`초」 규칙으로 계산한다.** (W-15B 결정 3 ③)
  design `9a`의 `.05/.1/.15/.2s`는 **섹션 4개 기준**이고 S8은 역할에 따라 섹션 수가 변한다.
  **실제로 렌더되는 섹션만** 세어 인덱스를 준다(숨은 섹션이 번호를 차지하면 첫 카드가 0.1s 뒤에 뜬다).
  🔴 지연은 인라인 `style`이 아니라 **커스텀 속성 `--rise-delay`**로 넘긴다 — 그래야 reduce가 이긴다.
- 🔴 **읽기 전용 안내는 「라벨 바로 아래」, 행동 힌트는 「카드 하단」.** (W-15B 결정 1 · design `9b`·`9c`)
  확정 문안 3종 — `승인 처리는 부장만 할 수 있습니다`(차장 ③) · `대기 중`(차장·교사 ③ 행) ·
  `열람 전용 계정입니다`(교사 ②·③). ⚠ **`대기 중`은 design `9c`에도 있어 교사 행에도 넣었다.**
  ⚠ `열람 전용 계정입니다`는 **S5가 이미 쓰는 문구**이고(§8.5.2 #5) S5의 「상시 힌트 자리를 대체하는
  한 줄」 방식을 따랐다 — design `9c`의 화면 상단 배너로 올리지 않았다.
  `대기 중`은 상호작용 요소가 아니라 **`Chip`**이다(`button`으로 만들지 마라).
- 🔴 **S8 ② 부원 목록은 `orderBy`를 서버에 보내지 않는다.** (W-15B §1-4)
  `where(departmentId==) + where(status=='active')` **등식 2개** + 클라이언트 정렬
  (**역할 권한 순 부장→차장→부원→교사→Dev → 이름 `localeCompare('ko')`**).
  IX-10의 `role ASC`는 **알파벳 순**이라 의미가 없고 인덱스도 미생성이다. **S8은 인덱스 0건이다.**
  ⚠ **`users`를 `list`로 읽는 것은 앱에서 이번이 처음이다** — 규칙 회차 재검증 M-1~M-3.
- 🔴 **부원 행 아바타는 「역할 첫 글자」다. 이름 첫 글자가 아니다.** (§8.8.3 #6 · design `9a`·`9c`·`9d`)
  `head`→`부` · `vice`→`차` · `member`→`부` · `teacher`→`교` · `dev`→`D`.
  부장·부원이 겹치지만 역할이 바로 아래 줄에 텍스트로 있고 아바타는 `aria-hidden`이다.
  ⚠ §9.3.1 NG-13 각주(「`name`의 첫 글자」)와 어긋난다 — **S10 §8.11.2 ①은 이름 첫 글자다.**
- 🔴 **`clearAdminCache()`를 넓혔다 — 여섯 번째 `clear*Cache`를 만들지 마라.** (W-15B §4-5)
  코드 캐시와 부원 목록 캐시를 함께 비운다. ⚠ **`clearHomeCache()`를 부르지 마라** —
  `stats.ts`의 `deptCache`는 `academicYear`·`classCountByGrade`만 담고 **`headUid`를 담지 않는다.**
- 🔴 **모든 쓰기는 `writeBatch` 하나로 묶는다 — 부분 성공이 없다.** (W-15A §4-1 · 실측)
  승인 4연산(`users`+`approvalRequests`+`inviteCodes`+`auditLogs`) · 거절 3연산 · 재발급 4연산.
  롤백 코드를 따로 쓰지 마라(`writeBatch`가 원자적이다 — `signup.ts` 선례).
  실측 — 3번째 연산에 `permission-denied` 주입 → **쓰기 로그 0건** · 네 문서 전부 이전 상태 · 화면 원복.
  🔴 **배치의 일부만 규칙에 걸려도 배치 전체가 거부된다** — 규칙 회차가 조합을 통째로 돌려야 한다.
- 🔴 **`batch.update`는 대상 문서가 없으면 배치 전체를 실패시킨다.** (W-15A §4-2)
  그래서 승인 배치는 `inviteCodes` 문서의 **존재를 미리 `getDoc`으로 확인**하고, 없으면
  **그 연산만 빼고** 진행한다. 🔴 **코드 문서 때문에 사람의 가입이 막히면 안 된다** —
  실측: `inviteCodeId`가 `null`이거나 코드 문서가 사라진 경우 **쓰기 3연산으로 승인 성공**.
- 🔴 **승인 시점에 코드 한도를 검사하지 않는다.** (W-15A 결정 1 — 사용자 확정)
  `useCount >= maxUses`여도 승인은 성공하고 `useCount`는 그대로 +1 된다(실측 30 → 31).
  BR-14의 실질 게이트는 **신청 시점(S2)**이고 승인은 부장의 명시적 판단이라 사람이 이미 게이트다.
  🔴 **한도 초과 경고 UI를 만들지 마라** — §8.10에 문구가 없다.
- 🔴 **「읽기 전용」은 `disabled`가 아니라 버튼의 부재다.** (W-15A §4-4 · §8.8.3 #10)
  차장·교사의 S8 ③은 행만 보이고 `승인`·`거절`이 **렌더되지 않는다**(실측 `pills: []`).
  `disabled`로 두면 스크린리더가 「버튼이 있는데 못 누른다」로 읽고 이유를 설명할 문구가 §8.10에 없다.
- 🔴 **초대 코드는 `crypto.getRandomValues()` + 거부 표집으로 만든다.** (W-15A §4-10)
  `Math.random()` **금지** — 예측 가능한 초대 코드는 초대 코드의 의미를 없앤다.
  혼동 문자는 **BR-17과 §9.3.3을 둘 다 만족**하는 쪽으로 갔다 — 알파벳 24종(`I O` 제외) + 숫자 8종(`0 1` 제외).
  🔴 `256 % 24 = 16`이라 `byte % 24`는 앞 16글자가 더 자주 나온다 — **상한 240 이상 바이트를 버린다.**
  실측 10,000회 — 형식 위반 0 · 금지 문자 0 · 충돌 0.
- 🔴 **감사 로그의 `before`/`after`에 개인정보를 담지 않는다.** (W-15A §3.7 · W-14 결정 3의 연장)
  `{ status: 'pending' } → { status: 'active' }` 같은 **상태 전이 요약**이면 충분하다.
  이름·이메일은 `targetId`(uid)로 역추적한다. 🔴 **읽는 화면은 만들지 않는다**(§17.1 — ④는 v1.1).
  `appendAudit`은 **배치에 넣기만 하고 커밋하지 않는다** — 따로 커밋하면 「본 연산은 실패했는데
  로그만 남는」 상태가 생긴다.
- 🔴 **오프라인 배치의 좁은 창은 막지 못한다 — 타임아웃을 발명하지 않았다.** (W-15A §4-7)
  §8.8.6의 「전 액션 버튼 비활성」이 정상 경로는 막는다(실측 버튼 6개 `disabled` · 강제 클릭에도 쓰기 0).
  🔴 **그러나 커밋이 시작된 뒤 끊기면 pill이 잠긴 채로 남는다**(실측 — 토스트도 행 변화도 없다).
  W-08 §5-7에서 스피너가 영원히 돌던 것과 같은 성질이다. **임의의 제한 시간은 「느린 네트워크」와
  「끊김」을 구분하지 못한다** → 오프라인 회차(W-20) 소유.
- 🔴 **승인 대기·부원 목록에 `orderBy`를 서버로 보내지 않는다.** (W-15A §1-4)
  `where('status','==','pending')` **등식 1개** + **클라이언트 정렬**(`createdAt` 내림차순).
  IX-11을 쓰면 미생성 인덱스에 의존하게 되고, 대기는 많아야 수십 건이다(W-12 등식 전용 설계와 같은 판단).
  **S8은 인덱스를 0건 요구한다.**
- 🔴 **S8 ③의 구독은 정당하다 — 「모든 조회에 구독을 켜지 않는다」의 예외 조건을 만족한다.** (W-15A §3.4)
  목록형이고 **변경 주체가 앱 안에 여럿이다**(다른 부장·Dev가 동시에 처리 · S2 신청이 실시간으로 들어온다).
  W-14의 S9(문서 1~2건 · 바꾸는 주체 없음)와 갈리는 지점이다.
  적용하는 구독 계약은 4개뿐 — `includeMetadataChanges: true` · 언마운트 해제 ·
  **캐시만으로 비어 있는 결과를 EM-04로 읽지 않기** · 신규 행 `rise 0.3s`. 커서·병합은 해당 없음.
- **`.pill`이 §15.1 조치로 올라갔다** — `min-height: 44px` · `padding: 0 13px` + flex 중앙 정렬.
  (W-15A §3 이탈 ⑦) §15.1이 적은 `padding: 12px 14px` 대신 **design `9a`의 좌우 13**을 썼다 —
  조치의 목적(44×44 타깃)은 달성되고 가로 규격은 시안과 일치한다. `.fchip`(W-13)과 같은 근거다.
- **§7.5의 「관리 화면 마지막 카드 130px」은 화면이 10px을 더해 만든다.** (W-15A §4-6)
  `AppShell`이 120px을 주고, `bottomGap` prop을 쓰려면 `router.tsx`가 handle을 읽어야 하는데
  둘 다 불가침이다. **여백 소유 계약은 그대로 두고 관리 화면에만 있는 차이만 화면이 갖는다**
  (`.adm-main { padding-bottom: 10px }`). 실측 120 + 10 = 130.
- **S8의 오프라인 안내는 네 번째 위험색 배너가 아니다.** (W-15A §4-5)
  전역 `.ofb`는 `AppShell` 소유라 화면이 문구를 바꿀 수 없다. `.adm-off`를 따로 만들되
  기하만 빌리고 **배경은 중립 틴트**를 썼다 — 오프라인은 오류가 아니라 상태다.
  🔴 **그래서 W-13 §0.1-4가 예약한 `.banner` 승격 트리거는 당겨지지 않았다**(위험색 3벌 그대로).
- 🔴 **회차 번호는 `prompts/W-14.md` §0.1 표가 정본이다.** (W-14 §0.1 · 사용자 확정)
  기존 문서 6편이 PRD 부록 C 번호로 「W-15가 규칙 배포」·「W-17 오프라인」·「W-18 접근성」을 쓴다.
  **앞으로 다른 작업은 번호가 아니라 이름으로 부른다** — 규칙 회차 / PWA 회차 / 오프라인 회차 /
  접근성 회차 / 배포 회차. ⚠ **로드맵이 `prompts/W-15A.md` §0.1에서 갱신됐다** — S8이 2분할되고 **규칙 회차가 W-16으로 당겨졌다**:
  W-15A S8 ③① → W-15B S8 ② → **W-16 규칙** → W-17 S10 설정 → W-18 정책 3종 →
  W-19 PWA → W-20 오프라인 → W-21 접근성 → W-22 배포.
  🔴 당긴 근거 4건은 `reports/W-14.md` §8-8이고 **사용자가 확정했다**(W-15A 결정 2).
- 🔴 **`getDoc`과 `onSnapshot`은 실패 방식이 반대다.** (W-14 §1-3 실측 · 실 SDK)
  서버에 못 닿고 캐시도 비었을 때 — `onSnapshot`은 **빈 스냅샷을 쏘고**(13ms · `fromCache: true`),
  `getDoc`은 **`unavailable`로 throw한다**(176ms · `Failed to get document because the client is offline.`).
  → 목록형(`onSnapshot`)은 「없음」과 「못 읽음」을 가르는 **추가 조건**이 필요하고(W-13 §4-5),
  단건(`getDoc`)은 **`exists() === false`에 도달했다는 것 자체가 서버 확인의 증거**다.
  🔴 **`{ source: 'server' }`를 판정 근거로 쓰지 마라** — 오프라인에서 조용히 캐시로 폴백한다(실측).
- 🔴 **모든 조회에 구독을 켜지 않는다.** (W-14 §4-1)
  W-13 §8-1의 구독 계약 7항은 **목록형 화면**의 것이다. S9는 문서 1~2건이고
  **MVP에서 그 문서를 바꾸는 주체가 앱 안에 없어**(§17.1이 `dutySchedules`를 「수동 시딩」으로 규정)
  구독을 켜도 두 번째 콜백이 평생 오지 않는다. `getDoc` + 당겨서 새로고침 + N-06으로 충분하다.
- 🔴 **BR-48의 공백을 이렇게 메웠다.** (W-14 §1-4 · 신규 확정값)
  BR-48은 「해당 월의 첫 월요일이 속한 주를 1주차」만 말하고 **그 이전 날짜가 몇 주차인지 침묵한다.**
  → **그 주의 월요일이 속한 달을 기준 달로 삼고, 그 달의 첫 월요일이 1주차다.**
  모든 주가 정확히 하나의 `(월, n주차)`에 대응해 중복도 공백도 없다.
  실측 14케이스(월 첫날 월/화/일요일 · 월 마지막 주 · 연말 `W53` · KST 자정) 전부 일치하고,
  **design `1j`의 `7월 4주차`가 독립 검증을 해 준다**(2026-07-31 금 → `7월 4주차`).
  🔴 `weekId`는 `toWeekKey` 반환값 **그대로**다. 주차를 화면에서 다시 계산하지 마라.
- 🔴 **담당자 이름은 `assigneeNames` 스냅샷이고 `users`를 읽지 않는다.** (W-14 §3.5 · BR-59)
  §9.3.6이 그 필드를 따로 둔 이유다. **S7의 탈퇴 취소선(BR-58)을 S9에 적용하면 안 된다** —
  BR-59가 「이미 편성된 과거 주차의 담당자 이름은 그대로 남긴다」로 **반대로** 규정한다.
  둘이 다르게 동작하는 것이 정상이다.
- 🔴 **S9에 편집 진입점이 하나도 없다.** (§17.1 MVP 제외 · W-14 §0.5)
  `일정 편집` 버튼도 EM-06의 `지금 등록하기`도 만들지 않았다. **비활성으로 두지도 않았다** —
  진입점이 아예 없어야 한다. 실측: 5역할 전부 `<main>` 안 `<button>` **0개**.
  design `6h`에 `지금 등록하기`가 그려져 있지만 옮기지 않았다.
- 🔴 **부서 기본값은 `duty.ts`가 직접 읽는다. `stats.ts`를 넓히지 않았다.** (W-14 §4-3)
  `fetchDepartment()`에 `patrolTime`·`patrolPlace`가 없는데, 거기에 얹으면 그 캐시가
  `clearHomeCache()` 하나로 묶여 있어 S9 새로고침이 홈 통계·명부까지 버린다.
  `fetchPatrolDefaults()`가 **두 필드만** 담아 돌려준다(결정 3의 클라이언트 규율을
  `users`가 아닌 컬렉션에 적용한 첫 사례). `DEPARTMENT_ID` 상수만 import한다.
- 🔴 **§9.6 「부원은 이름·역할 필드만」은 각주 + 클라이언트 규율로 닫는다.** (W-14 §0.3 결정 3)
  Rules의 `allow read`는 **문서 단위**라 필드 제한을 표현할 수 없다.
  → **부원 계정은 기술적으로 다른 부원의 `email`·`displayNameRaw`를 읽을 수 있다.**
  규율: **화면과 `lib/*`는 `users`에서 그 화면이 실제로 쓰는 필드만 반환값에 담는다.**
  각주 문장과 사례 3건은 `database_ToDo/W-14.md` §4에 그대로 적어 뒀다.
- **자정 감시 타이머를 만들지 않는다.** (W-14 §3.10) T-05의 문언은 「월요일 00:00 이후 **첫 진입 시**」다.
  마운트 시점의 `now`를 상태로 고정하고 당겨서 새로고침·재진입에서만 다시 계산한다.
  **캐시 키가 `weekId`라 주 경계가 저절로 갈린다.**
- 🔴 **실시간 구독에는 `{ includeMetadataChanges: true }`가 필수다.** (W-13 §1-3 P9 · SDK 소스 실측)
  `onSnapshot`의 기본값은 `false`이고, 그때 `QueryListener.shouldRaiseEvent`가
  「문서 변화 0건 + 메타데이터만 변함」을 **통째로 버린다**(`syncStateChanged`는 `includeMetadataChanges === true`일 때만 통과).
  = 캐시 스냅샷 이후 `fromCache: true → false` 전환이 **콜백으로 오지 않고** 동기화 칩이 영원히 「동기화 지연」이 된다.
  `waitForSyncWhenOnline: true`는 **`getDocs`/`getDoc` 전용**이고 `onSnapshot`에는 없다.
- 🔴 **구독은 첫 페이지 1개 · 과거는 정적 `getDocs` · 병합은 ID 기준 `Map` 합집합 · 커서는 첫 페이지 마지막에 고정.**
  (W-13 §3.7 · 실측) 커서를 스냅샷마다 갱신하면 밀려난 구간에 **구멍**이 생기고,
  `docChanges()`의 `removed`로 지우면 `limit` 창 밖으로 밀려난 문서까지 사라져 같은 구멍이 생긴다.
  🔴 그 대가로 **`status: 'deleted'`로 바뀐 문서가 목록에 남는다** — v1.1 삭제 회차가 「방금 지운 ID」를
  별도 제외 집합으로 처리한다(`reports/W-13.md` §8-4).
- 🔴 **구독 해제를 빠뜨리면 구독이 쌓이고 둘 다 콜백을 받는다.** (W-13 §1-3 P5 실측)
  독 이동은 `replace`라 화면이 언마운트된다. 실측 궤적: 기록→홈 왕복 3회 후 활성 구독 **1개**.
- 🔴 **캐시만으로 비어 있는 목록에 EM-02를 띄우지 않는다.** (W-13 §1-3 P2-b · §4-5)
  오프라인·빈 캐시에서 `onSnapshot`이 **13ms 만에** `size: 0` · `fromCache: true`를 쏜다.
  그것을 「기록이 없다」로 읽으면 W-10 §5-3이 막으려던 **「오늘 0건」이라는 거짓말**과 같은 종류다.
  서버가 확인했거나(`fromCache === false`) 캐시에 행이 있을 때만 목록을 확정하고, 그 전까지는 스켈레톤을 유지한다.
  **왜 기다리는지는 동기화 칩이 말한다**(`오프라인` / `동기화 지연`).
- 🔴 **`IntersectionObserver`의 `root`는 `ScrollRootContext`다.** (W-13 §4-2)
  지시서는 DOM을 거슬러 `overflow-y` 조상을 찾으라고 했지만 **그 노드를 노출하는 컨텍스트가 W-10부터 있다.**
  `AppShell` 수정 0줄이고 `usePullToRefresh`에 이어 **소비자 2호**다. `root`를 주지 않으면 뷰포트 기준이 되어
  스테이지 안에서 **절대 교차하지 않는다**.
- 🔴 **S7 카운터는 `records.ts`가 소유한다. `stats.ts`를 건드리지 않았다.** (W-13 §4-3)
  `stats.ts`의 캐시는 `clearHomeCache()` **하나로 묶여 있어** S7의 당겨서 새로고침이 홈 통계·부서·명부까지 버린다
  (W-11 §4-5가 `roster.ts`를 가른 것과 같은 이유). 대가로 `오늘`·`이번 주` 질의가 S3와 S7에 각각 있다 —
  **합치지 마라.** 합치려면 캐시를 먼저 쪼개야 한다.
- 🔴 **`Date` 산술로 오늘·어제를 판정하지 않는다. `dateKey` 문자열을 비교한다.** (W-13 §3.5)
  `toDateKey(now)`·`toDateKey(now - 24h)`를 만들어 **문자열 `===`**로 본다(`checkDuplicate`가 이미 쓰는 방식).
  표기는 `formatDateKeyLabel(dateKey)`가 키에서 직접 만든다 — 인자가 `Date`가 아니라 **문자열**인 것이 의도다.
- 🔴 **ST-04의 +1은 CountUp 바깥에서 더한다.** (W-13 §4-7)
  `useCountUp`의 `target`을 바꾸면 애니메이션이 **0부터 다시 돈다** — §12.2의 「진입당 1회」가 깨진다.
  `target`은 서버 값 그대로 두고 화면이 `value + delta`를 그린다. `aria-label`도 **최종값 + delta**로 만든다.
- 🔴 **작성자 상태 조회 실패는 「탈퇴 아님」으로 떨어진다.** (W-13 §4 · DoD 16)
  반대로 하면 멀쩡한 부원에게 취소선이 붙는다. **실패를 캐시하지도 않는다**(다음 조회에서 재시도).
  묶음은 `documentId() in [...]`이고 상한 **30**이다 — 🔴 **클라이언트 SDK는 개수를 검증하지 않아**
  31개를 넣어도 던지지 않고 실기기에서 `invalid-argument`로 터진다(W-13 §1-3 P8 실측).
- **`.fchip`이 §15.1 조치로 올라갔다** — `min-height: 44px` · `padding: 12px 14px`. (W-13 §3 어긋난 칸 6)
  design `20f`의 `padding: 8px 14px`(높이 약 33)보다 **§15.1이 우선한다** — 접근성 조치가 시각 원본을 이기는
  유일한 종류의 이탈이다. 좌우 14는 그대로라 한 줄 배치(§8.7.3)가 영향받지 않는다.
- **필터 칩의 활성 표시는 `aria-pressed`다(`radiogroup` 아님).** (W-13 §4-8)
  `radiogroup`은 로빙 tabindex + 화살표 이동을 갖춰야 규격에 맞는데, 그러면 칩 4개가 Tab 정지점 1개가 되어
  AC-09와 어긋난다. §15.3에 필터 칩 항목이 없어 사유 세그먼트 규칙을 끌어오지 않았다.
- 🔴 **W-12는 `records` 문서 1건만 쓴다. `users.recordCount`를 올리지 않는다.** (W-12 §0.2 결정 1)
  §9.6·부록 B의 본인 자기 갱신 허용 키에 `recordCount`가 **없다.** 지금 넣으면 W-04 임시 catch-all 덕에
  통과하다가 **W-15가 §9.6 본편을 배포하는 순간 기록 저장이 통째로 막힌다.**
  → **`recordCount`는 영원히 `0`이며 이것은 버그가 아니다.** 규칙 개정 요청은 `database_ToDo/W-12.md` §3.
  ⚠ 저장 경로 밖에서 `users/{uid}.lastActiveAt` 쓰기 1건이 관측되는데, 그것은 W-07 `useLastActiveAt`(DR-12)다.
- 🔴 **`setDoc`을 `await`하지 않는다.** (W-12 §1-3 실측)
  `persistentLocalCache`가 켜져 있으면 `setDoc`의 promise는 **서버 확인까지 resolve되지 않는다** —
  오프라인에서 `disableNetwork()` 뒤 8초가 지나도 `PENDING`이고, 같은 문서를 즉시 `getDoc`하면 11ms에
  `hasPendingWrites: true`로 돌아온다. SDK 원문도 「resolved once the write was acked/rejected by the backend」다.
  기다리면 음영 구간에서 스피너가 영원히 돈다(W-08에서 실제로 터진 실패).
  `lib/records.ts`의 `writeRecord()`는 **`void`를 반환한다** — 호출부가 실수로 기다릴 방법이 없다.
  **그래서 서버 거부(규칙 위반·시각 위조)는 이 계층에서 관측되지 않는다.** W-17이 이어받는다.
- 🔴 **30분 중복 검사는 등식(`in` 포함) 질의뿐이다. IX-06을 쓰지 않는다.** (W-12 §1-4)
  `where('studentDocId','==',…)` + `where('dateKey','in',[어제, 오늘])`. `orderBy`·범위 조건 **0개**라
  미생성 인덱스에 의존하지 않는다. 나머지 판정(30분 창·사유·`reasonText`·`status`)은 전부 클라이언트다.
  **항상 2일치를 읽는다** — 00:10 작성 시 30분 창이 전날 23:40까지 걸치는데, 분기하면 경계 버그가 생긴다.
  §8.6.3의 MD-05 판정도 **같은 결과 집합**을 쓴다(추가 질의 0회).
- 🔴 **중복 조회 실패는 「중복 없음」이 아니다.** (W-12 §3.8)
  `permission-denied`·`unavailable`·`failed-precondition`에서 **저장은 진행하고 MD-05는 띄우지 않는다.**
  기록 누락(P-02)이 중복보다 비싸고 재전송 중복은 멱등키(BR-02)가 흡수한다. `DuplicateVerdict`에
  `unknown` 갈래를 따로 뒀다 — `none`과 뭉개지 마라. 실패는 콘솔 경고 1건으로 남는다.
- 🔴 **`clientRecordId`는 시트 1회 오픈당 1개다.** (BR-02)
  마운트 시점의 `crypto.randomUUID()`이고 **MD-05 확인·차단 후 재시도에도 같은 값**이다(실측 5회 동일).
  시트 초기화는 `useEffect`가 아니라 **`key` 재마운트**가 한다 — `occurredAt`·`clientRecordId`·사유·기타 입력값이
  한 곳에서 초기화된다.
- 🔴 **세그먼트 선택 배경은 절대 위치 인디케이터 1개가 갖는다.** (W-12 §5-2)
  항목마다 배경을 토글하면 T-02의 0.22s 가로 슬라이드가 물리적으로 불가능하다.
  200% 2행(2+1)은 미디어 쿼리가 아니라 **`flex-wrap: wrap` + 항목 `min-width: 6.5em`**이 만든다(design 원문).
- **`.spark`에 `z-index: 51`이 붙었다.** (W-12 §4-5)
  `.ovl-root`는 스태킹 문맥을 만들지 않아(z auto·transform none·…) 파티클이 딤(40)·시트(50) **뒤로 깔렸다.**
  시트 위, 모달(55)·토스트(60) 아래다. **모달 위에서 파티클을 터뜨리는 화면이 생기면 이 값을 다시 봐야 한다.**
  `.ovl-root`의 `pointer-events: none`은 되돌리지 않았다.
- **바텀시트에 아래 스와이프 닫기와 `onClosed`가 생겼다.** (W-12 §4-2 · 착수 전 승인)
  그랩 핸들에서만 시작하고(시트 전체에 걸면 캐럿 드래그·세그먼트 탭과 뒤엉킨다), 임계값 **80px**는 **신규 값**이다.
  닫힘은 기존 `requestClose()` **한 경로**로 들어간다. S5 행 탭 빗장은 `onClosed`에서 푼다.
- **`--shadow-inset-soft`가 승격됐다** — `inset 0 1px 3px rgba(20,53,38,0.05)`.
  `.ff`에 이어 `.seg`가 두 번째 컴포넌트가 되어 W-03B §3 기준을 넘겼다. **§7.1 보완 토큰은 10개다.**
- 🔴 **S5 헤더는 `sticky` + 유리 띠다.** (W-11 §4-1 · QA-11-01 st4R 승인)
  `position:sticky; top:0; background: rgba(255,255,255,0.6); backdrop-filter: blur(22px) saturate(170%)`.
  **리스트에 자체 `overflow`를 걸지 마라** — 스크롤 요소는 `AppShell` 하나다(DoD 16).
  design `1f`·`20g`는 「고정 헤더 + 스크롤 본문」을 **패널이 스스로 자르는 방식**으로 풀어서 헤더 배경이 없다.
  단일 스크롤에서는 행이 헤더 뒤로 지나가므로 가림막이 필요했고, 값은 `.dock`(스크롤 위에 뜨는 유일한 선례)에서 가져왔다.
- **`sticky` 헤더는 상단 여백을 먹지 않는다.** (W-11 §4-2 실측)
  `top: 0`인데도 띠가 스크롤 영역의 **패딩 가장자리**에 선다 — 여백 26 → `y 26`, safe-area 59 → `y 85`,
  배너까지 → `y 141`. 세 경우 모두 스크롤 전후 불변이다. **화면이 상단 여백을 다시 계산할 필요가 없다.**
- **명부 조회는 `lib/roster.ts`가 소유한다.** (W-11 §4-5) `stats.ts`에 넣지 않았다 —
  캐시가 `clearHomeCache()` 하나로 묶여 있어 S3의 당겨서 새로고침이 명부까지 버린다.
  `fetchDepartment()`만 `stats.ts`에서 가져다 쓴다(모듈 캐시가 살아 있다).
- 🔴 **`isActive`는 서버 질의가 아니라 클라이언트에서 거른다.** (W-11 §2.2 B안 · 실측 W11-1 ≠ W11-2)
  `fetchStudents`의 서버 질의는 `academicYear + grade + classNo + orderBy('number')` = **IX-08 그대로**다.
  넣으면 PRD §9.5에 없는 신규 인덱스가 하나 는다. 한 반 최대 37명이라 초과 읽기가 무의미하게 작다.
  **S4가 센 수와 S5가 그린 행 수는 일치한다**(DoD 12로 잠갔다).
- **`orderBy('number')`는 `studentNo` 오름차순과 항등이다.** (W-11 §1-3)
  `academicYear`·`grade`·`classNo`가 등식으로 고정되면 DR-01의 앞 3자리가 상수다. 스텁 12건으로 실측했다.
- **`classNo` 상한 검증은 S5가 한다.** (W-11 §5-5) 부서 문서를 읽어야 해서 라우터에 둘 수 없다.
  `ClassGuard`(하한)와 **같은 문구(TS-13) · 같은 이동 방식(`replace`)** 을 쓴다.
- **스크롤 요소는 `AppShell`이 소유하고, 새로고침 정책은 화면이 소유한다.** (W-10 §5-1)
  `ScrollRootContext`가 **노드만** 노출한다. **`AppShell`에 `onRefresh` prop을 만들지 마라** —
  여백 단독 소유자에게 데이터 정책이 붙는다. 화면마다 «무엇을 다시 불러오는가»가 다르다(W-07 §6.1).
- **조회 실패를 「0건」으로 읽지 않는다.** (W-10 §5-3) `stats.ts`는 `throw`하지 않고
  `{ ok | failed }` 판별 유니온을 돌려준다. `0 건`은 「기록이 없다」이고 `-`는 「모른다」다.
  뭉개면 오프라인에서 **「오늘 0건」이라는 거짓말**이 뜬다(§0.4 예외 1).
  같은 규칙이 학년 버튼에도 적용된다 — **「명부가 없다는 것을 아는」 경우에만** 비활성이다.
  조회 전(`null`)이나 조회 실패는 활성으로 두고 통과시킨다.
- **T-02(탭 재진입)와 T-05(당기기)는 다른 함수다.** (W-10 §4-9)
  전자는 세션 캐시로 **애니메이션만** 다시 돌리고(네트워크 0건), 후자는 네트워크까지 간다.
  세션 캐시는 **모듈 수준**에 둔다 — S3는 탭을 옮길 때마다 `ScreenTransition`의 `key`가 바뀌어
  통째로 다시 마운트되므로 컴포넌트 상태에 두면 재진입마다 재조회가 돈다. **실패는 캐시하지 않는다.**
- **`sparkle`은 컴포넌트가 아니라 명령형 함수다.** (W-10 §4-6)
  T-03은 sparkle과 라우팅을 **같은 프레임**에 건다(design 프로토타입이 그렇게 한다).
  화면이 소유하면 `navigate()` 직후 언마운트되면서 0.5s 애니메이션이 첫 프레임에서 끊긴다.
  `.ovl-root`에 직접 붙여 소유자를 없애면 전환을 가로질러 550ms를 살아남는다.
  `fixed`가 아니라 스테이지 기준 `absolute`다(W-03B §4-9와 같은 이유).
  reduce에서는 **아예 만들지 않는다** — `animation: none`만 걸면 막대가 그대로 떠 있는다.
- **당김 인디케이터를 만들지 않았다.** (W-10 §4-7) §8.3.4 T-05의 애니메이션 열이 `—`이고
  §7.4에 없다. design 79프레임에서도 0건. 재조회 중에는 §8.3.5의 로딩 스켈레톤을 그대로 재사용한다.
  따라올 시각 요소가 없으므로 `preventDefault`도 부르지 않는다 — **리스너 전부 `passive`**라
  iOS Safari 바운스와 충돌할 지점이 없다. 임계값 `70px`은 **신규 값**이라 PRD 반영 목록에 올렸다.
- **`dateKey`·`weekKey` 카운터에 `academicYear` 필터를 붙이지 않는다.** (W-10 §1-4)
  두 키가 특정 일자·주차를 가리켜 학년도가 이미 결정되고, IX-03·IX-04에도 `academicYear`가 없다.
  **`students` 카운트에는 붙인다** — 이쪽은 조건에서 학년도가 결정되지 않는다(IX-09).
  🔴 **W-12가 `records`를 쓸 때 반드시 같은 `toDateKey`/`toWeekKey`를 써야 한다.**
  화면과 집계가 다른 규칙으로 키를 만들면 카운트가 영원히 어긋난다.
- **로딩 스켈레톤의 시각 원본은 design `6d`다.** (W-10 §4-2) §20.7이 `6a`~`6d`를 채택으로 적는다.
  `40 × 22px` · 반경 `8px` · 3정지점 그라디언트 · **반짝임 없음**. 같은 그라디언트가 `6a`~`6d` 전부에 쓰이므로
  **W-11이 두 번째 사용처를 만들면 공용 `.skel`로 승격한다.**
- **오프라인 배너는 오버레이가 아니라 흐름이다.** (W-09 §0.2 · 사용자 확정)
  `AppShell`이 **직접 소유**한다 — 포털도 `BannerRootContext`도 없다. 만드는 쪽이 없기 때문이다
  (전역 1개 · 조건이 `useOnline()` 하나). 배너가 **여백을 바꾸므로** 여백 단독 소유자와 일치한다.
  상단 오프셋 `16px`은 `top`이 아니라 **`margin-top`** 이다 — `.ofb`에 `position`·`top`·`left`·`right`를
  되살리면 `env(safe-area-inset-top)`이 **두 번** 들어간다. 스크롤 영역은 배너가 있을 때
  safe-area를 다시 더하지 않는다(`26px`/`34px`만).
  design 채택본 **`10b`**(§20.7 `스플래시·배너·토스트 = 10a~10f`)가 상단 정렬 화면에서 밀어내기로 그려져 있다.
- **스플래시는 배너를 덮지 않는다.** (W-09 §3-1 — 사용자 확정, 안 1)
  `.splash`는 **스크롤 영역**의 `absolute; inset:0`이라 배너가 밀면 함께 줄어 겹칠 기하가 사라진다.
  z 비교(`100 > 70`)는 **여전히 유효하다** — 스크롤 영역·오버레이 루트가 스택 컨텍스트를 만들지 않는다.
  판정 중 배너가 보이는 것이 정상이고 스플래시 콘텐츠는 **28px**(중앙 정렬이라 절반) 내려간다.
  **배너를 조건부로 끄는 코드를 넣지 마라.**
  🔴 스테이지·스크롤 영역·오버레이 루트 중 하나에 `transform`·`filter`·`opacity<1`·`isolation`·
  `contain`·`will-change`를 붙이면 z 관계(100 > 70 > 40 > 30)가 **조용히** 깨진다.
- **`.ofb`의 `pointer-events: none`은 선언으로 보존한다.** (W-09 §3-2 — 사용자 확정)
  흐름으로 나오면서 `.ovl-root` 상속이 끊겼다. `auto`로 두면 시트가 열린 동안 배너 띠(상단 40px)에서
  **딤 탭-투-클로즈가 죽는다.** 그래서 `elementFromPoint(배너)`는 딤을 반환한다 — 정상이다.
  「배너가 딤 위인가」는 **페인트 순서**(프로브에서만 `pointer-events: auto`)나 **차등 픽셀**로 재라.
  🔴 `backdrop-filter`·알파가 걸린 요소에 **픽셀 동일성 판정을 쓰지 마라** — 뒤에 깔린 것도 픽셀을 바꾼다.
- **OP-01은 안 1 + 안 2다.** (W-08 §0.2 결정 1 — 사용자 확정)
  안 1: `inviteCodes`의 **문서 ID가 코드 문자열 자체**다(`inviteCodes/DJSN-2691`). Rules는
  인증된 `@dshs.kr`의 **단건 `get`만** 허용하고 `list`는 `head`·`dev`로 계속 잠근다.
  안 2: `useCount` 증가가 신청 시점이 아니라 **부장 승인 시점(OP-09)** 이다 → **OP-01 배치는 2건**이다.
  `code` 필드는 남긴다(ID는 경로, 필드는 데이터). Rules가 `data.code == codeId`로 둘을 묶는다.
  🔴 **W-11이 OP-09에 증가를 붙이기 전까지 `useCount`는 영원히 0이고 BR-14가 동작하지 않는다.**
- **본인의 재신청 전이는 §9.6의 좁은 예외다.** (W-08 §0.2 결정 2)
  BR-61·BR-28이 규정한 `withdrawn`·`rejected` → `pending`을 §9.6 요약표가 막고 있었다.
  6조건(이전 상태 2종 · 새 상태 `pending` · `role='member'` · `withdrawnAt`·`rejectReason` null ·
  `anonymizedAt` null · 변경 필드 부분집합)을 **전부** 만족할 때만 연다.
  `createdAt`·`recordCount`·`email`·`uid`는 불변이다. **규칙 작성은 W-15**이고 요청서는 `database_ToDo/W-08.md`.
- **제출 버튼 활성 조건은 "9자 완성"이지 "검증 성공"이 아니다.** (W-08 §5-1)
  design `13d`(검증 실패인데 버튼 활성)와 `13b`(7자인데 비활성)를 동시에 만족하는 규칙이 이것뿐이고,
  "검증 성공일 때만"으로 하면 **다 치고 바로 누르는 동선에서 탭이 버튼에 닿지 않아 blur도 안 일어난다.**
  형식이 틀린 9자도 활성으로 두고 탭하면 형식 에러가 뜨게 한다. 서버 재판정은 제출 때 어차피 돈다.
- **`Field`는 값 변형을 스스로 한다.** (W-08 §5-2) `transform?: (raw, caret) => { value, caret }`.
  화면은 `input` 요소에 닿을 수 없다 — 캐럿 복원은 `Field`의 `useLayoutEffect`가 한다.
  **의존성 배열을 붙이지 마라** — 변형 결과가 직전 값과 같으면 리렌더가 생략되고, 그때 DOM에만
  버려진 원문이 남는다. `FieldHandle.reset()`은 "판정 없음"을 그리는 유일한 수단이다(조회 실패).
- **조회 실패를 "없는 코드"로 읽지 않는다.** (W-08 §3.1) `lookupInviteCode`는 `null`이 아니라
  **판별 가능한 유니온**(`ok`·`missing`·`unusable`·`failed`)을 돌려준다. `failed`는 인라인 에러가
  아니라 **상단 배너 ER-01**로 가고 필드는 검증 이전 상태로 되돌린다.
  🟡 단 `getDoc` 캐시 폴백 때문에 SDK 계층에서 무력화될 수 있다 — W-17 항목과 같이 봐야 한다.
- **S2에 오프라인 배너를 붙이지 않았다.** (W-08 §5-7) `.ofb`(`safe-area+16px`)가 S2의 헤더
  (`safe-area+26px`)와 **실제로 겹치는 첫 화면**이다. 겹침 해소는 `AppShell` 슬롯 신설이 필요해
  유예했고, 대신 §8.2.5의 **힌트 교체 + 버튼 비활성**으로 오프라인을 알린다.
  오프라인 제출을 막는 것은 미관 문제가 아니다 — `batch.commit()`이 오프라인에서 **resolve되지 않아
  스피너가 영원히 돈다.**
- **S2-1 승인 감지는 A안(`refresh()`)이다.** (W-07 §3 — B `onSnapshot`·C 화면 직접 조회 기각)
  `refresh()`가 `resolve()`를 재사용하므로 **W-17이 `getDoc` 경로를 고치면 함께 고쳐진다** —
  A의 약점으로 지목된 캐시 폴백은 A가 새로 만드는 위험이 아니다.
  B를 기각한 결정적 이유: 규칙이 아직 임시인 상태에서 W-17이 다시 볼 지점 위에
  **모든 인증 읽기의 형태를 바꾸는 베팅**이기 때문이다.
- 🔴 **W-12 부채**: `onSnapshot`으로 가면 **`refresh()`와 S2-1 폴링 루프를 걷어내야 한다.**
  전문은 `reports/W-07.md` §9. 지금 심는 부채이고, 이것이 A안의 값이다.
- **`refresh()`는 판정을 흔들지 않는다.** (W-07 §3.2) 실패해도 `status`·`profile` 그대로,
  `loading`으로 되돌리지 않는다(스플래시가 화면을 덮는다). 미인증에서는 즉시 반환한다.
  `resolve()`의 `{ silent }`가 그 두 줄을 건너뛴다. **`silent` 없이 재조회하지 마라.**
- **폴링 주기는 화면이 소유한다.** (W-07 §6.1) `AuthProvider`에 넣지 마라 — 화면마다 정책이 다르다.
  `pending`에서만·`visible`일 때만 돌고, 복귀 시 즉시 1회 후 주기를 재개한다.
- **`/signup`의 `allow`에 `rejected`가 있다.** (W-07 §4 — BR-28) 착지 표는 그대로다
  (`rejected`의 기본 착지는 여전히 `/pending`). **W-08은 진입 상태가 3가지다** — `noProfile`(신규)·
  `withdrawn`(재가입)·`rejected`(재신청).
- **S2-1 변형 B에는 보조 줄이 없다.** (W-07 §5-2) design 3b·PRD §8.2.6은 "있다"이지만
  §6.1이 `rejected`에서 폴링을 멈추므로 `자동으로 반영됩니다`가 사실이 아니게 된다.
  **되돌리려면 조건 1개.** PRD 반영 후보다.
- **`.neu`를 S2-1 때문에 바꾸지 않는다.** (W-07 §5-4) design 3a·3c에는 내부 링이 없고 눌림이
  `5px/12px`·`scale(0.97)`이지만 **PRD §7.3이 내부 링과 `scale(0.98)`을 명시**한다.
  `.neu`는 S4·S5·S8이 함께 쓰는 공유 표면이다. `.pend-busy`도 `.neu:active`와 같은 값으로 맞췄다.
- **스피너 트랙은 회전부 색의 알파 0.22다.** (W-07 §1.4) S1 `#2E6B4C`→`rgba(46,107,76,0.22)`,
  S2-1 `#1F5138`→`rgba(31,81,56,0.22)`. **두 색을 통일하지 마라.** 공유되는 것은 색이 아니라
  관계라서 토큰 1개로 표현되지 않는다 — 승격하지 않았다.
- **`@theme`에 커스텀 스케일을 추가하면 `src/lib/cn.ts`에도 반드시 등록한다.** (W-07 §5-6)
  W-06이 `--text-brand`·`--text-subtitle`을 추가하고 등록을 빠뜨렸다. CSS 클래스로만 쓰는 동안은
  증상이 없다가 **유틸리티로 처음 쓰는 순간 글자 크기가 조용히 사라진다.** W-07에서 등록했다.
- **`BrandLogo`가 84px 로고의 단독 소유자다.** (W-06 §4) Splash·S1이 공유한다.
  **크기·색 prop을 만들지 마라** — 두 사용처가 같은 값이다. 등장 모션만 `className`으로 받는다.
- **`.brand-logo`·`.brand-title`·`.brand-sub`는 Splash와 S1의 공유 클래스다.** (W-06 §5-5)
  이름에 `splash`가 없는 것이 의도다. 한쪽만 보고 고치면 다른 화면이 함께 깨진다.
- **타이틀 스태거는 두 화면이 다르다.** Splash `0/.09/.18/.27/.36`, S1 `.05/.13/.21/.29/.37`.
  design `10a`·`12a` 원문이고 **오류가 아니다.** 복사해서 맞추지 마라.
- **부제 문구도 다르다.** Splash `대전대신고등학교`, S1 `대전대신고등학교 · 부원 전용`.
- **Google `G` 로고는 4색 원본이다.** (§8.1.2 각주) 단색화·재색상화·`currentColor` 치환 금지.
  §7 팔레트 제약의 **유일한 예외**다. 흰 버튼에 `shine`을 쓰지 않는다(design R-01).
- **오프라인 배너는 콘텐츠를 밀어내지 않는다.** (W-06 §5-3 — PRD §6.4 우선)
  `absolute` · `z-70` · 상단 `calc(env(safe-area-inset-top) + 16px)` · 좌우 18px.
  16px은 독의 `bottom` 16px을 뒤집은 값이다(design 12d의 54px은 목업 상태 표시줄 포함이라 못 쓴다).
  **버튼·닫기를 붙이지 마라** — `.ovl-root`의 `pointer-events:none`을 되돌리지 않는 전제다.
  🟡 **독이 있는 화면(W-08 이후)에서는 제목과 겹친다.** 그때 `AppShell` 슬롯 안을 재판단한다.
- **연속 탭 차단은 상태가 아니라 ref로 한다.** (W-06 §5-4 — 실측에서 발견한 결함)
  `submitting` 상태와 DOM `disabled`는 같은 태스크 안의 연속 탭을 못 막는다(5회 탭 → `signIn` 5회).
  동기적으로 즉시 서는 빗장이 따로 필요하다. **다른 제출 버튼에도 같은 규칙을 적용하라.**
- **S1은 이동시키지 않고 `signOut`·`deleteUser`도 부르지 않는다.** (W-06 §2.2)
  도메인 불일치 처리는 `AuthProvider`가 이미 한다. S1은 배너·칩을 그리기만 한다.
- **상태 배너(C·E)와 시도 실패 배너는 동시에 뜨지 않는다.** (W-06 §5-6)
  `domainRejected`는 `auth/delete-user-failed`를 동반할 수 있어 둘 다 그리면 배너가 2개 쌓인다.
- **정책 3종은 인증 판정도 기다리지 않는다.** (W-06 §0.1-3 — W-05 DoD 9 개정)
  `RouteHandle.skipsAuthGate`. **다른 라우트에 붙이지 마라** — 로그인 화면 깜빡임이 N-05가 막으려던 것이다.
- **라우터는 `react-router` 단일 패키지다(v8.3.0).** (W-05 §4-3)
  `react-router-dom`은 v7부터 재수출 껍데기다. 설치하지 마라.
- **`AppShell`이 라우트 루트에 1개, 그 안에 `ToastProvider` 1개, 바깥에 `AuthProvider` 1개다.** (W-05 §4-2)
  화면마다 `AppShell`·`ToastProvider`를 두면 전환할 때 토스트가 사라진다(W-03C §7-5).
  `hasDock`은 라우트 `handle`에서 읽는다.
- **`AuthProvider`는 `router.tsx`의 `RootLayout` 안에 있다.** (W-05 §4-6)
  스플래시 오류 화면의 `다시 시도`가 `key`를 바꿔 재마운트해야 하기 때문이다.
  `window.location.reload()`를 쓰지 마라 — 서비스 워커가 없어(W-16) 오프라인에서 앱이 통째로 사라진다.
- **독은 `DockRootContext`로 스테이지에 포털한다.** (W-05 §4-2)
  `.dock`은 `absolute`라 스테이지 기준이어야 하는데 `DockLayout`이 반환하는 트리는 스크롤 영역 안이다.
  오버레이 루트를 재사용하지 마라 — `pointer-events: none`이라 탭이 죽고, 독은 딤(z-40) **아래**다.
- **`ScreenTransition`(`blurIn`)은 `DockLayout`보다 안쪽에 둔다.** (W-05 §4-4)
  바깥에 두면 탭 이동마다 독이 재마운트되어 활성 알약의 가로 슬라이드가 죽는다.
  **`blurIn`은 `fill-mode: both`라 끝난 뒤에도 `filter: blur(0)`이 남는다** —
  래퍼가 절대 위치 자손의 컨테이닝 블록이 된다. 화면 안 `absolute`는 스테이지 기준이 아니다.
- **상태 리다이렉트는 조용히, 역할 리다이렉트만 TS-12 토스트다.** (W-05 §4-5)
  모든 리다이렉트에 토스트를 붙이면 미인증 사용자가 앱을 열 때마다 `접근 권한이 없습니다`가 뜬다.
- **N-04 vs §8.5.3 충돌은 나눠서 처리했다.** (W-05 §4-1)
  `grade` 형식 오류 → `/`, `classNo` 형식 오류 → `/grade/:grade`. 둘 다 TS-13.
  N-01(한 단계 상위)의 정신을 따랐다. **N-04를 근거로 되돌리지 마라** — 요약으로 본다.
  경로 파라미터는 `Number()`가 아니라 `/^\d+$/`로 읽는다(`Number(' 2 ')`는 2다).
- **`pulseDot`은 design 10a(1.4s ease-in-out)가 아니라 PRD §7.4(1.6s ease)다.** (W-05 §5.2)
  design 안에서도 1.4s는 7곳 중 1곳뿐이다. 지연 스태거 `0/.2/.4s`만 design을 따랐다.
- **`lastActiveAt`만 쓰고 `updatedAt`은 올리지 않는다.** (W-05 §4-8)
  접속을 "프로필 변경"으로 기록하면 `updatedAt`이 DR-12 동기화 신호와 섞여 구분 불가능해진다.
  쓰기 실패 시 스로틀 창을 되돌린다.
- **디자인 원본은 `design/index.html`이다.** (2026-08-20 사용자 확정)
  리셋 전 원격에 있던 `index.html`은 커밋이 더 나중이었으나 **채택하지 않는다.**
  "최신본 채택" 규약을 이 파일에 적용해 원격본을 다시 꺼내오지 말 것.
- 폰트는 CDN 방식이다(지시서 §2 고정값, PRD §7.1).
  `https://cdn.jsdelivr.net/gh/spoqa/spoqa-han-sans@latest/css/SpoqaHanSansNeo.css`
- **`.fchip-on`은 PRD §7.3의 단색 `#1F5138`이 아니라 design의 그라디언트를 쓴다.**
  (2026-08-20 사용자 확정 — PRD 표기를 전사 오류로 판단). PRD로 되돌리지 말 것.
- **`.shine` 적용 규칙은 W-02에서 신규 확정한 값이다.** 전문은 `reports/W-02.md` §5.2.
- 간격은 커스텀 토큰 없이 Tailwind 기본 `--spacing` 배수를 쓴다.
- **`.pill-fill` 그림자 — 🔴 W-15B에서 재판단해 복원했다.** (W-15B 결정 3 ② — 사용자 확정)
  ~~W-03A 지시서 §0.3-2: 「그림자 없음은 의도된 차이다. W-03B에서 실제 목록 행에 렌더한 뒤에도
  어색하면 그때 다시 본다」~~ → 유예 조건이 충족됐다. `.pill`은 W-03A 이후 **소비자가 0개**였고,
  W-15A가 만든 S8 ③ `승인` pill이 **그 「실제 목록 행」의 첫 렌더**다(W-15A §4-8이 실측만 남기고
  판단을 요청했다). design `9a` 원문 **`box-shadow: 0 5px 12px rgba(31,81,56,0.28)`**로 복원했다.
  실측 computed `rgba(31, 81, 56, 0.28) 0px 5px 12px 0px`. 소비자는 여전히 S8 ③ 하나다.
  ⚠ `--shadow-primary`(`0 10px 24px …0.35`)와 값이 달라 **토큰을 쓰지 않는다.**
- **`cn()`은 `extendTailwindMerge`로 커스텀 스케일 3그룹을 등록했다.** (W-03A §1)
  `@theme`에 새 커스텀 스케일을 추가하면 `src/lib/cn.ts`의 `classGroups`에도 함께 등록해야 한다.
- **포커스 테두리 1.5px는 `border-width`가 아니라 inset 그림자로 낸다.** (W-03A §4-3)
  `border-width`를 바꾸면 padding box가 줄어 라벨 기준선이 흔들린다. 되돌리지 말 것.
- **`.ff-ro`의 "테두리 없음"은 `border: 1px solid transparent`다.** (W-03A §4-1)
  `border: 0`으로 지우면 읽기 전용만 라벨 좌측 기준선이 1px 어긋난다.
- **reduce에서 스피너는 정지시키지 않는다.** (W-03A §4-4) 진행 중을 알리는 유일한 피드백이다.
- **성공 상태는 blur에서만 켠다.** (W-03A §4-2 → **W-03B §1에서 수정**)
  `ref.validate()`(제출)도 성공 체크를 켜지 않는다. 제출 시점의 주인공은 에러다.
  단 **켜져 있던 성공을 끄지도 않는다** — 해제는 값 변경만 담당한다.
- **`-webkit-` 접두사를 손으로 적지 않는다.** (W-03B §4-1)
  표준과 나란히 적으면 lightningcss가 둘을 같은 속성으로 보고 **표준 선언을 지운다.**
  표준만 적으면 접두사판을 자동으로 덧붙인다. `backdrop-filter`에서 실제로 터졌다.
- **reduce에서 덮어야 하는 값은 인라인 `style`로 주지 않는다.** (W-03B §4-3)
  인라인은 미디어 쿼리가 이길 수 없다. 커스텀 속성(`--rise-delay`)으로 넘긴다.
- **`ResizeObserver` + `setState`는 결과를 폭에서 직접 계산한다.** (W-03B §4-2)
  직전 상태에서 한 칸씩 움직이면 경계 폭에서 무한 루프가 된다(React #185 — 실제로 380px에서 앱이 죽었다).
  `space-around`의 항목 사이 간격은 `free / n`이다(`n-1`이 아니다).
- **독 활성 배경은 항목이 아니라 절대 위치 알약 1개가 갖는다.** (PRD §6.2 · design `15d`)
  항목마다 배경을 켜고 끄면 가로 슬라이드가 불가능하다.
- **독 비활성 라벨 `rgba(20,53,38,0.5)`은 §15.2 AC-01과 충돌하는 채로 뒀다.** (W-03B §7)
  §6.2 원문을 따랐다. **임의로 0.70으로 올리지 말 것.** W-18 대비 실측에서 재판정한다.
- **Footer 링크 44px은 투명 패딩 + 음수 마진으로 낸다.** (W-03B §4-7)
  `padding: 15px 0` + `margin-block: -15px`. 시각 높이 14px이 유지돼야 접힘 줄 간격이 10px이 된다.
- **`fs.rmSync`가 이 환경(Node v24.2.0/Windows)에서 프로세스째 죽는다.** (W-03C §4-1)
  재귀가 아니라 파일 1개여도 exit 127(`0xC0000409`). `fs.promises.rm`·`unlinkSync`는 정상이다.
  vite `emptyOutDir`가 `rmSync`를 쓰기 때문에 `dist/`가 남아 있으면 빌드가 통째로 죽었다.
  **W-03B가 OneDrive를 원인으로 지목한 것은 오진이다.** `build` 스크립트가 선삭제로 막는다.
- **오버레이 3종은 스테이지 안 `absolute`다.** (W-03C §3·§4-2)
  `AppShell`의 오버레이 루트에 `createPortal`한다. `fixed`로 두면 430px 밖에서 스테이지를 벗어난다.
  루트는 스크롤 영역의 **형제**여야 딤이 배경 스크롤을 막는다. 루트에 `z-index`를 주지 마라.
- **포커스 트랩은 `open && mounted`로 건다.** (W-03C §4-3)
  `open`만 보면 마운트 전 렌더에서 ref가 비어 조기 반환하고, 이후 다시 돌지 않아 트랩이 죽는다.
- **토스트는 큐가 없다.** (W-03C §4-10) 새 메시지가 기존을 즉시 교체하고 `key`가 바뀌어 모션이 다시 돈다.
- **모달 버튼에 `Pill`을 쓰지 않는다.** (W-03C §4-8)
  "line/fill 스타일"은 표면 처리를 가리키는 말이고 모달 버튼은 높이 48 · radius 14다.
- **확인 모달의 초기 포커스는 좌측 `취소`, 딤 탭·ESC도 취소다.** (W-03C §4-7)
  파괴적 버튼에 포커스를 두면 Enter 오조작이 난다. 좌 버튼 라벨은 항상 `취소`이며 prop으로 열지 않는다.
- **충돌 해소 순서가 확정됐다.** (W-04 지시서 §0.2)
  1) PRD가 명시한 값이 있고 design이 그 손실 없는 이형이 아니면 → **PRD**
  2) PRD 표기가 design의 축약·전사로 보이면 → **design**
  3) PRD가 침묵하면 → **design**(DS-01)  4) 둘 다 없으면 → 멈추고 질문(QA-01)
  이 기준으로 시트 모션은 PRD(`translateY(110%)`) 유지, 시트 내부 하이라이트는 design(`0.9`)으로 되돌렸다.
- **인증은 Google Provider 단독이다.** (PRD C4 · §9.1 · W-04 §4)
  `email_verified` 검사 · 인증 메일 발송 · 비밀번호 API/필드를 **만들지 마라.** `users`에 비밀번호 필드가 없다.
  **이메일 로컬 파트를 분해하지 마라** — `25_yij1029@dshs.kr`의 `1029`는 생일이다(DR-14 · PR-05).
  **프로필 사진 URL을 저장하지 마라**(NG-13). 아바타는 `name` 첫 글자로 만든다.
- **도메인 거절은 `deleteUser` → `signOut` 순서다.** (W-04 §5.3)
  뒤집으면 권한이 사라져 계정이 그대로 남는다. `deleteUser`가 실패해도 로그아웃은 반드시 한다.
- **도메인 정규식 끝의 `$`는 필수다.** (§9.6 필수 규칙 1)
  없으면 `attacker@dshs.kr.evil.com`이 통과한다. 코드(`/@dshs\.kr$/`)와 Rules 양쪽 모두.
- **DR-10 교차 검증은 MVP 유보다.** (W-04 부칙 §1 — 선택지 A 확정)
  DR-14와 정면 충돌은 아니지만(문언은 생일 4자리·화면 노출을 겨냥) 비용/편익으로 접었다.
  산출물이 아무도 읽지 않는 `console.warn`이고, 유급·전입이 정당한 예외라 오탐이 기본값에 가까우며,
  학번 신선도는 DR-12 재파싱이 이미 덮는다. **"절대 분해하지 않는다"가 "앞 2자리만 분해한다"보다
  지키기 쉬운 규약이다.** DoD 22(`이메일 로컬 파트 분해 0건`)는 그대로 유효하다.
  PRD v1.3의 `[MVP 유보]` 표시는 **PM이 한다. PRD를 고치지 마라.**
- **W-15 주의: §9.6 요약표 ≠ 부록 B 전문.** (W-04 부칙 §2)
  자기 갱신 허용 키는 **부록 B가 정본**이다(`docs/SunDO_PRD.md:2866` — `name`·`memberStudentNo`
  계열 포함, `:2915` 주석이 근거). §9.6 요약표만 보고 Rules를 쓰면 **DR-12 동기화가 막힌다.**
  요약과 전문이 다르면 전문이 정본이다. 요약표 보정은 PM이 v1.3에서 한다.
- **도메인 거절은 인증 상태에서 파생시키지 않는다.** (W-04 §4-2 — 결함 수정)
  `signOut`이 `onAuthStateChanged(null)`을 발화시키면 시퀀스 가드에 걸려 `domainRejected`·
  `rejectedEmail`이 **아예 기록되지 않았다.** `rejectionRef` 래치를 `deleteUser` **전에** 세우고
  `clearRejection()`·정상 로그인에서만 푼다. T-05 배너·칩·버튼 라벨이 이 값에 달려 있다.
- **`users/{uid}` 조회 실패는 `noProfile`이 아니다.** (W-04 §4-3)
  문서 없음(`exists()===false`) → `noProfile`, 읽기 실패 → `loading` + `errorCode`.
  뭉개면 오프라인·규칙 오류일 때 활성 부원이 가입 신청 화면으로 튕긴다.
  ~~**W-06 전제**~~ → **W-05가 끝냈다.** `Splash`가 3초를 넘기면 `errorCode`로 오류 화면을 띄운다(§8.1.5).
  🟡 단 §9의 캐시 폴백 위험 때문에 이 분기가 실제로는 안 탈 수 있다 — 아래 W-17 항목을 함께 봐라.
- **DoD 18(리다이렉트 왕복)은 환경 제약이면 `보류(환경)`로 표시한다.** (W-04 부칙 §3 — 사전 승인)
  `signInWithRedirect`는 최신 Chrome의 서드파티 쿠키·스토리지 분할 정책 때문에 `localhost`에서
  `getRedirectResult()`가 `null`로 돌아올 수 있다. 호출은 정상인데 결과만 비면 **환경 제약이다.
  코드를 고치려 들지 마라.** W-19(배포 도메인)에서 재검증한다.
  폴백 **분기 로직**(standalone 감지 · 3개 에러 코드 → redirect)은 코드로 검증해 따로 보고한다.

## 예약된 작업 — 회차가 이미 정해진 것 (열린 질문이 아니다)

### 인라인 배너 `.banner` 승격 (W-13 지시서 §0.1-4)

**같은 값의 배너 선언이 이미 세 벌이다** — `.s1-banner`(W-06) · `.s2-banner`(W-08) · `.rs-banner`(W-12).
W-12 §4-4의 「새 표면을 만들지 않았다」는 승인됐고 **시안은 요청하지 않는다**(값이 같으므로 새 디자인이 아니다).
🔴 **다음에 인라인 배너를 쓰는 화면이 `.banner`로 승격한다**(W-11 §0.1-1과 같은 규칙).
S7은 배너를 쓰지 않아 W-13에서 손대지 않았다.

### W-18 접근성·모션 점검 이월 3건 (W-09 지시서 §5)

| # | 항목 | 출처 |
| --- | --- | --- |
| 1 | **토스트에 수동 닫기 수단 없음** — 스크린리더가 낭독하는 중에 2.2초로 사라진다 | W-03C §7-4 (W-07 지시서 §0.1 회신 4에서 W-18 이월 확정) |
| 2 | **`승인 확인` 쿨다운 중 남은 초 미표시** — 왜 눌리지 않는지 알 수 없다 | W-07 §7-5 |
| 3 | **검증 중 성공 테두리 선점** — `settleY`가 판정 전에 0.2s 조기 재생된다 | W-08 §5-3 (W-09 지시서 §0.1-5에서 **현행 유지 · W-18 후보 확정**) |

3번의 대안(조기 재생 억제)은 **규격에서 더 멀어진다**는 W-08의 분석이 채택됐다. 지금 고치지 않는다.
독 비활성 라벨 `rgba(20,53,38,0.5)` ↔ §15.2 AC-01 충돌 재판정도 **W-18 대비 실측**에서 함께 본다
(위 「확정된 결정」의 W-03B §7 항목).

### S6 기록 작성 시트 회차 — `Field` 계약 정리 (W-09 지시서 §0.1-1)

**열린 질문이 아니라 예약된 결정이다.**
`Field`의 `transform` prop과 `FieldHandle.reset()`은 승인됐다. 다만 **진짜 결함은 `reset()`이
없었다는 것이 아니라, 비동기 판정이 들어올 선언적 통로가 `Field`에 없다는 것**이다 —
`validate`가 `string | null` 2값이라 "판정 없음"을 표현할 수 없어, 조회 실패를 성공(거짓말)이나
실패(정상 코드에 오탐)로밖에 그리지 못한다. `reset()`은 그 구멍을 **명령형으로** 메운 것이고,
화면이 늘면 화면마다 다른 escape hatch가 생긴다.

**지금 고치지 않는다** — 실사용 소비자가 S2 하나뿐이라 설계할 근거가 부족하다.
~~S6 회차에 소비자 2개를 놓고 `Field` 계약을 정리한다.~~

🔴 **W-12에서 검토했고 전제가 성립하지 않아 다시 미뤘다**(`reports/W-12.md` §4-15).
S6의 `기타 사유`는 **완전히 동기적**이다 — 서버 조회가 없고 `checking`도 `reset()`도 쓰지 않는다.
즉 **두 번째 소비자가 그 구멍(비동기 「판정 없음」)을 재현하지 않아** 설계 근거가 여전히 S2 한 건뿐이다.
W-12가 넓힌 `counter?`는 표시 슬롯이라 `validate` 2값 문제와 무관하다.
→ **비동기 판정을 실제로 쓰는 두 번째 소비자**(S8 학생 임포트 검증 또는 S9 검색)가 생기는 회차로 옮긴다.

## 막힌 지점 / 사용자 확인 필요
- ✅ **`inviteCodes` 콘솔 시딩 요청을 철회했다.** (`database_ToDo/W-15A.md` §1)
  🔴 **이제 앱이 첫 코드를 만든다** — 부장 계정 `/admin` → `코드 재발급`이
  코드 문서 8필드 + `departments.activeInviteCodeId` + 감사 로그를 **한 배치로** 정확히 채운다.
  손으로 넣으면 `data.code == codeId` 불일치가 나기 쉽고, 그러면 규칙 배포 후 코드 조회가 통째로 막힌다.
  `database_ToDo/W-08.md` §3의 ③④는 **더 이상 실행하지 않아도 된다.**
- 🔴 **승인 대기 화면을 실기기로 보려면 두 번째 `@dshs.kr` 계정이 필요하다.**
  (`database_ToDo/W-15A.md` §2) 콘솔로 `approvalRequests`를 손으로 만들지 마라 —
  `users/{uid}`와 짝이 맞아야 하고 승인 배치가 그 둘을 함께 고친다.
  **순서: ① 앱에서 코드 발급 → ② 두 번째 계정으로 S2 가입 신청 → ③ 부장이 승인.**
- 🔴 **규칙 회차(W-16)에 넘길 재검증이 총 40건으로 마감됐다.**
  W-12 §4(10) + W-13 §4(2) + W-14 §3(1) + W-15A §4(11) + W-15A §4.5 배치 조합(1) +
  **W-15B §3(14) + W-15B §3.5 배치 조합(1)**.
  🔴 **`database_ToDo/W-15B.md` §5가 그 표의 정본이고 규칙 회차는 그 문서 하나만 보면 된다**
  (S8이 MVP 범위에서 끝나 **전량을 거기서 마감했다**). 함께 처리할 결정 3건도 §5.1에 있다.
  ⚠ **배치 조합을 개별 규칙 테스트로 대체하지 마라** — 배치는 4개 컬렉션을 동시에 건드리고
  **하나라도 걸리면 전체가 거부된다.** 🔴 **양도는 4연산 조합과 3연산 조합(BR-23 복구) 둘 다 돌려야 한다.**
- 🔴 **권한 양도를 실기기로 확인하려면 승인된 두 번째 계정이 필요하다 — 세 번째 계정은 필요 없다.**
  (`database_ToDo/W-15B.md` §1) 순서: **① 앱에서 코드 발급 → ② 두 번째 계정 가입 신청 →
  ③ 부장이 승인 → ④ 부장이 그 계정에게 양도.** ③까지 끝나기 전에는 ②에 EM-05
  `양도할 수 있는 부원이 없습니다`가 뜨는 것이 정상이다 — 버그가 아니다.
  🔴 **양도는 앱에서 되돌릴 수 없다** — 넘긴 사람은 더 이상 부장이 아니다.
  **Dev 콘솔 복구 절차(`users`×2 + `departments.headUid`를 한 번에)가 `database_ToDo/W-15B.md` §1.4에 있다.**
  확인이 끝나면 **반드시 §1.4로 원상 복구할 것.** `auditLogs`는 지우지 않는다(§9.6 필수 조건 6).
- 🔴 **`dutySchedules` 문서가 0건이라 실기기 S9는 EM-06이다 — 버그가 아니다.**
  (`database_ToDo/W-14.md` §1) 요청서가 **다른 세션이 단독 실행할 수 있는 형태**로 적혀 있다 —
  문서 ID `2026-W35`(2026-08-24~28 · `8월 4주차`)와 선택 `2026-W34`, 필드 10개, 타입, 입력 순서,
  맵 키 `mon`~`fri`(🔴 **문자열이다** — 숫자 키는 W-09 §7-3 사고와 같은 자리), 부장 uid까지.
  🔴 **S9는 인덱스를 0건 요구한다** — 아래 IX-01·IX-02 작업과 **완전히 독립**이다.
- 🔴 **IX-01 · IX-02를 만들어야 S7 목록이 실기기에서 돈다.** (`database_ToDo/W-13.md` §1)
  W-13이 내는 질의 형태로 5건을 다시 갈랐다 — **지금 필요한 것은 2건**이고
  IX-03·IX-04·IX-05는 **등식 2개뿐이라 없어도 돈다**(W-10 실측 선례). IX-06은 여전히 소비자가 없다.
  🔴 **만들기 전까지 S7 목록은 ER-02 `기록을 불러오지 못했습니다`다 — 버그가 아니다.**
  `failed-precondition`이 EM-02로 새지 않도록 설계했다(W-13 DoD 17 — 3코드 전부 ER-02 실측).
  생성 절차(오류 링크 / 콘솔 경로)는 요청서에 있다. **필드 순서가 곧 인덱스다.**
  W-12 자신은 등식 전용 질의라 인덱스가 **0건 필요**하지만, 그 사실을 실 Firestore에서 확인하지는 못했다
  (쓰기 0건 제약). 명부 시딩 후 실기기 저장 1회로 확인 요청.
- ✅ **`users.recordCount`는 A안으로 확정됐다.** (W-13 §0.2 결정 1 · `database_ToDo/W-13.md` §2)
  §9.6 자기 갱신 허용 키에 `recordCount` 추가 + `request.resource.data.recordCount == resource.data.recordCount + 1`.
  🔴 **실행 순서가 전부다** — ① W-15가 규칙을 열고 → ② **같은 회차에** S6 저장 경로에 증가 한 줄 →
  ③ v1.1 삭제 회차가 감소. ②를 먼저 하면 W-15 배포 순간 저장이 통째로 막히고, ③을 먼저 하면 값이 음수가 된다.
  **W-13은 그 코드를 한 줄도 붙이지 않았다.** 그때까지 값은 `0`이며 버그가 아니다.
- ✅ **~~학생 명부 시딩 — A안(콘솔 1개 반)~~ → 🔴 W-16D가 전교생 1,033건으로 닫았다.** (W-13 §0.2 결정 2)
  `2026-08-23T23:55` · `scripts/seed-students.mjs --prune --commit` · **1,033/1,033 · 실패 0 · 2.7초**.
  W-12 §6의 9건(`2026_20301~09`)은 임포트 범위 안이라 **덮어써졌다**. IX-01·IX-02도 배포됐다.
  아래는 당시 배경이다.
  W-13은 실 Firestore에 접속하지 않아 확인할 방법이 없다. **시딩 후 S7까지 보려면 IX-01·IX-02가 먼저다** —
  기록이 쌓여도 인덱스가 없으면 목록은 ER-02다. 아래는 W-12의 원문 배경이다.
- ✅ **~~학생 명부 시딩 — 없으면 S6에 실기기로 도달할 수 없다.~~ → 🔴 W-16D가 닫았다.** (`database_ToDo/W-12.md` §6)
  🔬 **그리고 그 순간 S6가 실기기에서 처음 열렸고, 개발 서버 전용 버그가 드러났다** —
  `BottomSheet`의 N-02 효과에 cleanup이 없어 StrictMode가 시트를 열자마자 닫는다.
  **프로덕션 빌드는 정상**이다(`reports/W-16D.md` §9.5). 아래는 당시 배경이다.
  `students`가 비어 S5가 빈 상태(EM-01)이고, 행이 없으면 시트를 열 트리거가 없다.
  **최소 1개 반**(문서 ID `{academicYear}_{studentNo}`)이 필요하다. §9.7 `[결정 필요 D-05]`가 열려 있다.
  W-13은 「기록이 실제로 쌓인 상태」를 요구하는데 그 기록을 만들 유일한 입구가 S6이고 S6의 입구가 명부다.
- ✅ **~~규칙·인덱스가 파일로 섰다 — 배포만 남았다.~~ → 🔴 W-16D가 배포했다.** (W-16 · `database_ToDo/W-16.md`)
  규칙 릴리스 **`2026-08-24T00:36:15.578Z`** · 배포본이 로컬 358줄과 **바이트 일치** · 활성 catch-all **0건**.
  인덱스 **3건**(IX-01·IX-02·**IX-08**) 전부 `READY`. **스모크 8/8 통과 · 롤백 0회**.
  🔴 **남은 것은 §14.5 13·14번 콘솔 확인 2건뿐이고 W-17로 이월됐다.** 아래는 당시 배경이다.
  `firestore.rules`·`firestore.indexes.json`이 저장소에 있고 **규칙 테스트 152건이 전량 통과**하지만
  **`firebase deploy`는 한 번도 실행하지 않았다.** 🔴 **지금 실기기를 떠받치는 것은 여전히 W-04 임시 catch-all이다.**
  배포 순서(**인덱스 먼저 → 「사용 설정됨」 확인 → 규칙**) · 명령 전문 · **1분 롤백 절차**(`firestore.rules.catchall`) ·
  **배포 후 스모크 7건**이 전부 `database_ToDo/W-16.md`에 있다.
  🔴 **배포 전까지 아래 「W-08 규칙 3건」은 여전히 미반영이다** — catch-all 덕분에 우연히 통과할 뿐이다.
- ✅ **~~Firebase 규칙 3건 — 배포 대기~~ → 🔴 W-16D가 배포했고 1번은 실물로 검증됐다.** (`database_ToDo/W-11.md` §4)
  🔬 **5-3 실측** — 제3 계정이 `SSYM-9352`로 가입 신청에 성공했다(`approvalRequests` `00:54:14` 생성).
  **`inviteCodes` 단건 `get`이 없으면 코드를 읽지 못해 제출 자체가 불가능하다.** 아래는 당시 배경이다.
  `inviteCodes` 단건 `get` · 필수 조건 2/BR-30a 예외 · `users` update 재신청 예외.
  판정 근거는 **PRD에 없는 임의 컬렉션(`zzruleprobe`)이 읽힌다**는 것 — W-04 §5의 catch-all이 살아 있다.
  (`inviteCodes` list·`auditLogs` 읽기가 통과하는 것은 지금 계정이 `head`라 **판별력이 없다.**)
  **지금은 W-04의 임시 규칙(`@dshs.kr`이면 전부 허용) 덕분에 우연히 통과한다.**
  W-15가 §9.6 본편을 배포하는 순간 3건이 없으면 S2가 전부 막힌다.
  콘솔에 이미 만든 `inviteCodes` 문서가 있으면 **ID를 코드 문자열로 다시 만들어야 한다**(안 1).
- 🔴 **초과 승인 문제 — 여전히 미결.** (W-08 §10) 안 2로 `useCount` 증가가 승인 시점으로 갔다.
  한도를 채운 뒤에도 `pending` 신청이 남아 있을 수 있다 — 초과 승인을 허용할지, 승인 시점에
  재검사해 거부할지(문구가 §8.10에 없다), 자동 거절할지 결정이 필요하다.
  ⚠ **W-11이 판단할 것으로 적혀 있었으나 지시서 `prompts/W-11.md`의 범위가 S4·S5뿐이라 다루지 않았다.**
  승인 화면(S8 관리)을 만드는 회차로 넘긴다.
- 🔴 **IX-08 복합 인덱스를 만들어야 한다.** (`database_ToDo/W-11.md` §2)
  `students`: `academicYear ASC, grade ASC, classNo ASC, number ASC`. **PRD §9.5에 이미 설계돼 있다.**
  **만들기 전까지 S5는 ER-03 `학생 명부를 불러오지 못했습니다`가 뜬다 — 버그가 아니다.**
  만들면 EM-01로 바뀌고 칩이 `-명` → `0명`이 되는 것이 검증 지점이다. 생성 링크는 요청서에 있다.
  🔴 **`firestore.indexes.json`을 만들지 마라.** 저장소 규칙 체계를 세울지는 W-15 판단이다.
  ✅ W-10 DoD 4는 닫혔다 — S3의 세 질의는 인덱스 없이 통과한다(`OK count=0` ×3).
  ⚠ **`records`의 IX-01~IX-05도 전부 미생성이다.** W-12가 중복 검사(BR-13)를 붙이면 **IX-06**이 필요해진다.
- **PRD 반영 대기 78건(st4R).** — 정본은 **`reports/W-15B.md` §7**의 회차별 전량 표다.
  W-15A의 67건에 W-15B 신규 **11건**이 붙었다. 🔴 **4건이 「확정」됐다**(결정 1·결정 3) —
  design 문구 3종 · `rise` 스태거 규칙 · `.pill-fill` 그림자 · `14px` §7.2 편입.
  **결정은 닫혔고 PRD 문서 반영만 PM 소유로 남는다**(§7 표의 `✅ 확정` 표시).
  **W-15B 신규 11건**: ① R-02·BR-19의 「본인 → member」가 Dev 경로에서 거짓 ·
  ② MD-02 Dev 변형 부재 · ③ 부원 목록 정렬 규칙 부재(IX-10 `role ASC`는 알파벳 순) ·
  ④ EM-05 조건 문언(「부원 1명」) · ⑤ BR-24 부원↔차장 역할 변경에 UI 없음 ·
  ⑥ 🔴 §9.3.1 NG-13(이름 첫 글자) ↔ §8.8.3 #6(역할 첫 글자) 아바타 충돌 ·
  ⑦ §8.8.3 #14-1 `이름 수정` pill이 design에 없다 · ⑧ §8.10.5 양도 힌트가 Dev에게 거짓 ·
  ⑨ design `9c`의 ② 라벨이 `부원 목록` · ⑩ 결정 1 문구 3종의 위치·코드 부여 ·
  ⑪ 🔴 **R-06의 갱신 시점이 규격에 없다 — TS-12 충돌의 근원이다.**
  아래는 W-15A 시점의 이력이다.
- **~~PRD 반영 대기 67건(st4R).~~** — 정본은 **`reports/W-15A.md` §7**의 회차별 전량 표다.
  W-14의 58건에서 닫힌 항목 없이 W-15A 신규 9건이 붙었다. **W-15A 신규**:
  ① 거절 사유 입력 UI가 규격에 없다 · ② `activeInviteCodeId` `null` 상태가 §8.8.6에 없다 ·
  ③ BR-17 ↔ §9.3.3 혼동 문자 충돌 · ④ BR-14는 신청 시점 검사임을 각주로 ·
  ⑤ §15.1 `.pill` vs §7.3 padding · ⑥ §8.8.3 버튼 라벨이 design과 **뒤바뀌어** 있다 ·
  ⑦ design `9b`·`9c`의 문구 3종이 §8.10에 없다 · ⑧ 섹션 카드 `rise` 스태거가 섹션 4개 기준 ·
  ⑨ `.pill-fill` 그림자.
  아래는 W-14 시점의 이력이다.
- **~~PRD 반영 대기 58건(st4R).~~** — 정본은 **`reports/W-14.md` §7**의 회차별 전량 표다.
  W-13의 50건에서 닫힌 항목 없이 W-14 신규 8건이 붙었다. **W-14 신규**:
  ① 🔴 BR-48 공백(첫 월요일 이전 날짜)과 고정한 해석 · ② §7.2 H1 700 ↔ design `1j` 800 ·
  ③ 🔴 §8.10.2에 T-04 문구 2건 부재(`이번 주 순찰이 종료되었습니다` · 「다음 주 미등록」) ·
  ④ S9 로딩 스켈레톤 시안 부재 · ⑤ design `1j`의 `등교 지도 ` 접두사가 사전에 없다 ·
  ⑥ AC-01 목록에 없는 0.5/0.42 텍스트가 **3화면**에 있다(한 번에 결정할 것) ·
  ⑦ 🔴 §9.6 각주 확정(결정 3) · ⑧ §20.7 재작업 대기 목록의 `1j` 행 해소 + **시점 표기 정정**.
  ⚠ **W-12 이월 17번(§7.2에 `14px`이 없다)이 두 번째 소비자를 얻어 승격 기준을 넘겼다.**
  아래는 W-13 시점의 이력이다.
- **~~PRD 반영 대기 50건(st4R).~~** — 정본은 **`reports/W-13.md` §7**의 회차별 전량 표다.
  W-12의 46건에서 2건이 닫히고(§0.1-4 「S6 배너 시안 불필요」 · §0.1-8 「ER-17 방어로 유지」)
  W-13 신규 6건이 붙었다. **W-13 신규**: ① §8.7.3 「최근 30일」 ↔ `limit(30)` 문언 충돌 ·
  ② `gh` 11px(PRD) ↔ 12px(design) · ③ Row title 14.5·Caption 11.5(PRD) ↔ 15·12(design) ·
  ④ §8.7.5에 「오프라인 + 빈 캐시」 행이 없다 · ⑤ 동기화 칩 「지연」 임계 시간 없음(**3,000ms는 신규 값**) ·
  ⑥ §7.2에 `20px`이 없다.
  🔴 **추가 — §9.6 「부원은 이름·역할 필드만」은 Rules로 표현할 수 없다.** `allow read`는 문서 단위다.
  W-13이 `users`를 처음으로 남의 문서까지 읽으므로 결정이 필요하다(`database_ToDo/W-13.md` §4 · 재검증 2건).
  아래는 W-11 시점의 이력이다.
- **~~PRD 반영 대기 38건(st4R).~~** (W-11 §7 — 회차별 표 하나로 정리했다)
  W-11 신규 5건 + W-10 신규 3건 + W-09 6건 + W-08 신규 13건 + 토큰 1건 + W-07 이월 5건 + W-06 이월 5건.
  ✅ **개수 논란은 끝났다** — 지시서 §0.1-8이 정본을 **33건**으로 확정했고(W-09 §6의 29건 + 누락분 1건 +
  W-10 신규 3건), 거기에 이번 신규 5건을 더해 38건이다. W-10 보고서의 「32건」은 누락분 1건이 빠진 수다.
  **W-08 신규 13건 중 6건이 §9.6·§11.2 규칙·오퍼레이션 개정이라 W-15와 직결된다.**
  W-11 신규 5건: IX-08에 `isActive`가 없다(클라이언트 필터 우회 · 명부가 차면 재검토) ·
  §8.5.3 정렬 키(`studentNo`)와 IX-08(`number`)의 **등가성 문언** ·
  `다시 시도` 버튼에 문구 코드 없음 · §8.5.2 #7 「세로 스크롤」이 구현 지정으로 읽힘 ·
  **S4 빈 상태 시안 없음**(아이콘 없이 문구만 그렸다).
- 🟡 **`docs/SunDO_PRD.md`가 미커밋 상태다.** st4R이 부록에 콘솔 작업 기록을 쓰는 중이다.
  시딩 ①②③④는 **전부 완료됐는데**(W-10 착수 시점 확인) 그 부록의 「진행 상황」 표는
  아직 ②③④를 `⬜ 미완`으로 적고 있다. 4행 전부 `✅`로 맞춰야 한다. PRD 반영은 PM 소유다.
- ✅ **~~`students` 컬렉션이 없다~~ → 🔴 W-16D가 1,033건을 넣었다.** (`database_ToDo/W-11.md` §5)
  실물 확인: S3 학년 버튼 활성 · S4 인원 칩에 **실제 숫자** · S5 **정상 목록**(ER-03 아님).
  **S4 카운트 == S5 행 수**(W-11 §3-1이 잠근 등가성)도 실측으로 재확인했다. 아래는 당시 배경이다.
  실물에서 이렇게 보이는 것이 **정확한 동작**이다. 버그로 진단하지 마라.
  · S3 — 학년 버튼 3개 전부 회색 + TS-14
  · S4 — **1~10반이 뜨고 10칸 전부 `opacity 0.5`** + 탭하면 TS-15(카운트 질의는 인덱스 없이 통과한다)
  · S5 — **EM-01이 아니라 ER-03**(IX-08이 없어 `failed-precondition`). 인덱스를 만들면 EM-01로 바뀐다.
  명부 투입은 W-15(Rules 확정) 이후다. **순서는 인덱스 → 규칙 → 명부다.**
- **W-03C 보고서 §7의 확인 5건 중 4건이 미회신이다.**
  1. §1 지정 문자열(`fs.rmSync`)을 `fs.promises.rm`으로 바꾼 것 — 안 바꾸면 빌드가 죽는다
  2. 시트 내부 하이라이트가 design보다 알파 0.05 낮은 것(지시서 §5.1을 따른 결과)
  3. 시트 모션이 design `sheetUp`(55% + 페이드)이 아니라 지시서 `translateY(110%)`인 것
  4. 토스트에 수동 닫기 수단이 없는 것 → W-18 접근성 점검으로 넘길지
  5. ~~`ToastProvider`를 라우트 루트에 1개~~ → W-05에서 그대로 구현했다(§4-2). 남은 것은 1~4번이다.
- **Node 버전 재검토.** `fs.rmSync`가 죽는 것이 v24.2.0 고유 문제인지 확인이 필요하다.
  LTS(22.x)로 내리면 해결될 가능성이 높다.
- `public/fonts/`의 Spoqa TTF 5종은 CDN 채택으로 여전히 미사용이다.
  오프라인 PWA 단계에서 로컬 `@font-face` 전환 여부를 재검토한다.
- `README.md`·`LICENSE`(MIT)를 원격에 복구할지, 복구한다면 어느 작업 단계에 넣을지 미정.

## 해소된 항목
- ~~S4 빈 상태에 아이콘이 없다 (시안 필요 여부)~~ → **W-12 지시서 §0.1-5에서 「아이콘 없음이 정본」으로 닫혔다.**
  시안을 요청하지 않는다. §8.10.2 EM-07이 규정하는 것도 문구뿐이다.
  **PRD 반영 대기가 38건 → 37건이 됐다**(W-12 신규 9건을 더해 현재 **46건** — `reports/W-12.md` §7).
- ~~S6 기록 작성 시트를 여는 자리(W-11 §8-1)~~ → **W-12에서 붙였다.**
  `handleStudent(student, event)` 안, `spawnSparkle(...)` 바로 다음 줄. 행 탭 빗장은
  `setTimeout(…, 0)`이 아니라 **시트가 완전히 닫힐 때**(`BottomSheet.onClosed`) 푼다.
- ~~`toMonthKey`가 없다~~ → **W-12에서 `dateKeys.ts`에 추가했다.** `formatDateTimeKst`(BR-47 표기)도 함께다.
  **둘 다 기존 `toKst()`를 통과한다** — KST 변환을 복사하지 마라.
- ~~IX-06을 W-12 착수 전에 요청해야 한다(W-11 §8-4)~~ → **필요 없어졌다.**
  등식 `in` 질의로 대체해 W-12가 요구하는 인덱스가 **0건**이다. IX-06이 필요해지는 시점은
  서버 중복 검사 전환 또는 S7 학생별 이력이다(`database_ToDo/W-12.md` §2).
- ~~오프라인 배너와 상단 헤더의 겹침~~ → **W-09에서 밀어내기로 해소했다.**
  `AppShell`이 배너를 직접 소유하고(포털·컨텍스트 없음) 콘텐츠를 **56px** 민다.
  S2 실측: 배너 하단 `56` · 뒤로 버튼 `88.25` · 제목 `82` — 겹침 0.
  🔴 **화면에서 `<OfflineBanner />`를 그리지 마라.** 전역 1개이고 `.ofb`가 2개가 된다.
- ~~W-08 보고서 §7의 확인 6건~~ → **W-09 지시서 §0.1에서 전부 회신됐다.**
  1·2·3 승인 / 4 `9자 완성` 확정(지시서가 틀렸다) / 5 현행 유지 + **W-18 후보 확정** /
  6 이번 회차가 답이다. `Field` 계약 정리는 **S6로 예약**(위 「예약된 작업」).
- ~~W-03C 보고서 §7의 미회신 4건~~ → **W-07 지시서 §0.1에서 전부 회신됐다**(2회차 이월 종료).
  1 `fs.promises.rm` **현행 유지** · Node 다운그레이드 없음 · **OneDrive 오진 정정**(`reports/W-03B.md`에 기록) /
  2 시트 하이라이트는 W-04에서 이미 해소 / 3 시트 모션 `translateY(110%)` 유지(PRD §7.4 명시) /
  4 토스트 수동 닫기는 **W-18 이월 확정**.
- ~~W-06 보고서 §8의 확인 5건~~ → W-07 지시서 §0.2에서 전부 회신됐다.
  1 칩 X `aria-label` → **`거절된 계정 안내 닫기`**(`지우기`는 계정을 지운다고 읽힌다) /
  2 카드 하이라이트 `0.85` **유지** / 3 배너 겹침은 W-08 / 4 §0.1에서 회신 / 5 번들은 W-16.
- ~~S2-1 승인 감지 수단 부재~~ → **A안(`refresh()`) 확정·구현 완료**(W-07 §3).
- ~~W-05 보고서 §8의 확인 5건~~ → W-06 지시서 §0.1에서 전부 회신됐다.
  1 `permission-denied` → **`권한이 없습니다`**(EC-13·EC-42 원문, PRD v1.4에서 `ER-20`) /
  2 `DockRootContext` **승인** / 3 정책 3종은 **판정 대기를 건너뛴다**(W-05 DoD 9 개정) /
  4 번들은 W-16 / 5 W-03C 4건은 다음 회차.
- ~~`Footer`의 `onNavigate` 미연결(S1)~~ → W-06에서 연결. 전체 재적재 0건 실측.
  **S2-1·S10분은 아직이다**(W-07·W-10).
- ~~27px·13px 리터럴 승격 대기~~ → `--text-brand`·`--text-subtitle`로 승격(W-06 §3).
  로고 그림자도 `--shadow-logo`로 승격했다.
- ~~W-04 보고서 §7의 확인 4건~~ → W-05 지시서 §0.1에서 전부 회신됐다.
  1 `loading` 3초 타임아웃은 **W-05 소유**(스플래시와 함께) / 2 코드 분할은 W-16 /
  3 거절 계정 삭제 실패는 현행 유지 / 4 PRD v1.3은 PM이 한다.
- ~~`blurIn` 이식(W-03B가 W-05로 넘김)~~ → 완료. `.blur-in` + `@keyframes blurIn`(design 원문).
- ~~역할 전환 시 활성 탭 가드(W-03B §8-4)~~ → `RequireRole`이 처리한다(W-05 §4-5).
  강등된 계정이 `/admin`에 남아 있으면 다음 판정에서 `/` + TS-12로 밀려난다.
- ~~`tailwind-merge` 커스텀 스케일 충돌~~ → W-03A §1에서 해결. 실측 11케이스 통과.
- ~~`index.html`에 `viewport-fit=cover`·`theme-color` 부재~~ → W-03A §2에서 추가.
- ~~`.pill-fill` 그림자 확인 필요~~ → 의도된 차이로 확정했다가 **W-15B 결정 3 ②로 복원했다**(위 「확정된 결정」).
- ~~W-03A 확인 요청 5건~~ → W-03B 지시서 §0.1에서 전부 회신됐다.
- ~~W-03B 확인 요청 5건~~ → W-03C 지시서 §0.1에서 전부 회신됐다.
  1 빌드 스크립트 수정(§1) / 2 독 모션 reduce 0.01s(§2.2) / 3 **최소 지원 폭 360px 확정** /
  4 역할 전환 알약은 W-05 라우팅 가드 / 5 Footer 하단 여백 현행 유지.
- ~~빌드 크래시 원인 미상~~ → `fs.rmSync`로 특정하고 `build` 스크립트에서 막았다(W-03C §4-1).
- ~~DR-10 구현 여부~~ → **MVP 유보 확정**(W-04 회신 §1, 선택지 A). 근거는 `reports/W-04.md` §8.
- ~~DR-12 vs §9.6 자기 갱신 허용 키~~ → **충돌 아님.** 부록 B 전문이 정본이고 요약표가 좁았다.
  확인: `docs/SunDO_PRD.md:2866`(허용 키에 `name`·`memberStudentNo` 계열 포함) · `:2915`(사유 주석).
- ~~시트 내부 하이라이트 0.85 vs design 0.9~~ → **design(0.9)으로 되돌렸다**(W-04 §2).
  `--highlight-top` 토큰은 `.glass`·`.dock` 2곳이 계속 쓰므로 유지한다.
- ~~W-03B의 OneDrive 오진 기록~~ → `reports/W-03B.md` 2곳에 정정 1줄씩 덧붙였다(W-04 §1).
  1 끄기(§1 반영) / 2 승인 / 3 승인(`--color-sundo-tint-06` 신설) / 4 W-09으로 연기 / 5 승인.

## 참고
- **`src/App.tsx` 스토리 페이지는 W-05에서 사라졌다.** 필요하면 `git show 1e06962:src/App.tsx`.
  `App.tsx` 주석에도 같은 명령을 적어 뒀다. 되돌리기 비용은 한 줄이다.
- **`AuthProvider`는 `users/{uid}`를 `getDoc` 1회로 읽는다. 구독이 아니다.** (W-05 §6)
  역할 변경이 실시간 반영되지 않는다. R-06의 `getIdToken(true)` + 재조회는 **W-12**가 붙인다.
- **🟡 W-17 항목 — `getDoc` 캐시 폴백.** (W-05 §9 · W-05 지시서 §0.3)
  인증 판정 시 캐시 폴백 때문에 **조회 실패가 "문서 없음"으로 보일 수 있다.**
  `getDoc()`은 서버 실패 시 로컬 캐시로 떨어지고, 캐시가 "문서 없음"을 들고 있으면
  에러가 던져지지 않고 `exists() === false`가 반환된다. W-04 §4-3의 분기는 옳지만
  실제 `permission-denied`에서 안 탈 수 있다 — 활성 부원이 가입 신청 화면으로 튕긴다.
  **지금 없앨 수 없다.** 그 폴백이 곧 오프라인 지원(EC-01)이다.
  W-17에서 `getDocFromServer` 우선 + 실패 시 캐시 같은 전략을 판단한다.
  이 위험이 실현되면 W-05 §7의 `permission-denied` 문구 문제도 함께 사라진다. 두 항목을 같이 봐야 한다.
- **실측 방법(W-05).** `vite preview`가 SPA 폴백을 하므로 **모든 경로를 실제 URL 직접 진입**으로 쟀다.
  `status`·`role` 주입은 임시 `ProbeAuthProvider`가 맡았고(AuthContext를 임시 export),
  측정 후 4곳 전부 되돌렸다. **프로브 제거 후 빌드 산출물 해시가 도입 전과 같은 것이 제거의 증거다.**
- W-01 착수 시 원격 저장소가 비어 있지 않아(커밋 27개) 사용자 선택에 따라 히스토리를
  리셋하고 v0.0.1 단일 커밋으로 시작했다.
  기존 원격 히스토리 백업: `바탕 화면/SunDO_remote_backup_20260820.bundle`
  (복원: `git clone SunDO_remote_backup_20260820.bundle`)
- `design/index.html`은 번들 아티팩트라 마크업이 JSON 문자열로 인코딩돼 있다.
  CSS를 꺼내려면 이스케이프를 2단계 해제해야 한다. 방법은 `reports/W-02.md` §5 서두 참조.
- **검증 환경 주의.** W-02는 Chrome 확장으로 `getComputedStyle`을 읽었으나 W-03A 재개 세션에서는
  확장이 연결돼 있지 않았다. 헤드리스 Chrome을 CDP로 직접 구동해 같은 측정을 했다
  (의존성 추가 없음, node 전역 `WebSocket`/`fetch`만 사용). 방법은 `reports/W-03A.md` §1 참조.
  - 헤드리스는 창이 활성이 아니면 `focus`/`blur` 이벤트가 **발생하지 않는다.**
    `Emulation.setFocusEmulationEnabled`를 켜야 blur 검증을 실측할 수 있다.
- ~~`src/App.tsx`는 스토리 페이지다~~ → **W-05에서 라우터 루트로 대체됐다.**
- **순수 함수 실측 방법(W-04).** 테스트 러너가 없어 Node의 타입 스트리핑으로 `src/lib/*.ts`를
  직접 import해 실행했다. 확장자 없는 상대 import는 `module.register` 해석 훅으로 `.ts`를 붙였다.
  소스를 복사하지 않으므로 실측 대상과 저장소 파일이 어긋나지 않는다. 스크립트는 스크래치패드에 있다.
- **W-12 검증 방법.** 스텁 `firestore`가 `where(..., 'in', [...])`를 해석하고 질의 키를 기록한다.
  `setDoc`은 오프라인 모드에서 **로컬 반영 즉시 + promise 영원히 pending**으로 흉내 낸다 —
  화면이 `await`했다면 여기서 타임아웃 난다. 시각은 `Page.addScriptToEvaluateOnNewDocument`로
  앱보다 먼저 `Date`를 덮어 주입했다(**화면 코드에 시각 주입 훅을 만들지 않았다**).
  `clientRecordId`는 React fiber(`__reactFiber$…`)의 `useState` 체인에서 직접 읽었다.
  🔴 **히스토리 검증은 앱 내비게이션으로 화면에 들어가야 한다** — `Page.navigate`로 바로 꽂으면
  이전 엔트리가 `about:blank`라 뒤로가기 1회에 타깃이 detach된다.
  🔴 **쓰기 카운트는 `records/` 경로만 세라** — `useLastActiveAt`(DR-12)가 `users`를 상시로 쓴다.
- **최소 지원 폭은 360px이다.** (W-03C 지시서 §0.2) 320px에서 독 간격이 0이 되는 것은 지원 범위 밖이다.
- **토큰화 기준(W-03B 지시서 §3)**: 서로 다른 컴포넌트 2개 이상에서 쓰이면 토큰.
  같은 컴포넌트의 상태 변형 반복은 CSS 기계적 중복이지 토큰 수요가 아니다.
- W-09 승격 대기: `inset 0 1px 3px rgba(20,53,38,0.05)`(→ `--shadow-inset-soft`),
  `inset 0 1px 0 rgba(255,255,255,0.28)`(`.btnp`+모달 버튼 2종 3회),
  **`rgba(46,107,76,0.22)`**(스피너 트랙 — `.ff-spin`·`.gbtn-spin` 2개 컴포넌트, W-06에서 2번째 사용처 발생).
  `rgba(20,53,38,0.45)`·`inset 0 1px 0 rgba(255,255,255,0.85)`는 W-03C에서 승격 완료
  (`--color-sundo-ink-45` · `--highlight-top`). **§7.1 보완 토큰은 9개다**(W-08에서 `--color-sundo-tint-danger-10` 승격).
- 🔴 **규칙 검증은 Firestore 에뮬레이터다(W-16).** `npm run rules:test`가 규칙 테스트
  **231건**(162 → 213 → 231), `npm run rules:probe`가 Q-1~Q-5 실측,
  `RULES_MUTATE=<이름> npm run rules:mutate`가 **역검증 39변형**이다.
  🔴 **`breaks` 목록은 먼저 적고 실측으로 고치는 것이지, 실측을 보고 맞춰 적는 것이 아니다** —
  W-21B에서 「이 조건이 무엇을 막는가」를 **두 번 연속** 틀렸다(`reports/W-21B.md` §7 ①②).
  🔴 **역검증은 `firestore.rules`를 그 자리에서 덮어쓴다 — 사라질 수 있다.**
  W-21C에서 커밋 직전에 이 회차의 규칙 변경이 **통째로 없어져 있었다**(원인 미확정 ·
  `reports/W-21C.md` §8 ①). 규칙 테스트 11건이 빨개져 잡았다.
  ⇒ 🔴 **규칙을 고친 회차는 커밋 직전에 `git diff --numstat firestore.rules`를 보라.
  0줄이면 사라진 것이다.** diff가 비면 `git add -A`가 조용히 지나가고 **코드만 배포된다.**
  방법은 `reports/W-16.md` §6. 요점 3가지:
  - 🔴 **`java -version`이 11+인지 먼저 보라.** Oracle Java 8이 시스템 PATH 앞자리를 잡고 있으면
    Temurin 21을 설치해도 **8이 나오고 에뮬레이터가 뜨지 않는다.** 세션 PATH에 21을 앞에 끼운 창에서
    Claude Code를 띄워 해결했다. **「설치했다」와 「에뮬레이터가 쓰는 Java가 21이다」는 다른 문장이다.**
  - 🔴 **테스트 `projectId`는 `demo-sundo-rules`다.** `demo-` 접두사라 에뮬레이터가 실 서비스 접근을 막는다 —
    `sundo-dev-8ef38`을 테스트에 쓰지 마라.
  - 🔴 **픽스처는 `withSecurityRulesDisabled`로 심는다.** 규칙을 통과시켜 심으면 테스트가 검증 대상에 의존한다.
- 🔴 **규칙을 고치면 `tests/rules/mutations.mjs`의 조각도 함께 고쳐야 한다.** 조각이 어긋나면 러너가 exit 2로 알린다.
  **저장소 규칙 파일은 CRLF이고 변형 조각은 LF다** — `run.mjs`가 비교 전 정규화하고 원복은 바이트 사본에서 한다.
- 🔴 **규칙이 표현할 수 없는 것 3가지(W-16 실측)** — ① **필드 단위 읽기 제한**(문서 단위만 가능) ·
  ② **집합 판정**(「부장이 정확히 1명」을 셀 수 없다) · ③ **형제 연산 참조**(같은 배치의 다른 문서 변경을
  `get()`이 보지 못한다 — **배치 이전 상태를 본다**). 규칙으로 막으려 하기 전에 이 셋인지 먼저 보라.

- 🔴 **CDP 검증 방법이 W-18에서 바뀌었다.** W-03A 이래 쓰던 「node가 Chrome을 spawn한다」가
  **이 하네스에서는 통하지 않는다** — spawn 직후 프로세스 트리가 통째로 회수돼 출력이 끊기고
  종료 코드 **127**(command not found)이 나온다. `try`/`catch`·`uncaughtException`·`exit` 훅
  어느 것도 걸리지 않고 샌드박스를 꺼도 같다. 🔴 **PowerShell이 `Start-Process`로 헤드리스
  Chrome을 먼저 띄우고, node는 `http://127.0.0.1:9333/json/list`로 붙기만 한다.**
  증상이 「node가 없다」로 보여 원인을 PATH·모듈 해석에서 찾게 되니 주의하라(`reports/W-18.md` §6.1).
- 🔴 **`min-height: 100%`는 높이를 확정하지 않는다**(W-18 §6.2 실측).
  `min-height`만 있고 `height:auto`면 컨테이너가 **먼저 내용에 맞게 커져** 「남는 공간」이 0이 되고,
  자식의 `flex:1`이 아무것도 분배받지 못한다. `min-height:0`은 **허가이지 명령이 아니다.**
  정책 카드가 1,731px로 늘고 내부 스크롤이 0이 됐다 → `height:100%`로 바꿔 720px·+967px로 잡혔다.
  ⚠ **기존 화면 8종의 `min-h-full`이 반례가 아니다** — 그것들은 문서 스크롤이라 확정 높이가 필요 없다.
- 🔴 **저장소 안에서도 파일마다 개행이 다르다.** `firestore.rules`는 CRLF, `src/**`는 **LF**다.
  스크립트로 CSS를 삽입할 때 CRLF를 넣어 144줄이 혼용됐다(W-18 §6.4). 삽입 후
  `git diff --numstat`이 **추가만**(`218 0`)인지 확인하라.
- **화면 높이를 카드 내부 스크롤로 나누는 예시는 `src/components/PolicyScreen.tsx`다.**
  `.blur-in:has(> .pol)`로 `ScreenTransition`을 고치지 않고 높이 사슬을 잇는 방법이 거기 있다.
- **긴 문안을 데이터로 두는 예시는 `src/lib/policy.ts` + `PolicyScreen`이다.**
  🔬 A/B 실측 — 같은 문안을 JSX로 박으면 셸을 빼고도 **+5.34 kB** 크다(W-18 §4.3).

## 다음 세션이 먼저 읽어야 할 파일
- 🔴 **reports/W-21C.md §10(🔴 **W-22 마지막 인수인계** — 특히 **§10.2 §17.1 MVP 항목별
  대조표**) · §11(v1.1·v1.2 전량 — **v1.0.0 이후의 출발점**) · §7(이월 결함 2건의 처리) ·
  §4(마이그레이션 전말 + 되돌리는 법) · §5(규칙 · 역검증 1:1) · §6(권한 매트릭스) ·
  🔴 §8(측정 함정 — **① 역검증이 규칙 파일을 지울 수 있다**) · §9 ③(시안 없이 정한 값 전량)**
- 🔴 **database_ToDo/W-21C.md** — §1 배포 순서 5단계(🔴 **§1.1 규칙을 앱보다 먼저 둬도 되는
  근거**) · §2 마이그레이션 결과와 **되돌리는 법** · 🔴 **§2.4 배포 직후 부장이 할 일** ·
  §4 스모크 15건 · §5 삭제한 기록 되돌리기 · §7 승계 목록 정정
- 🔴 **reports/W-21B.md §9(W-21C 전제 — 🔴 **중식/석식 데이터 모델 확정본** · design `19` 실물 ·
  사양 충돌 2건 · 기능 4의 첫 확인 지점) · §4(규칙 변경 전량 + 🔴 §4.3 지시서를 이긴 사실 +
  §4.4 뒤집은 케이스 4건 + §4.5 역검증 1:1 15변형) · §5(권한 매트릭스 — 규칙 ↔ 화면) ·
  §6(판단 8건 — 🔴 §6.1 `recordCount` 감소를 안 넣는 근거 4가지) ·
  🔴 §7(측정 함정 신규 4건 — **「이 조건이 무엇을 막는가」를 두 번 틀렸다**) · §8(st4R 신규 8건)**
- 🔴 **database_ToDo/W-21B.md** — 🔴 **§1 배포 순서 4단계**(좁히는 규칙이 앱 **뒤**인 이유 ·
  임시본 만드는 법) · §3 스모크 8건(🔴 **8번 부장 승인이 급소다**) ·
  🔴 **§4 Dev 부재 시 코드 발급 불가 + 콘솔 우회 절차** · 🔴 **§5 삭제한 기록 되돌리는 절차** ·
  §7 1분 롤백 · §8.2 W-21C가 받는 콘솔 작업
- 🔴 **reports/W-21.md §4(P-0 전말 — 세 가설을 무엇이 갈랐나 · `authDomain` 규칙) ·
  §3(패치 9건 관문 4칸 + 전/후 computed) · §5(측정 함정 신규 5건) ·
  §6(st4R 신규 8건) · 🔴 §7(W-21B 전제 — 규칙·데이터 모델·인덱스 조사) · §10.5(변경 파일 대조)**
- 🔴 **database_ToDo/W-21.md** — §1 `authDomain` 콘솔 절차와 **되돌리는 법** ·
  §2 `inviteCodes` 왕복 · §3 2차 배포와 원복 · §4 배포 절차
- 🔴 **reports/W-18.md §2(구현 사실 인벤토리 12항 — **개인정보 문서의 정본**) ·
  §3(사실 대조표 — 본문 문장 ↔ 근거) · §4.1(S2 동의 이탈) · §4.3(데이터 모듈 A/B) ·
  🔴 §8(PM 확인 11건 + 실기기 확인 3건) · §9(W-19 전제 5건)**
- 🔴 **database_ToDo/W-18.md** — **리전 확인 1건**(국외 이전 문언이 여기서 갈린다) ·
  개인정보 보호책임자 연락처(선택) · 이번 회차가 하지 않은 콘솔 작업 대조표
- 🔴 **database_ToDo/W-16.md** — **배포 실행 지시서.** 배포 순서(인덱스 먼저) · 명령 전문 ·
  **1분 롤백** · **스모크 7건** · §14.5 14항목 대응표 · 미포함 인덱스 11건의 이유 · 이관 7건.
  🔴 **아직 배포되지 않았다** — 그 전에는 실기기가 W-04 catch-all 위에서 돈다.
- 🔴 **reports/W-16.md §2(Q-1~Q-5 실측 — 규칙 구조를 결정한 다섯) · §3-4(역검증 18변형 표) ·
  §4-3(BR-18을 규칙이 어디까지 보장하나 — 3층과 구멍 2개) · §4-5(`recordCount` 단독 update 판단) ·
  §8(넘길 전제 — 🔴 §8-1 `selfWithdraw` 허용 키 3개 · §8-3 `memberCount` 감소가 배치를 죽인다 ·
  §8-4 배포 상태) · §7-1(PRD 신규 5건 — 🔴 #79 `$` 앵커 설명이 사실이 아니다)**
- 🔴 **database_ToDo/W-15B.md** — **규칙 회차(W-16)의 단일 입력.** §1 양도 실기기 확인 + **Dev 콘솔 복구 절차** ·
  §2 인덱스 0건 근거 · §3 재검증 **14건**(🔴 `users` **list** · 역할 변경 7건 · `departments` · `auditLogs`) +
  §3.5 **배치 조합 2건** · §4 **BR-18의 최종 보장자는 Rules** · §5 **누적 40건 전량 마감** + 결정 3건 ·
  §5.2 **규칙 배포 후 깨질 수 있는 지점 7개(우선순위)**
- **reports/W-15B.md §4-1(강등 대상 = `departments.headUid`) · 🔴 §4-2(성공 직후 TS-12 —
  세 번 실패한 뒤 잡았다) · §4-7(`14px` 편입 회귀 대조표) · §5(컴포넌트 API — `fetchMembers`·`transferHead`) ·
  §6(측정 함정 5건 — 🔴 신규 ④ 픽스처가 스키마보다 좁으면 결함이 코드로 오해된다) ·
  §8(넘길 전제 — 🔴 §8-1 **모든 쓰기 경로 8종 전량** · §8-3 클라이언트↔Rules 이중 방어 4건)**
- CLAUDE.md, docs/SunDO_PRD.md, prompts/{다음 작업}.md
- **reports/W-15A.md §5(컴포넌트 API — `admin.ts` 배치 헬퍼 · `generateInviteCode` · `Pill` 확장) ·
  §8(넘길 전제 6건 — 🔴 §8-2 부장 1명 불변식 · §8-3 `users` 목록에 `orderBy` 금지 ·
  §8-5 규칙 회차 재검증 25건 총계 — **W-15B가 40건으로 갱신했다**) · §4(판단 13건 — 🔴 §4-1 배치 계약 · §4-2 `update`의 함정 ·
  §4-7 오프라인 배치) · §6(측정 함정 5건 — 🔴 신규 ③ 「강제 탭」을 DOM 조작으로 흉내 내지 마라)**
- **database_ToDo/W-15A.md** — §1 `inviteCodes` 시딩 철회 · §2 두 번째 계정 절차 ·
  §4 재검증 11건 + 배치 조합 · §7 오프라인 배치 이관(W-20)
- **reports/W-14.md §5(컴포넌트 API — `duty.ts` 3갈래 유니온 · `dateKeys` 4종) ·
  §8(넘길 전제 8건 — 🔴 §8-2 `getDoc` ↔ `onSnapshot` 실패 방식 · §8-4 S8이 처음 만드는 것 4종 ·
  §8-8 규칙 회차 앞당김 근거) · §1-3(실 SDK 실측 P1~P3) · §1-4(BR-48 경계 14케이스) ·
  §6(측정 함정 10건 — 🔴 신규 ④ `users` 질의를 그대로 세면 안 된다)**
- **database_ToDo/W-14.md** — 🔴 **`dutySchedules` 시딩(다른 세션이 단독 실행하는 형태)** ·
  인덱스 0건 근거 · 결정 3 각주 문장 · 병렬 항목 확인란
- **reports/W-13.md §5(컴포넌트 API — `records.ts` 조회 계층) ·
  §8-1(🔴 실시간 구독 규칙 7항 — **목록형 화면 전용이다**)**
- **database_ToDo/W-13.md** — IX-01·IX-02 · `recordCount` 실행 순서 3단계 · `users` 읽기 재검증 2건 · 명부 시딩
- reports/W-12.md §5(컴포넌트 API — `RecordSheet` · `Segmented` · `records.ts` · 🔴 「쓰지 않는 5필드」 계약)
- database_ToDo/W-12.md — 규칙 재검증 시나리오 10건(W-13에서 변경분 0건)
- reports/W-11.md §5(컴포넌트 API — `roster` · `CenterNotice` · `.skel`/`.back` 승격) · §8(W-12 전제 5건)
- reports/W-10.md §5(`ScrollRootContext` · 신규 훅 3종 · `lib` 2종)
- reports/W-09.md §5(`AppShell` 신규 계약 · 스테이지 구조도)
- **reports/W-06.md §1(`AppShell` API 15항목 — W-09 §5가 1·2번을 갱신했다) · §9(W-07 전제)**
- reports/W-05.md §4(라우팅 판단 근거 10건) · §9(W-17 캐시 폴백)
- reports/W-03C.md §7(남은 확인 1~4번)
- 라우팅 구조가 궁금하면 `src/router.tsx` 하나면 된다 — 라우트 표·가드·전환·N-04가 전부 거기 있다.
- 화면 조립 예시는 `src/screens/Login.tsx`가 유일한 실물이다. 변형 분기·문구 상수 배치를 여기서 본다.
- 데이터 화면 조립 예시는 `src/screens/Home.tsx` + `src/lib/stats.ts`다.
  «화면은 Firestore를 직접 부르지 않고 `lib`이 판별 유니온을 돌려준다»는 계약을 여기서 본다.
- **목록 화면 + 4상태(로딩·정상·빈 상태·에러) 조립 예시는 `src/screens/ClassStudents.tsx` +
  `src/lib/roster.ts` + `src/components/CenterNotice.tsx`다.** `sticky` 헤더도 여기 있다.
- **실시간 구독 + 무한 스크롤 조립 예시는 `src/screens/Records.tsx` + `src/lib/records.ts`다.**
  구독 1개 · 정적 페이지 · ID Map 합집합 · 고정 커서 · `ScrollRootContext` root가 전부 거기 있다.
  🔴 **W-21B가 롱프레스 → 시트 2종을 얹었다.** ⓐ 권한 판정은 `records.ts`의
  `canEditRecord()` **하나**이고 `firestore.rules`의 같은 이름 함수와 **같은 문장**이다 —
  화면에 역할 분기를 또 쓰지 마라 ⓑ **ID Map은 합집합이라 스스로 줄지 않는다** —
  소프트 삭제된 행은 `dropRow`가 뺀다 ⓒ **시트를 겹쳐 띄우지 마라** — `BottomSheet`가
  히스토리 엔트리를 쌓으므로 액션 시트를 먼저 닫고 `onClosed`에서 다음을 연다.
- **단건 `getDoc` 화면 + 「없음이 정상 상태」 조립 예시는 `src/screens/Duty.tsx` + `src/lib/duty.ts`다.**
  3갈래 유니온(`ok|empty|failed`) · 도메인 키 캐시(`weekId`) · 구독을 켜지 않는 근거가 거기 있다.
- **배치 쓰기 + 감사 로그 조립 예시는 `src/lib/admin.ts`다.** `writeBatch` 1개 · `appendAudit`가
  배치에 넣기만 하는 이유 · `batch.update`가 없는 문서에서 배치 전체를 죽이는 것을 피하는 법 ·
  역할별 섹션 노출은 `src/screens/Admin.tsx`에 있다.

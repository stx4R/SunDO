# SunDO Progress

## 현재 버전
v0.5.0

## 완료한 작업
- [x] W-01 프로젝트 스캐폴딩
- [x] W-02 디자인 토큰 이식 (PRD §7.1~7.3)
- [x] W-03A 기반 레이어 + AppShell + Field + Switch (DoD 24개 전부 통과)
- [x] W-03B 표면 컴포넌트 6종 + Dock + Footer (DoD 28개 전부 통과)
- [x] W-03C Toast · BottomSheet · ConfirmModal (DoD 28개 전부 통과) — **W-03 3분할 완료**
- [x] W-04 Firebase 초기화 + Google Auth 컨텍스트 (DoD 28개 전부 통과, 2단계 분할 수행)
- [x] W-05 라우팅 + 역할 가드 (DoD 27개 전부 통과) — **스토리 페이지가 사라지고 앱이 됐다**
- [x] W-06 S1 로그인 화면 (DoD 30개 전부 통과) — **첫 실물 화면.** S2·S2-1은 W-07로 분리됐다
- [x] W-07 S2-1 승인 대기 + `refresh()` 계약 확장 (DoD 30개 전부 통과, 27번은 §8 단서로 해당 없음)
      **S2는 W-08로 분리됐다** — OP-01이 프로파일 A에서 성립하지 않는다
- [x] W-08 S2 가입 신청 (DoD 31개 전부 통과) — **OP-01 충돌이 안 1+안 2로 해소됐다.**
      `inviteCodes` 단건 `get` + `useCount` 증가를 승인 시점으로 이동. 규칙 요청서는 `database_ToDo/W-08.md`
- [x] W-09 오프라인 배너 슬롯 — 여백 계약 변경 (DoD 22개 전부 통과)
      **배너가 오버레이에서 흐름으로 내려와 콘텐츠를 56px 밀어낸다.** 배너가 없을 때 6화면의
      computed 값은 한 칸도 안 바뀌었다. 착수 전 확인 2건을 받아 DoD 15·9의 검증 기준을 고쳤다.
      시드 요청서는 `database_ToDo/W-09.md`
- [x] W-10 S3 홈 · 학년 선택 (+ CountUp · sparkle · 당겨서 새로고침) — **DoD 34개 전부 통과**
      **자리표시자가 사라지고 첫 데이터 화면이 됐다.** 신규 부품 5종:
      `useCountUp` · `dateKeys`(순수 함수) · `stats`(판별 유니온) · `usePullToRefresh`+`ScrollRootContext` · `sparkle`.
      `AppShell`의 여백 계산은 **한 줄도 바뀌지 않았다**(들여쓰기 재정렬만).
      보고서 `reports/W-10.md` · 인덱스 요청서 `database_ToDo/W-10.md`
      **DoD 4(인덱스 실측)는 W-11 착수 시점에 st4R 환경에서 닫혔다** — S3의 세 질의는 인덱스 없이
      통과한다(6행 전문은 보고서 §1-3). 같은 실행에서 **S5의 `orderBy` 질의가 `failed-precondition`**임이 드러났다
- [x] W-11 S4 반 선택 · S5 학생 목록 (+ `roster` · `CenterNotice` · `.skel`/`.back` 승격) — **DoD 33개 전부 통과**
      **명부 화면 두 장이 섰다.** 신규 부품 2종(`lib/roster.ts` 판별 유니온 · `CenterNotice`) +
      아이콘 2종. 🔴 **착수 전 확인 1건(QA-11-01)** — `1f` 원문이 `sticky`와 양립하지 않아 멈추고 물었고
      **유리 헤더 띠**로 확정됐다. `AppShell` **diff 0줄** · 기존 화면 변경은 클래스 이름 승격뿐(회귀 0칸).
      보고서 `reports/W-11.md` · 인덱스 요청서 `database_ToDo/W-11.md`
- [x] W-12 S6 기록 작성 시트 + 저장 로직 — **DoD 35개 중 33개 통과 · 2개 미측정**
      🔴 **앱이 처음으로 데이터를 만든다.** 지금까지는 전부 읽기였다.
      신규 3종(`RecordSheet` · `Segmented`(공용) · `lib/records.ts`) + 확장 4종
      (`dateKeys.toMonthKey`/`formatDateTimeKst` · `spawnSparkle(color?)` · `Field(counter?)` ·
      `BottomSheet(onClosed?, 아래 스와이프)`). `AppShell` **diff 0줄** · 기존 화면 변경은 `ClassStudents.tsx` 하나뿐.
      **의존성 추가 0**(UUID는 `crypto.randomUUID()`) · 실 Firestore 쓰기 **0건** · 콘솔 조작 **0건**.
      착수 전 확인 2건(`Field` 카운터 API 형태 · 아래 스와이프 소유자) — 둘 다 승인.
      **미측정 2건**: ER-17(UI 도달 불가 — §4-8) · 「인덱스 없이 실제로 동작」(실 접속 0 제약 — §1-4).
      보고서 `reports/W-12.md` · 콘솔 요청서 `database_ToDo/W-12.md`
- [x] W-13 S7 기록 조회 (+ 실시간 구독 · 무한 스크롤 · 카운터 3종) — **DoD 36개 중 35개 통과 · 1개 부분 측정**
      🔴 **앱이 처음으로 `onSnapshot` 실시간 구독을 켠다.** 지금까지 모든 조회는 단발 `getDoc`/`getDocs`였다.
      `Records.tsx` 자리표시자가 실물이 됐고, `records.ts`에 조회 계층 5종
      (`subscribeRecords` · `fetchRecordPage` · `fetchRecordCounts` · `fetchWithdrawnAuthors` · `clearRecordsCache`)이 붙었다.
      `dateKeys.ts` 2종(`formatTimeKst` · `formatDateKeyLabel`) · 아이콘 2종 · CSS 25종 추가.
      `AppShell` **diff 0줄** · `stats.ts`·`router.tsx`·`usePullToRefresh` **전부 diff 0줄** · 의존성 추가 **0** ·
      실 Firestore 접속 **0건** · 콘솔 조작 **0건** · 린트 경고 **18개(기준선 그대로, 신규 0)**.
      **착수 전 질문 0건** — §9의 5조건에 걸리는 항목이 없었다(구독 형태 성립 · 스크롤 root는 이미 컨텍스트가 있다).
      **부분 측정 1건**: 동기화 칩 「정상」의 실 SDK `fromCache` 플립(서버 왕복이 필요 — 접속 0 제약).
      보고서 `reports/W-13.md` · 콘솔 요청서 `database_ToDo/W-13.md`

## 진행 중
- 없음. **W-13까지 커밋 완료**(`SunDO v0.5.0`).

## 다음 작업
- **W-14 이후** — 지시서 대기. 다음은 **S9 선도 일정(조회)** 또는 **S8 관리**다.
  착수 전 **`reports/W-13.md` §5(컴포넌트 API — `records.ts` 조회 계층 · `dateKeys` 2종) ·
  §8(넘길 전제 5건)** 을 읽는다.
  🔴 **실시간 구독을 다른 화면이 재사용할 때의 규칙 7항이 `reports/W-13.md` §8-1에 있다.**
  특히 ④ `{ includeMetadataChanges: true }`가 **없으면 `fromCache` 전환이 콜백으로 오지 않는다**(SDK 소스 실측),
  ⑤ 독 이동은 `replace`라 언마운트되므로 **반드시 해제**한다(구독 2개가 살면 둘 다 콜백을 받는다 — 실측),
  ⑥ **캐시만으로 비어 있는 결과를 「없음」으로 읽지 마라**(오프라인·빈 캐시에서 13ms 만에 빈 스냅샷이 온다 — 실측).
  🔴 **`IntersectionObserver` root는 `useContext(ScrollRootContext)` 한 줄이면 된다.** DOM을 거슬러 오르지 마라.
  🔴 **IX-01·IX-02가 미생성이라 실기기 S7 목록은 ER-02다.** 버그가 아니다 — `database_ToDo/W-13.md` §1.
  🔴 **`recordCount` A안의 실행 순서는 W-15가 소유한다** — ①규칙 ②S6 증가를 **같은 회차에**,
  ③감소는 v1.1 삭제 회차. 뒤집으면 저장이 막히거나 값이 음수가 된다(`database_ToDo/W-13.md` §2).
  ⚠ **v1.1 삭제가 목록에서 행을 지우려면 합집합 병합 규칙을 먼저 풀어야 한다**(`reports/W-13.md` §8-4).

## 확정된 결정
- 🔴 **실시간 구독에는 `{ includeMetadataChanges: true }`가 필수다.** (W-13 §1-3 P9 · SDK 소스 실측)
  `onSnapshot`의 기본값은 `false`이고, 그때 `QueryListener.shouldRaiseEvent`가
  「문서 변화 0건 + 메타데이터만 변함」을 **통째로 버린다**(`syncStateChanged`는 `includeMetadataChanges === true`일 때만 통과).
  = 캐시 스냅샷 이후 `fromCache: true → false` 전환이 **콜백으로 오지 않고** 동기화 칩이 영원히 「동기화 지연」이 된다.
  `waitForSyncWhenOnline: true`는 **`getDocs`/`getDoc` 전용**이고 `onSnapshot`에는 없다.
- 🔴 **구독은 첫 페이지 1개 · 과거는 정적 `getDocs` · 병합은 ID 기준 `Map` 합집합 · 커서는 첫 페이지 마지막에 고정.**
  (W-13 §3.7 · 실측) 커서를 스냅샷마다 갱신하면 밀려난 구간에 **구멍**이 생기고,
  `docChanges()`의 `removed`로 지우면 `limit` 창 밖으로 밀려난 문서까지 사라져 같은 구멍이 생긴다.
  🔴 그 대가로 **`status: 'deleted'`로 바뀐 문서가 목록에 남는다** — v1.1 삭제 회차가 「방금 지운 ID」를
  별도 제외 집합으로 처리한다(`reports/W-13.md` §8-4).
- 🔴 **구독 해제를 빠뜨리면 구독이 쌓이고 둘 다 콜백을 받는다.** (W-13 §1-3 P5 실측)
  독 이동은 `replace`라 화면이 언마운트된다. 실측 궤적: 기록→홈 왕복 3회 후 활성 구독 **1개**.
- 🔴 **캐시만으로 비어 있는 목록에 EM-02를 띄우지 않는다.** (W-13 §1-3 P2-b · §4-5)
  오프라인·빈 캐시에서 `onSnapshot`이 **13ms 만에** `size: 0` · `fromCache: true`를 쏜다.
  그것을 「기록이 없다」로 읽으면 W-10 §5-3이 막으려던 **「오늘 0건」이라는 거짓말**과 같은 종류다.
  서버가 확인했거나(`fromCache === false`) 캐시에 행이 있을 때만 목록을 확정하고, 그 전까지는 스켈레톤을 유지한다.
  **왜 기다리는지는 동기화 칩이 말한다**(`오프라인` / `동기화 지연`).
- 🔴 **`IntersectionObserver`의 `root`는 `ScrollRootContext`다.** (W-13 §4-2)
  지시서는 DOM을 거슬러 `overflow-y` 조상을 찾으라고 했지만 **그 노드를 노출하는 컨텍스트가 W-10부터 있다.**
  `AppShell` 수정 0줄이고 `usePullToRefresh`에 이어 **소비자 2호**다. `root`를 주지 않으면 뷰포트 기준이 되어
  스테이지 안에서 **절대 교차하지 않는다**.
- 🔴 **S7 카운터는 `records.ts`가 소유한다. `stats.ts`를 건드리지 않았다.** (W-13 §4-3)
  `stats.ts`의 캐시는 `clearHomeCache()` **하나로 묶여 있어** S7의 당겨서 새로고침이 홈 통계·부서·명부까지 버린다
  (W-11 §4-5가 `roster.ts`를 가른 것과 같은 이유). 대가로 `오늘`·`이번 주` 질의가 S3와 S7에 각각 있다 —
  **합치지 마라.** 합치려면 캐시를 먼저 쪼개야 한다.
- 🔴 **`Date` 산술로 오늘·어제를 판정하지 않는다. `dateKey` 문자열을 비교한다.** (W-13 §3.5)
  `toDateKey(now)`·`toDateKey(now - 24h)`를 만들어 **문자열 `===`**로 본다(`checkDuplicate`가 이미 쓰는 방식).
  표기는 `formatDateKeyLabel(dateKey)`가 키에서 직접 만든다 — 인자가 `Date`가 아니라 **문자열**인 것이 의도다.
- 🔴 **ST-04의 +1은 CountUp 바깥에서 더한다.** (W-13 §4-7)
  `useCountUp`의 `target`을 바꾸면 애니메이션이 **0부터 다시 돈다** — §12.2의 「진입당 1회」가 깨진다.
  `target`은 서버 값 그대로 두고 화면이 `value + delta`를 그린다. `aria-label`도 **최종값 + delta**로 만든다.
- 🔴 **작성자 상태 조회 실패는 「탈퇴 아님」으로 떨어진다.** (W-13 §4 · DoD 16)
  반대로 하면 멀쩡한 부원에게 취소선이 붙는다. **실패를 캐시하지도 않는다**(다음 조회에서 재시도).
  묶음은 `documentId() in [...]`이고 상한 **30**이다 — 🔴 **클라이언트 SDK는 개수를 검증하지 않아**
  31개를 넣어도 던지지 않고 실기기에서 `invalid-argument`로 터진다(W-13 §1-3 P8 실측).
- **`.fchip`이 §15.1 조치로 올라갔다** — `min-height: 44px` · `padding: 12px 14px`. (W-13 §3 어긋난 칸 6)
  design `20f`의 `padding: 8px 14px`(높이 약 33)보다 **§15.1이 우선한다** — 접근성 조치가 시각 원본을 이기는
  유일한 종류의 이탈이다. 좌우 14는 그대로라 한 줄 배치(§8.7.3)가 영향받지 않는다.
- **필터 칩의 활성 표시는 `aria-pressed`다(`radiogroup` 아님).** (W-13 §4-8)
  `radiogroup`은 로빙 tabindex + 화살표 이동을 갖춰야 규격에 맞는데, 그러면 칩 4개가 Tab 정지점 1개가 되어
  AC-09와 어긋난다. §15.3에 필터 칩 항목이 없어 사유 세그먼트 규칙을 끌어오지 않았다.
- 🔴 **W-12는 `records` 문서 1건만 쓴다. `users.recordCount`를 올리지 않는다.** (W-12 §0.2 결정 1)
  §9.6·부록 B의 본인 자기 갱신 허용 키에 `recordCount`가 **없다.** 지금 넣으면 W-04 임시 catch-all 덕에
  통과하다가 **W-15가 §9.6 본편을 배포하는 순간 기록 저장이 통째로 막힌다.**
  → **`recordCount`는 영원히 `0`이며 이것은 버그가 아니다.** 규칙 개정 요청은 `database_ToDo/W-12.md` §3.
  ⚠ 저장 경로 밖에서 `users/{uid}.lastActiveAt` 쓰기 1건이 관측되는데, 그것은 W-07 `useLastActiveAt`(DR-12)다.
- 🔴 **`setDoc`을 `await`하지 않는다.** (W-12 §1-3 실측)
  `persistentLocalCache`가 켜져 있으면 `setDoc`의 promise는 **서버 확인까지 resolve되지 않는다** —
  오프라인에서 `disableNetwork()` 뒤 8초가 지나도 `PENDING`이고, 같은 문서를 즉시 `getDoc`하면 11ms에
  `hasPendingWrites: true`로 돌아온다. SDK 원문도 「resolved once the write was acked/rejected by the backend」다.
  기다리면 음영 구간에서 스피너가 영원히 돈다(W-08에서 실제로 터진 실패).
  `lib/records.ts`의 `writeRecord()`는 **`void`를 반환한다** — 호출부가 실수로 기다릴 방법이 없다.
  **그래서 서버 거부(규칙 위반·시각 위조)는 이 계층에서 관측되지 않는다.** W-17이 이어받는다.
- 🔴 **30분 중복 검사는 등식(`in` 포함) 질의뿐이다. IX-06을 쓰지 않는다.** (W-12 §1-4)
  `where('studentDocId','==',…)` + `where('dateKey','in',[어제, 오늘])`. `orderBy`·범위 조건 **0개**라
  미생성 인덱스에 의존하지 않는다. 나머지 판정(30분 창·사유·`reasonText`·`status`)은 전부 클라이언트다.
  **항상 2일치를 읽는다** — 00:10 작성 시 30분 창이 전날 23:40까지 걸치는데, 분기하면 경계 버그가 생긴다.
  §8.6.3의 MD-05 판정도 **같은 결과 집합**을 쓴다(추가 질의 0회).
- 🔴 **중복 조회 실패는 「중복 없음」이 아니다.** (W-12 §3.8)
  `permission-denied`·`unavailable`·`failed-precondition`에서 **저장은 진행하고 MD-05는 띄우지 않는다.**
  기록 누락(P-02)이 중복보다 비싸고 재전송 중복은 멱등키(BR-02)가 흡수한다. `DuplicateVerdict`에
  `unknown` 갈래를 따로 뒀다 — `none`과 뭉개지 마라. 실패는 콘솔 경고 1건으로 남는다.
- 🔴 **`clientRecordId`는 시트 1회 오픈당 1개다.** (BR-02)
  마운트 시점의 `crypto.randomUUID()`이고 **MD-05 확인·차단 후 재시도에도 같은 값**이다(실측 5회 동일).
  시트 초기화는 `useEffect`가 아니라 **`key` 재마운트**가 한다 — `occurredAt`·`clientRecordId`·사유·기타 입력값이
  한 곳에서 초기화된다.
- 🔴 **세그먼트 선택 배경은 절대 위치 인디케이터 1개가 갖는다.** (W-12 §5-2)
  항목마다 배경을 토글하면 T-02의 0.22s 가로 슬라이드가 물리적으로 불가능하다.
  200% 2행(2+1)은 미디어 쿼리가 아니라 **`flex-wrap: wrap` + 항목 `min-width: 6.5em`**이 만든다(design 원문).
- **`.spark`에 `z-index: 51`이 붙었다.** (W-12 §4-5)
  `.ovl-root`는 스태킹 문맥을 만들지 않아(z auto·transform none·…) 파티클이 딤(40)·시트(50) **뒤로 깔렸다.**
  시트 위, 모달(55)·토스트(60) 아래다. **모달 위에서 파티클을 터뜨리는 화면이 생기면 이 값을 다시 봐야 한다.**
  `.ovl-root`의 `pointer-events: none`은 되돌리지 않았다.
- **바텀시트에 아래 스와이프 닫기와 `onClosed`가 생겼다.** (W-12 §4-2 · 착수 전 승인)
  그랩 핸들에서만 시작하고(시트 전체에 걸면 캐럿 드래그·세그먼트 탭과 뒤엉킨다), 임계값 **80px**는 **신규 값**이다.
  닫힘은 기존 `requestClose()` **한 경로**로 들어간다. S5 행 탭 빗장은 `onClosed`에서 푼다.
- **`--shadow-inset-soft`가 승격됐다** — `inset 0 1px 3px rgba(20,53,38,0.05)`.
  `.ff`에 이어 `.seg`가 두 번째 컴포넌트가 되어 W-03B §3 기준을 넘겼다. **§7.1 보완 토큰은 10개다.**
- 🔴 **S5 헤더는 `sticky` + 유리 띠다.** (W-11 §4-1 · QA-11-01 st4R 승인)
  `position:sticky; top:0; background: rgba(255,255,255,0.6); backdrop-filter: blur(22px) saturate(170%)`.
  **리스트에 자체 `overflow`를 걸지 마라** — 스크롤 요소는 `AppShell` 하나다(DoD 16).
  design `1f`·`20g`는 「고정 헤더 + 스크롤 본문」을 **패널이 스스로 자르는 방식**으로 풀어서 헤더 배경이 없다.
  단일 스크롤에서는 행이 헤더 뒤로 지나가므로 가림막이 필요했고, 값은 `.dock`(스크롤 위에 뜨는 유일한 선례)에서 가져왔다.
- **`sticky` 헤더는 상단 여백을 먹지 않는다.** (W-11 §4-2 실측)
  `top: 0`인데도 띠가 스크롤 영역의 **패딩 가장자리**에 선다 — 여백 26 → `y 26`, safe-area 59 → `y 85`,
  배너까지 → `y 141`. 세 경우 모두 스크롤 전후 불변이다. **화면이 상단 여백을 다시 계산할 필요가 없다.**
- **명부 조회는 `lib/roster.ts`가 소유한다.** (W-11 §4-5) `stats.ts`에 넣지 않았다 —
  캐시가 `clearHomeCache()` 하나로 묶여 있어 S3의 당겨서 새로고침이 명부까지 버린다.
  `fetchDepartment()`만 `stats.ts`에서 가져다 쓴다(모듈 캐시가 살아 있다).
- 🔴 **`isActive`는 서버 질의가 아니라 클라이언트에서 거른다.** (W-11 §2.2 B안 · 실측 W11-1 ≠ W11-2)
  `fetchStudents`의 서버 질의는 `academicYear + grade + classNo + orderBy('number')` = **IX-08 그대로**다.
  넣으면 PRD §9.5에 없는 신규 인덱스가 하나 는다. 한 반 최대 37명이라 초과 읽기가 무의미하게 작다.
  **S4가 센 수와 S5가 그린 행 수는 일치한다**(DoD 12로 잠갔다).
- **`orderBy('number')`는 `studentNo` 오름차순과 항등이다.** (W-11 §1-3)
  `academicYear`·`grade`·`classNo`가 등식으로 고정되면 DR-01의 앞 3자리가 상수다. 스텁 12건으로 실측했다.
- **`classNo` 상한 검증은 S5가 한다.** (W-11 §5-5) 부서 문서를 읽어야 해서 라우터에 둘 수 없다.
  `ClassGuard`(하한)와 **같은 문구(TS-13) · 같은 이동 방식(`replace`)** 을 쓴다.
- **스크롤 요소는 `AppShell`이 소유하고, 새로고침 정책은 화면이 소유한다.** (W-10 §5-1)
  `ScrollRootContext`가 **노드만** 노출한다. **`AppShell`에 `onRefresh` prop을 만들지 마라** —
  여백 단독 소유자에게 데이터 정책이 붙는다. 화면마다 «무엇을 다시 불러오는가»가 다르다(W-07 §6.1).
- **조회 실패를 「0건」으로 읽지 않는다.** (W-10 §5-3) `stats.ts`는 `throw`하지 않고
  `{ ok | failed }` 판별 유니온을 돌려준다. `0 건`은 「기록이 없다」이고 `-`는 「모른다」다.
  뭉개면 오프라인에서 **「오늘 0건」이라는 거짓말**이 뜬다(§0.4 예외 1).
  같은 규칙이 학년 버튼에도 적용된다 — **「명부가 없다는 것을 아는」 경우에만** 비활성이다.
  조회 전(`null`)이나 조회 실패는 활성으로 두고 통과시킨다.
- **T-02(탭 재진입)와 T-05(당기기)는 다른 함수다.** (W-10 §4-9)
  전자는 세션 캐시로 **애니메이션만** 다시 돌리고(네트워크 0건), 후자는 네트워크까지 간다.
  세션 캐시는 **모듈 수준**에 둔다 — S3는 탭을 옮길 때마다 `ScreenTransition`의 `key`가 바뀌어
  통째로 다시 마운트되므로 컴포넌트 상태에 두면 재진입마다 재조회가 돈다. **실패는 캐시하지 않는다.**
- **`sparkle`은 컴포넌트가 아니라 명령형 함수다.** (W-10 §4-6)
  T-03은 sparkle과 라우팅을 **같은 프레임**에 건다(design 프로토타입이 그렇게 한다).
  화면이 소유하면 `navigate()` 직후 언마운트되면서 0.5s 애니메이션이 첫 프레임에서 끊긴다.
  `.ovl-root`에 직접 붙여 소유자를 없애면 전환을 가로질러 550ms를 살아남는다.
  `fixed`가 아니라 스테이지 기준 `absolute`다(W-03B §4-9와 같은 이유).
  reduce에서는 **아예 만들지 않는다** — `animation: none`만 걸면 막대가 그대로 떠 있는다.
- **당김 인디케이터를 만들지 않았다.** (W-10 §4-7) §8.3.4 T-05의 애니메이션 열이 `—`이고
  §7.4에 없다. design 79프레임에서도 0건. 재조회 중에는 §8.3.5의 로딩 스켈레톤을 그대로 재사용한다.
  따라올 시각 요소가 없으므로 `preventDefault`도 부르지 않는다 — **리스너 전부 `passive`**라
  iOS Safari 바운스와 충돌할 지점이 없다. 임계값 `70px`은 **신규 값**이라 PRD 반영 목록에 올렸다.
- **`dateKey`·`weekKey` 카운터에 `academicYear` 필터를 붙이지 않는다.** (W-10 §1-4)
  두 키가 특정 일자·주차를 가리켜 학년도가 이미 결정되고, IX-03·IX-04에도 `academicYear`가 없다.
  **`students` 카운트에는 붙인다** — 이쪽은 조건에서 학년도가 결정되지 않는다(IX-09).
  🔴 **W-12가 `records`를 쓸 때 반드시 같은 `toDateKey`/`toWeekKey`를 써야 한다.**
  화면과 집계가 다른 규칙으로 키를 만들면 카운트가 영원히 어긋난다.
- **로딩 스켈레톤의 시각 원본은 design `6d`다.** (W-10 §4-2) §20.7이 `6a`~`6d`를 채택으로 적는다.
  `40 × 22px` · 반경 `8px` · 3정지점 그라디언트 · **반짝임 없음**. 같은 그라디언트가 `6a`~`6d` 전부에 쓰이므로
  **W-11이 두 번째 사용처를 만들면 공용 `.skel`로 승격한다.**
- **오프라인 배너는 오버레이가 아니라 흐름이다.** (W-09 §0.2 · 사용자 확정)
  `AppShell`이 **직접 소유**한다 — 포털도 `BannerRootContext`도 없다. 만드는 쪽이 없기 때문이다
  (전역 1개 · 조건이 `useOnline()` 하나). 배너가 **여백을 바꾸므로** 여백 단독 소유자와 일치한다.
  상단 오프셋 `16px`은 `top`이 아니라 **`margin-top`** 이다 — `.ofb`에 `position`·`top`·`left`·`right`를
  되살리면 `env(safe-area-inset-top)`이 **두 번** 들어간다. 스크롤 영역은 배너가 있을 때
  safe-area를 다시 더하지 않는다(`26px`/`34px`만).
  design 채택본 **`10b`**(§20.7 `스플래시·배너·토스트 = 10a~10f`)가 상단 정렬 화면에서 밀어내기로 그려져 있다.
- **스플래시는 배너를 덮지 않는다.** (W-09 §3-1 — 사용자 확정, 안 1)
  `.splash`는 **스크롤 영역**의 `absolute; inset:0`이라 배너가 밀면 함께 줄어 겹칠 기하가 사라진다.
  z 비교(`100 > 70`)는 **여전히 유효하다** — 스크롤 영역·오버레이 루트가 스택 컨텍스트를 만들지 않는다.
  판정 중 배너가 보이는 것이 정상이고 스플래시 콘텐츠는 **28px**(중앙 정렬이라 절반) 내려간다.
  **배너를 조건부로 끄는 코드를 넣지 마라.**
  🔴 스테이지·스크롤 영역·오버레이 루트 중 하나에 `transform`·`filter`·`opacity<1`·`isolation`·
  `contain`·`will-change`를 붙이면 z 관계(100 > 70 > 40 > 30)가 **조용히** 깨진다.
- **`.ofb`의 `pointer-events: none`은 선언으로 보존한다.** (W-09 §3-2 — 사용자 확정)
  흐름으로 나오면서 `.ovl-root` 상속이 끊겼다. `auto`로 두면 시트가 열린 동안 배너 띠(상단 40px)에서
  **딤 탭-투-클로즈가 죽는다.** 그래서 `elementFromPoint(배너)`는 딤을 반환한다 — 정상이다.
  「배너가 딤 위인가」는 **페인트 순서**(프로브에서만 `pointer-events: auto`)나 **차등 픽셀**로 재라.
  🔴 `backdrop-filter`·알파가 걸린 요소에 **픽셀 동일성 판정을 쓰지 마라** — 뒤에 깔린 것도 픽셀을 바꾼다.
- **OP-01은 안 1 + 안 2다.** (W-08 §0.2 결정 1 — 사용자 확정)
  안 1: `inviteCodes`의 **문서 ID가 코드 문자열 자체**다(`inviteCodes/DJSN-2691`). Rules는
  인증된 `@dshs.kr`의 **단건 `get`만** 허용하고 `list`는 `head`·`dev`로 계속 잠근다.
  안 2: `useCount` 증가가 신청 시점이 아니라 **부장 승인 시점(OP-09)** 이다 → **OP-01 배치는 2건**이다.
  `code` 필드는 남긴다(ID는 경로, 필드는 데이터). Rules가 `data.code == codeId`로 둘을 묶는다.
  🔴 **W-11이 OP-09에 증가를 붙이기 전까지 `useCount`는 영원히 0이고 BR-14가 동작하지 않는다.**
- **본인의 재신청 전이는 §9.6의 좁은 예외다.** (W-08 §0.2 결정 2)
  BR-61·BR-28이 규정한 `withdrawn`·`rejected` → `pending`을 §9.6 요약표가 막고 있었다.
  6조건(이전 상태 2종 · 새 상태 `pending` · `role='member'` · `withdrawnAt`·`rejectReason` null ·
  `anonymizedAt` null · 변경 필드 부분집합)을 **전부** 만족할 때만 연다.
  `createdAt`·`recordCount`·`email`·`uid`는 불변이다. **규칙 작성은 W-15**이고 요청서는 `database_ToDo/W-08.md`.
- **제출 버튼 활성 조건은 "9자 완성"이지 "검증 성공"이 아니다.** (W-08 §5-1)
  design `13d`(검증 실패인데 버튼 활성)와 `13b`(7자인데 비활성)를 동시에 만족하는 규칙이 이것뿐이고,
  "검증 성공일 때만"으로 하면 **다 치고 바로 누르는 동선에서 탭이 버튼에 닿지 않아 blur도 안 일어난다.**
  형식이 틀린 9자도 활성으로 두고 탭하면 형식 에러가 뜨게 한다. 서버 재판정은 제출 때 어차피 돈다.
- **`Field`는 값 변형을 스스로 한다.** (W-08 §5-2) `transform?: (raw, caret) => { value, caret }`.
  화면은 `input` 요소에 닿을 수 없다 — 캐럿 복원은 `Field`의 `useLayoutEffect`가 한다.
  **의존성 배열을 붙이지 마라** — 변형 결과가 직전 값과 같으면 리렌더가 생략되고, 그때 DOM에만
  버려진 원문이 남는다. `FieldHandle.reset()`은 "판정 없음"을 그리는 유일한 수단이다(조회 실패).
- **조회 실패를 "없는 코드"로 읽지 않는다.** (W-08 §3.1) `lookupInviteCode`는 `null`이 아니라
  **판별 가능한 유니온**(`ok`·`missing`·`unusable`·`failed`)을 돌려준다. `failed`는 인라인 에러가
  아니라 **상단 배너 ER-01**로 가고 필드는 검증 이전 상태로 되돌린다.
  🟡 단 `getDoc` 캐시 폴백 때문에 SDK 계층에서 무력화될 수 있다 — W-17 항목과 같이 봐야 한다.
- **S2에 오프라인 배너를 붙이지 않았다.** (W-08 §5-7) `.ofb`(`safe-area+16px`)가 S2의 헤더
  (`safe-area+26px`)와 **실제로 겹치는 첫 화면**이다. 겹침 해소는 `AppShell` 슬롯 신설이 필요해
  유예했고, 대신 §8.2.5의 **힌트 교체 + 버튼 비활성**으로 오프라인을 알린다.
  오프라인 제출을 막는 것은 미관 문제가 아니다 — `batch.commit()`이 오프라인에서 **resolve되지 않아
  스피너가 영원히 돈다.**
- **S2-1 승인 감지는 A안(`refresh()`)이다.** (W-07 §3 — B `onSnapshot`·C 화면 직접 조회 기각)
  `refresh()`가 `resolve()`를 재사용하므로 **W-17이 `getDoc` 경로를 고치면 함께 고쳐진다** —
  A의 약점으로 지목된 캐시 폴백은 A가 새로 만드는 위험이 아니다.
  B를 기각한 결정적 이유: 규칙이 아직 임시인 상태에서 W-17이 다시 볼 지점 위에
  **모든 인증 읽기의 형태를 바꾸는 베팅**이기 때문이다.
- 🔴 **W-12 부채**: `onSnapshot`으로 가면 **`refresh()`와 S2-1 폴링 루프를 걷어내야 한다.**
  전문은 `reports/W-07.md` §9. 지금 심는 부채이고, 이것이 A안의 값이다.
- **`refresh()`는 판정을 흔들지 않는다.** (W-07 §3.2) 실패해도 `status`·`profile` 그대로,
  `loading`으로 되돌리지 않는다(스플래시가 화면을 덮는다). 미인증에서는 즉시 반환한다.
  `resolve()`의 `{ silent }`가 그 두 줄을 건너뛴다. **`silent` 없이 재조회하지 마라.**
- **폴링 주기는 화면이 소유한다.** (W-07 §6.1) `AuthProvider`에 넣지 마라 — 화면마다 정책이 다르다.
  `pending`에서만·`visible`일 때만 돌고, 복귀 시 즉시 1회 후 주기를 재개한다.
- **`/signup`의 `allow`에 `rejected`가 있다.** (W-07 §4 — BR-28) 착지 표는 그대로다
  (`rejected`의 기본 착지는 여전히 `/pending`). **W-08은 진입 상태가 3가지다** — `noProfile`(신규)·
  `withdrawn`(재가입)·`rejected`(재신청).
- **S2-1 변형 B에는 보조 줄이 없다.** (W-07 §5-2) design 3b·PRD §8.2.6은 "있다"이지만
  §6.1이 `rejected`에서 폴링을 멈추므로 `자동으로 반영됩니다`가 사실이 아니게 된다.
  **되돌리려면 조건 1개.** PRD 반영 후보다.
- **`.neu`를 S2-1 때문에 바꾸지 않는다.** (W-07 §5-4) design 3a·3c에는 내부 링이 없고 눌림이
  `5px/12px`·`scale(0.97)`이지만 **PRD §7.3이 내부 링과 `scale(0.98)`을 명시**한다.
  `.neu`는 S4·S5·S8이 함께 쓰는 공유 표면이다. `.pend-busy`도 `.neu:active`와 같은 값으로 맞췄다.
- **스피너 트랙은 회전부 색의 알파 0.22다.** (W-07 §1.4) S1 `#2E6B4C`→`rgba(46,107,76,0.22)`,
  S2-1 `#1F5138`→`rgba(31,81,56,0.22)`. **두 색을 통일하지 마라.** 공유되는 것은 색이 아니라
  관계라서 토큰 1개로 표현되지 않는다 — 승격하지 않았다.
- **`@theme`에 커스텀 스케일을 추가하면 `src/lib/cn.ts`에도 반드시 등록한다.** (W-07 §5-6)
  W-06이 `--text-brand`·`--text-subtitle`을 추가하고 등록을 빠뜨렸다. CSS 클래스로만 쓰는 동안은
  증상이 없다가 **유틸리티로 처음 쓰는 순간 글자 크기가 조용히 사라진다.** W-07에서 등록했다.
- **`BrandLogo`가 84px 로고의 단독 소유자다.** (W-06 §4) Splash·S1이 공유한다.
  **크기·색 prop을 만들지 마라** — 두 사용처가 같은 값이다. 등장 모션만 `className`으로 받는다.
- **`.brand-logo`·`.brand-title`·`.brand-sub`는 Splash와 S1의 공유 클래스다.** (W-06 §5-5)
  이름에 `splash`가 없는 것이 의도다. 한쪽만 보고 고치면 다른 화면이 함께 깨진다.
- **타이틀 스태거는 두 화면이 다르다.** Splash `0/.09/.18/.27/.36`, S1 `.05/.13/.21/.29/.37`.
  design `10a`·`12a` 원문이고 **오류가 아니다.** 복사해서 맞추지 마라.
- **부제 문구도 다르다.** Splash `대전대신고등학교`, S1 `대전대신고등학교 · 부원 전용`.
- **Google `G` 로고는 4색 원본이다.** (§8.1.2 각주) 단색화·재색상화·`currentColor` 치환 금지.
  §7 팔레트 제약의 **유일한 예외**다. 흰 버튼에 `shine`을 쓰지 않는다(design R-01).
- **오프라인 배너는 콘텐츠를 밀어내지 않는다.** (W-06 §5-3 — PRD §6.4 우선)
  `absolute` · `z-70` · 상단 `calc(env(safe-area-inset-top) + 16px)` · 좌우 18px.
  16px은 독의 `bottom` 16px을 뒤집은 값이다(design 12d의 54px은 목업 상태 표시줄 포함이라 못 쓴다).
  **버튼·닫기를 붙이지 마라** — `.ovl-root`의 `pointer-events:none`을 되돌리지 않는 전제다.
  🟡 **독이 있는 화면(W-08 이후)에서는 제목과 겹친다.** 그때 `AppShell` 슬롯 안을 재판단한다.
- **연속 탭 차단은 상태가 아니라 ref로 한다.** (W-06 §5-4 — 실측에서 발견한 결함)
  `submitting` 상태와 DOM `disabled`는 같은 태스크 안의 연속 탭을 못 막는다(5회 탭 → `signIn` 5회).
  동기적으로 즉시 서는 빗장이 따로 필요하다. **다른 제출 버튼에도 같은 규칙을 적용하라.**
- **S1은 이동시키지 않고 `signOut`·`deleteUser`도 부르지 않는다.** (W-06 §2.2)
  도메인 불일치 처리는 `AuthProvider`가 이미 한다. S1은 배너·칩을 그리기만 한다.
- **상태 배너(C·E)와 시도 실패 배너는 동시에 뜨지 않는다.** (W-06 §5-6)
  `domainRejected`는 `auth/delete-user-failed`를 동반할 수 있어 둘 다 그리면 배너가 2개 쌓인다.
- **정책 3종은 인증 판정도 기다리지 않는다.** (W-06 §0.1-3 — W-05 DoD 9 개정)
  `RouteHandle.skipsAuthGate`. **다른 라우트에 붙이지 마라** — 로그인 화면 깜빡임이 N-05가 막으려던 것이다.
- **라우터는 `react-router` 단일 패키지다(v8.3.0).** (W-05 §4-3)
  `react-router-dom`은 v7부터 재수출 껍데기다. 설치하지 마라.
- **`AppShell`이 라우트 루트에 1개, 그 안에 `ToastProvider` 1개, 바깥에 `AuthProvider` 1개다.** (W-05 §4-2)
  화면마다 `AppShell`·`ToastProvider`를 두면 전환할 때 토스트가 사라진다(W-03C §7-5).
  `hasDock`은 라우트 `handle`에서 읽는다.
- **`AuthProvider`는 `router.tsx`의 `RootLayout` 안에 있다.** (W-05 §4-6)
  스플래시 오류 화면의 `다시 시도`가 `key`를 바꿔 재마운트해야 하기 때문이다.
  `window.location.reload()`를 쓰지 마라 — 서비스 워커가 없어(W-16) 오프라인에서 앱이 통째로 사라진다.
- **독은 `DockRootContext`로 스테이지에 포털한다.** (W-05 §4-2)
  `.dock`은 `absolute`라 스테이지 기준이어야 하는데 `DockLayout`이 반환하는 트리는 스크롤 영역 안이다.
  오버레이 루트를 재사용하지 마라 — `pointer-events: none`이라 탭이 죽고, 독은 딤(z-40) **아래**다.
- **`ScreenTransition`(`blurIn`)은 `DockLayout`보다 안쪽에 둔다.** (W-05 §4-4)
  바깥에 두면 탭 이동마다 독이 재마운트되어 활성 알약의 가로 슬라이드가 죽는다.
  **`blurIn`은 `fill-mode: both`라 끝난 뒤에도 `filter: blur(0)`이 남는다** —
  래퍼가 절대 위치 자손의 컨테이닝 블록이 된다. 화면 안 `absolute`는 스테이지 기준이 아니다.
- **상태 리다이렉트는 조용히, 역할 리다이렉트만 TS-12 토스트다.** (W-05 §4-5)
  모든 리다이렉트에 토스트를 붙이면 미인증 사용자가 앱을 열 때마다 `접근 권한이 없습니다`가 뜬다.
- **N-04 vs §8.5.3 충돌은 나눠서 처리했다.** (W-05 §4-1)
  `grade` 형식 오류 → `/`, `classNo` 형식 오류 → `/grade/:grade`. 둘 다 TS-13.
  N-01(한 단계 상위)의 정신을 따랐다. **N-04를 근거로 되돌리지 마라** — 요약으로 본다.
  경로 파라미터는 `Number()`가 아니라 `/^\d+$/`로 읽는다(`Number(' 2 ')`는 2다).
- **`pulseDot`은 design 10a(1.4s ease-in-out)가 아니라 PRD §7.4(1.6s ease)다.** (W-05 §5.2)
  design 안에서도 1.4s는 7곳 중 1곳뿐이다. 지연 스태거 `0/.2/.4s`만 design을 따랐다.
- **`lastActiveAt`만 쓰고 `updatedAt`은 올리지 않는다.** (W-05 §4-8)
  접속을 "프로필 변경"으로 기록하면 `updatedAt`이 DR-12 동기화 신호와 섞여 구분 불가능해진다.
  쓰기 실패 시 스로틀 창을 되돌린다.
- **디자인 원본은 `design/index.html`이다.** (2026-08-20 사용자 확정)
  리셋 전 원격에 있던 `index.html`은 커밋이 더 나중이었으나 **채택하지 않는다.**
  "최신본 채택" 규약을 이 파일에 적용해 원격본을 다시 꺼내오지 말 것.
- 폰트는 CDN 방식이다(지시서 §2 고정값, PRD §7.1).
  `https://cdn.jsdelivr.net/gh/spoqa/spoqa-han-sans@latest/css/SpoqaHanSansNeo.css`
- **`.fchip-on`은 PRD §7.3의 단색 `#1F5138`이 아니라 design의 그라디언트를 쓴다.**
  (2026-08-20 사용자 확정 — PRD 표기를 전사 오류로 판단). PRD로 되돌리지 말 것.
- **`.shine` 적용 규칙은 W-02에서 신규 확정한 값이다.** 전문은 `reports/W-02.md` §5.2.
- 간격은 커스텀 토큰 없이 Tailwind 기본 `--spacing` 배수를 쓴다.
- **`.pill-fill` 그림자 없음은 의도된 차이다.** (W-03A 지시서 §0.3-2 확정)
  W-03B에서 실제 목록 행에 렌더한 뒤에도 어색하면 그때 다시 본다.
- **`cn()`은 `extendTailwindMerge`로 커스텀 스케일 3그룹을 등록했다.** (W-03A §1)
  `@theme`에 새 커스텀 스케일을 추가하면 `src/lib/cn.ts`의 `classGroups`에도 함께 등록해야 한다.
- **포커스 테두리 1.5px는 `border-width`가 아니라 inset 그림자로 낸다.** (W-03A §4-3)
  `border-width`를 바꾸면 padding box가 줄어 라벨 기준선이 흔들린다. 되돌리지 말 것.
- **`.ff-ro`의 "테두리 없음"은 `border: 1px solid transparent`다.** (W-03A §4-1)
  `border: 0`으로 지우면 읽기 전용만 라벨 좌측 기준선이 1px 어긋난다.
- **reduce에서 스피너는 정지시키지 않는다.** (W-03A §4-4) 진행 중을 알리는 유일한 피드백이다.
- **성공 상태는 blur에서만 켠다.** (W-03A §4-2 → **W-03B §1에서 수정**)
  `ref.validate()`(제출)도 성공 체크를 켜지 않는다. 제출 시점의 주인공은 에러다.
  단 **켜져 있던 성공을 끄지도 않는다** — 해제는 값 변경만 담당한다.
- **`-webkit-` 접두사를 손으로 적지 않는다.** (W-03B §4-1)
  표준과 나란히 적으면 lightningcss가 둘을 같은 속성으로 보고 **표준 선언을 지운다.**
  표준만 적으면 접두사판을 자동으로 덧붙인다. `backdrop-filter`에서 실제로 터졌다.
- **reduce에서 덮어야 하는 값은 인라인 `style`로 주지 않는다.** (W-03B §4-3)
  인라인은 미디어 쿼리가 이길 수 없다. 커스텀 속성(`--rise-delay`)으로 넘긴다.
- **`ResizeObserver` + `setState`는 결과를 폭에서 직접 계산한다.** (W-03B §4-2)
  직전 상태에서 한 칸씩 움직이면 경계 폭에서 무한 루프가 된다(React #185 — 실제로 380px에서 앱이 죽었다).
  `space-around`의 항목 사이 간격은 `free / n`이다(`n-1`이 아니다).
- **독 활성 배경은 항목이 아니라 절대 위치 알약 1개가 갖는다.** (PRD §6.2 · design `15d`)
  항목마다 배경을 켜고 끄면 가로 슬라이드가 불가능하다.
- **독 비활성 라벨 `rgba(20,53,38,0.5)`은 §15.2 AC-01과 충돌하는 채로 뒀다.** (W-03B §7)
  §6.2 원문을 따랐다. **임의로 0.70으로 올리지 말 것.** W-18 대비 실측에서 재판정한다.
- **Footer 링크 44px은 투명 패딩 + 음수 마진으로 낸다.** (W-03B §4-7)
  `padding: 15px 0` + `margin-block: -15px`. 시각 높이 14px이 유지돼야 접힘 줄 간격이 10px이 된다.
- **`fs.rmSync`가 이 환경(Node v24.2.0/Windows)에서 프로세스째 죽는다.** (W-03C §4-1)
  재귀가 아니라 파일 1개여도 exit 127(`0xC0000409`). `fs.promises.rm`·`unlinkSync`는 정상이다.
  vite `emptyOutDir`가 `rmSync`를 쓰기 때문에 `dist/`가 남아 있으면 빌드가 통째로 죽었다.
  **W-03B가 OneDrive를 원인으로 지목한 것은 오진이다.** `build` 스크립트가 선삭제로 막는다.
- **오버레이 3종은 스테이지 안 `absolute`다.** (W-03C §3·§4-2)
  `AppShell`의 오버레이 루트에 `createPortal`한다. `fixed`로 두면 430px 밖에서 스테이지를 벗어난다.
  루트는 스크롤 영역의 **형제**여야 딤이 배경 스크롤을 막는다. 루트에 `z-index`를 주지 마라.
- **포커스 트랩은 `open && mounted`로 건다.** (W-03C §4-3)
  `open`만 보면 마운트 전 렌더에서 ref가 비어 조기 반환하고, 이후 다시 돌지 않아 트랩이 죽는다.
- **토스트는 큐가 없다.** (W-03C §4-10) 새 메시지가 기존을 즉시 교체하고 `key`가 바뀌어 모션이 다시 돈다.
- **모달 버튼에 `Pill`을 쓰지 않는다.** (W-03C §4-8)
  "line/fill 스타일"은 표면 처리를 가리키는 말이고 모달 버튼은 높이 48 · radius 14다.
- **확인 모달의 초기 포커스는 좌측 `취소`, 딤 탭·ESC도 취소다.** (W-03C §4-7)
  파괴적 버튼에 포커스를 두면 Enter 오조작이 난다. 좌 버튼 라벨은 항상 `취소`이며 prop으로 열지 않는다.
- **충돌 해소 순서가 확정됐다.** (W-04 지시서 §0.2)
  1) PRD가 명시한 값이 있고 design이 그 손실 없는 이형이 아니면 → **PRD**
  2) PRD 표기가 design의 축약·전사로 보이면 → **design**
  3) PRD가 침묵하면 → **design**(DS-01)  4) 둘 다 없으면 → 멈추고 질문(QA-01)
  이 기준으로 시트 모션은 PRD(`translateY(110%)`) 유지, 시트 내부 하이라이트는 design(`0.9`)으로 되돌렸다.
- **인증은 Google Provider 단독이다.** (PRD C4 · §9.1 · W-04 §4)
  `email_verified` 검사 · 인증 메일 발송 · 비밀번호 API/필드를 **만들지 마라.** `users`에 비밀번호 필드가 없다.
  **이메일 로컬 파트를 분해하지 마라** — `25_yij1029@dshs.kr`의 `1029`는 생일이다(DR-14 · PR-05).
  **프로필 사진 URL을 저장하지 마라**(NG-13). 아바타는 `name` 첫 글자로 만든다.
- **도메인 거절은 `deleteUser` → `signOut` 순서다.** (W-04 §5.3)
  뒤집으면 권한이 사라져 계정이 그대로 남는다. `deleteUser`가 실패해도 로그아웃은 반드시 한다.
- **도메인 정규식 끝의 `$`는 필수다.** (§9.6 필수 규칙 1)
  없으면 `attacker@dshs.kr.evil.com`이 통과한다. 코드(`/@dshs\.kr$/`)와 Rules 양쪽 모두.
- **DR-10 교차 검증은 MVP 유보다.** (W-04 부칙 §1 — 선택지 A 확정)
  DR-14와 정면 충돌은 아니지만(문언은 생일 4자리·화면 노출을 겨냥) 비용/편익으로 접었다.
  산출물이 아무도 읽지 않는 `console.warn`이고, 유급·전입이 정당한 예외라 오탐이 기본값에 가까우며,
  학번 신선도는 DR-12 재파싱이 이미 덮는다. **"절대 분해하지 않는다"가 "앞 2자리만 분해한다"보다
  지키기 쉬운 규약이다.** DoD 22(`이메일 로컬 파트 분해 0건`)는 그대로 유효하다.
  PRD v1.3의 `[MVP 유보]` 표시는 **PM이 한다. PRD를 고치지 마라.**
- **W-15 주의: §9.6 요약표 ≠ 부록 B 전문.** (W-04 부칙 §2)
  자기 갱신 허용 키는 **부록 B가 정본**이다(`docs/SunDO_PRD.md:2866` — `name`·`memberStudentNo`
  계열 포함, `:2915` 주석이 근거). §9.6 요약표만 보고 Rules를 쓰면 **DR-12 동기화가 막힌다.**
  요약과 전문이 다르면 전문이 정본이다. 요약표 보정은 PM이 v1.3에서 한다.
- **도메인 거절은 인증 상태에서 파생시키지 않는다.** (W-04 §4-2 — 결함 수정)
  `signOut`이 `onAuthStateChanged(null)`을 발화시키면 시퀀스 가드에 걸려 `domainRejected`·
  `rejectedEmail`이 **아예 기록되지 않았다.** `rejectionRef` 래치를 `deleteUser` **전에** 세우고
  `clearRejection()`·정상 로그인에서만 푼다. T-05 배너·칩·버튼 라벨이 이 값에 달려 있다.
- **`users/{uid}` 조회 실패는 `noProfile`이 아니다.** (W-04 §4-3)
  문서 없음(`exists()===false`) → `noProfile`, 읽기 실패 → `loading` + `errorCode`.
  뭉개면 오프라인·규칙 오류일 때 활성 부원이 가입 신청 화면으로 튕긴다.
  ~~**W-06 전제**~~ → **W-05가 끝냈다.** `Splash`가 3초를 넘기면 `errorCode`로 오류 화면을 띄운다(§8.1.5).
  🟡 단 §9의 캐시 폴백 위험 때문에 이 분기가 실제로는 안 탈 수 있다 — 아래 W-17 항목을 함께 봐라.
- **DoD 18(리다이렉트 왕복)은 환경 제약이면 `보류(환경)`로 표시한다.** (W-04 부칙 §3 — 사전 승인)
  `signInWithRedirect`는 최신 Chrome의 서드파티 쿠키·스토리지 분할 정책 때문에 `localhost`에서
  `getRedirectResult()`가 `null`로 돌아올 수 있다. 호출은 정상인데 결과만 비면 **환경 제약이다.
  코드를 고치려 들지 마라.** W-19(배포 도메인)에서 재검증한다.
  폴백 **분기 로직**(standalone 감지 · 3개 에러 코드 → redirect)은 코드로 검증해 따로 보고한다.

## 예약된 작업 — 회차가 이미 정해진 것 (열린 질문이 아니다)

### 인라인 배너 `.banner` 승격 (W-13 지시서 §0.1-4)

**같은 값의 배너 선언이 이미 세 벌이다** — `.s1-banner`(W-06) · `.s2-banner`(W-08) · `.rs-banner`(W-12).
W-12 §4-4의 「새 표면을 만들지 않았다」는 승인됐고 **시안은 요청하지 않는다**(값이 같으므로 새 디자인이 아니다).
🔴 **다음에 인라인 배너를 쓰는 화면이 `.banner`로 승격한다**(W-11 §0.1-1과 같은 규칙).
S7은 배너를 쓰지 않아 W-13에서 손대지 않았다.

### W-18 접근성·모션 점검 이월 3건 (W-09 지시서 §5)

| # | 항목 | 출처 |
| --- | --- | --- |
| 1 | **토스트에 수동 닫기 수단 없음** — 스크린리더가 낭독하는 중에 2.2초로 사라진다 | W-03C §7-4 (W-07 지시서 §0.1 회신 4에서 W-18 이월 확정) |
| 2 | **`승인 확인` 쿨다운 중 남은 초 미표시** — 왜 눌리지 않는지 알 수 없다 | W-07 §7-5 |
| 3 | **검증 중 성공 테두리 선점** — `settleY`가 판정 전에 0.2s 조기 재생된다 | W-08 §5-3 (W-09 지시서 §0.1-5에서 **현행 유지 · W-18 후보 확정**) |

3번의 대안(조기 재생 억제)은 **규격에서 더 멀어진다**는 W-08의 분석이 채택됐다. 지금 고치지 않는다.
독 비활성 라벨 `rgba(20,53,38,0.5)` ↔ §15.2 AC-01 충돌 재판정도 **W-18 대비 실측**에서 함께 본다
(위 「확정된 결정」의 W-03B §7 항목).

### S6 기록 작성 시트 회차 — `Field` 계약 정리 (W-09 지시서 §0.1-1)

**열린 질문이 아니라 예약된 결정이다.**
`Field`의 `transform` prop과 `FieldHandle.reset()`은 승인됐다. 다만 **진짜 결함은 `reset()`이
없었다는 것이 아니라, 비동기 판정이 들어올 선언적 통로가 `Field`에 없다는 것**이다 —
`validate`가 `string | null` 2값이라 "판정 없음"을 표현할 수 없어, 조회 실패를 성공(거짓말)이나
실패(정상 코드에 오탐)로밖에 그리지 못한다. `reset()`은 그 구멍을 **명령형으로** 메운 것이고,
화면이 늘면 화면마다 다른 escape hatch가 생긴다.

**지금 고치지 않는다** — 실사용 소비자가 S2 하나뿐이라 설계할 근거가 부족하다.
~~S6 회차에 소비자 2개를 놓고 `Field` 계약을 정리한다.~~

🔴 **W-12에서 검토했고 전제가 성립하지 않아 다시 미뤘다**(`reports/W-12.md` §4-15).
S6의 `기타 사유`는 **완전히 동기적**이다 — 서버 조회가 없고 `checking`도 `reset()`도 쓰지 않는다.
즉 **두 번째 소비자가 그 구멍(비동기 「판정 없음」)을 재현하지 않아** 설계 근거가 여전히 S2 한 건뿐이다.
W-12가 넓힌 `counter?`는 표시 슬롯이라 `validate` 2값 문제와 무관하다.
→ **비동기 판정을 실제로 쓰는 두 번째 소비자**(S8 학생 임포트 검증 또는 S9 검색)가 생기는 회차로 옮긴다.

## 막힌 지점 / 사용자 확인 필요
- 🔴 **IX-01 · IX-02를 만들어야 S7 목록이 실기기에서 돈다.** (`database_ToDo/W-13.md` §1)
  W-13이 내는 질의 형태로 5건을 다시 갈랐다 — **지금 필요한 것은 2건**이고
  IX-03·IX-04·IX-05는 **등식 2개뿐이라 없어도 돈다**(W-10 실측 선례). IX-06은 여전히 소비자가 없다.
  🔴 **만들기 전까지 S7 목록은 ER-02 `기록을 불러오지 못했습니다`다 — 버그가 아니다.**
  `failed-precondition`이 EM-02로 새지 않도록 설계했다(W-13 DoD 17 — 3코드 전부 ER-02 실측).
  생성 절차(오류 링크 / 콘솔 경로)는 요청서에 있다. **필드 순서가 곧 인덱스다.**
  W-12 자신은 등식 전용 질의라 인덱스가 **0건 필요**하지만, 그 사실을 실 Firestore에서 확인하지는 못했다
  (쓰기 0건 제약). 명부 시딩 후 실기기 저장 1회로 확인 요청.
- ✅ **`users.recordCount`는 A안으로 확정됐다.** (W-13 §0.2 결정 1 · `database_ToDo/W-13.md` §2)
  §9.6 자기 갱신 허용 키에 `recordCount` 추가 + `request.resource.data.recordCount == resource.data.recordCount + 1`.
  🔴 **실행 순서가 전부다** — ① W-15가 규칙을 열고 → ② **같은 회차에** S6 저장 경로에 증가 한 줄 →
  ③ v1.1 삭제 회차가 감소. ②를 먼저 하면 W-15 배포 순간 저장이 통째로 막히고, ③을 먼저 하면 값이 음수가 된다.
  **W-13은 그 코드를 한 줄도 붙이지 않았다.** 그때까지 값은 `0`이며 버그가 아니다.
- 🔴 **학생 명부 시딩 — A안(콘솔 1개 반)으로 확정됐고 완료 여부는 미확인이다.** (W-13 §0.2 결정 2)
  W-13은 실 Firestore에 접속하지 않아 확인할 방법이 없다. **시딩 후 S7까지 보려면 IX-01·IX-02가 먼저다** —
  기록이 쌓여도 인덱스가 없으면 목록은 ER-02다. 아래는 W-12의 원문 배경이다.
- 🔴 **학생 명부 시딩 — 없으면 S6에 실기기로 도달할 수 없다.** (`database_ToDo/W-12.md` §6)
  `students`가 비어 S5가 빈 상태(EM-01)이고, 행이 없으면 시트를 열 트리거가 없다.
  **최소 1개 반**(문서 ID `{academicYear}_{studentNo}`)이 필요하다. §9.7 `[결정 필요 D-05]`가 열려 있다.
  W-13은 「기록이 실제로 쌓인 상태」를 요구하는데 그 기록을 만들 유일한 입구가 S6이고 S6의 입구가 명부다.
- 🔴 **Firebase 규칙 3건 — 「미반영」으로 확정됐다.** (W-11 §0.3 실측 · `database_ToDo/W-11.md` §4)
  `inviteCodes` 단건 `get` · 필수 조건 2/BR-30a 예외 · `users` update 재신청 예외.
  판정 근거는 **PRD에 없는 임의 컬렉션(`zzruleprobe`)이 읽힌다**는 것 — W-04 §5의 catch-all이 살아 있다.
  (`inviteCodes` list·`auditLogs` 읽기가 통과하는 것은 지금 계정이 `head`라 **판별력이 없다.**)
  **지금은 W-04의 임시 규칙(`@dshs.kr`이면 전부 허용) 덕분에 우연히 통과한다.**
  W-15가 §9.6 본편을 배포하는 순간 3건이 없으면 S2가 전부 막힌다.
  콘솔에 이미 만든 `inviteCodes` 문서가 있으면 **ID를 코드 문자열로 다시 만들어야 한다**(안 1).
- 🔴 **초과 승인 문제 — 여전히 미결.** (W-08 §10) 안 2로 `useCount` 증가가 승인 시점으로 갔다.
  한도를 채운 뒤에도 `pending` 신청이 남아 있을 수 있다 — 초과 승인을 허용할지, 승인 시점에
  재검사해 거부할지(문구가 §8.10에 없다), 자동 거절할지 결정이 필요하다.
  ⚠ **W-11이 판단할 것으로 적혀 있었으나 지시서 `prompts/W-11.md`의 범위가 S4·S5뿐이라 다루지 않았다.**
  승인 화면(S8 관리)을 만드는 회차로 넘긴다.
- 🔴 **IX-08 복합 인덱스를 만들어야 한다.** (`database_ToDo/W-11.md` §2)
  `students`: `academicYear ASC, grade ASC, classNo ASC, number ASC`. **PRD §9.5에 이미 설계돼 있다.**
  **만들기 전까지 S5는 ER-03 `학생 명부를 불러오지 못했습니다`가 뜬다 — 버그가 아니다.**
  만들면 EM-01로 바뀌고 칩이 `-명` → `0명`이 되는 것이 검증 지점이다. 생성 링크는 요청서에 있다.
  🔴 **`firestore.indexes.json`을 만들지 마라.** 저장소 규칙 체계를 세울지는 W-15 판단이다.
  ✅ W-10 DoD 4는 닫혔다 — S3의 세 질의는 인덱스 없이 통과한다(`OK count=0` ×3).
  ⚠ **`records`의 IX-01~IX-05도 전부 미생성이다.** W-12가 중복 검사(BR-13)를 붙이면 **IX-06**이 필요해진다.
- **PRD 반영 대기 50건(st4R).** — 정본은 **`reports/W-13.md` §7**의 회차별 전량 표다.
  W-12의 46건에서 2건이 닫히고(§0.1-4 「S6 배너 시안 불필요」 · §0.1-8 「ER-17 방어로 유지」)
  W-13 신규 6건이 붙었다. **W-13 신규**: ① §8.7.3 「최근 30일」 ↔ `limit(30)` 문언 충돌 ·
  ② `gh` 11px(PRD) ↔ 12px(design) · ③ Row title 14.5·Caption 11.5(PRD) ↔ 15·12(design) ·
  ④ §8.7.5에 「오프라인 + 빈 캐시」 행이 없다 · ⑤ 동기화 칩 「지연」 임계 시간 없음(**3,000ms는 신규 값**) ·
  ⑥ §7.2에 `20px`이 없다.
  🔴 **추가 — §9.6 「부원은 이름·역할 필드만」은 Rules로 표현할 수 없다.** `allow read`는 문서 단위다.
  W-13이 `users`를 처음으로 남의 문서까지 읽으므로 결정이 필요하다(`database_ToDo/W-13.md` §4 · 재검증 2건).
  아래는 W-11 시점의 이력이다.
- **~~PRD 반영 대기 38건(st4R).~~** (W-11 §7 — 회차별 표 하나로 정리했다)
  W-11 신규 5건 + W-10 신규 3건 + W-09 6건 + W-08 신규 13건 + 토큰 1건 + W-07 이월 5건 + W-06 이월 5건.
  ✅ **개수 논란은 끝났다** — 지시서 §0.1-8이 정본을 **33건**으로 확정했고(W-09 §6의 29건 + 누락분 1건 +
  W-10 신규 3건), 거기에 이번 신규 5건을 더해 38건이다. W-10 보고서의 「32건」은 누락분 1건이 빠진 수다.
  **W-08 신규 13건 중 6건이 §9.6·§11.2 규칙·오퍼레이션 개정이라 W-15와 직결된다.**
  W-11 신규 5건: IX-08에 `isActive`가 없다(클라이언트 필터 우회 · 명부가 차면 재검토) ·
  §8.5.3 정렬 키(`studentNo`)와 IX-08(`number`)의 **등가성 문언** ·
  `다시 시도` 버튼에 문구 코드 없음 · §8.5.2 #7 「세로 스크롤」이 구현 지정으로 읽힘 ·
  **S4 빈 상태 시안 없음**(아이콘 없이 문구만 그렸다).
- 🟡 **`docs/SunDO_PRD.md`가 미커밋 상태다.** st4R이 부록에 콘솔 작업 기록을 쓰는 중이다.
  시딩 ①②③④는 **전부 완료됐는데**(W-10 착수 시점 확인) 그 부록의 「진행 상황」 표는
  아직 ②③④를 `⬜ 미완`으로 적고 있다. 4행 전부 `✅`로 맞춰야 한다. PRD 반영은 PM 소유다.
- 🟡 **`students` 컬렉션이 없다 — 의도된 상태다.** (`database_ToDo/W-11.md` §5)
  실물에서 이렇게 보이는 것이 **정확한 동작**이다. 버그로 진단하지 마라.
  · S3 — 학년 버튼 3개 전부 회색 + TS-14
  · S4 — **1~10반이 뜨고 10칸 전부 `opacity 0.5`** + 탭하면 TS-15(카운트 질의는 인덱스 없이 통과한다)
  · S5 — **EM-01이 아니라 ER-03**(IX-08이 없어 `failed-precondition`). 인덱스를 만들면 EM-01로 바뀐다.
  명부 투입은 W-15(Rules 확정) 이후다. **순서는 인덱스 → 규칙 → 명부다.**
- **W-03C 보고서 §7의 확인 5건 중 4건이 미회신이다.**
  1. §1 지정 문자열(`fs.rmSync`)을 `fs.promises.rm`으로 바꾼 것 — 안 바꾸면 빌드가 죽는다
  2. 시트 내부 하이라이트가 design보다 알파 0.05 낮은 것(지시서 §5.1을 따른 결과)
  3. 시트 모션이 design `sheetUp`(55% + 페이드)이 아니라 지시서 `translateY(110%)`인 것
  4. 토스트에 수동 닫기 수단이 없는 것 → W-18 접근성 점검으로 넘길지
  5. ~~`ToastProvider`를 라우트 루트에 1개~~ → W-05에서 그대로 구현했다(§4-2). 남은 것은 1~4번이다.
- **Node 버전 재검토.** `fs.rmSync`가 죽는 것이 v24.2.0 고유 문제인지 확인이 필요하다.
  LTS(22.x)로 내리면 해결될 가능성이 높다.
- `public/fonts/`의 Spoqa TTF 5종은 CDN 채택으로 여전히 미사용이다.
  오프라인 PWA 단계에서 로컬 `@font-face` 전환 여부를 재검토한다.
- `README.md`·`LICENSE`(MIT)를 원격에 복구할지, 복구한다면 어느 작업 단계에 넣을지 미정.

## 해소된 항목
- ~~S4 빈 상태에 아이콘이 없다 (시안 필요 여부)~~ → **W-12 지시서 §0.1-5에서 「아이콘 없음이 정본」으로 닫혔다.**
  시안을 요청하지 않는다. §8.10.2 EM-07이 규정하는 것도 문구뿐이다.
  **PRD 반영 대기가 38건 → 37건이 됐다**(W-12 신규 9건을 더해 현재 **46건** — `reports/W-12.md` §7).
- ~~S6 기록 작성 시트를 여는 자리(W-11 §8-1)~~ → **W-12에서 붙였다.**
  `handleStudent(student, event)` 안, `spawnSparkle(...)` 바로 다음 줄. 행 탭 빗장은
  `setTimeout(…, 0)`이 아니라 **시트가 완전히 닫힐 때**(`BottomSheet.onClosed`) 푼다.
- ~~`toMonthKey`가 없다~~ → **W-12에서 `dateKeys.ts`에 추가했다.** `formatDateTimeKst`(BR-47 표기)도 함께다.
  **둘 다 기존 `toKst()`를 통과한다** — KST 변환을 복사하지 마라.
- ~~IX-06을 W-12 착수 전에 요청해야 한다(W-11 §8-4)~~ → **필요 없어졌다.**
  등식 `in` 질의로 대체해 W-12가 요구하는 인덱스가 **0건**이다. IX-06이 필요해지는 시점은
  서버 중복 검사 전환 또는 S7 학생별 이력이다(`database_ToDo/W-12.md` §2).
- ~~오프라인 배너와 상단 헤더의 겹침~~ → **W-09에서 밀어내기로 해소했다.**
  `AppShell`이 배너를 직접 소유하고(포털·컨텍스트 없음) 콘텐츠를 **56px** 민다.
  S2 실측: 배너 하단 `56` · 뒤로 버튼 `88.25` · 제목 `82` — 겹침 0.
  🔴 **화면에서 `<OfflineBanner />`를 그리지 마라.** 전역 1개이고 `.ofb`가 2개가 된다.
- ~~W-08 보고서 §7의 확인 6건~~ → **W-09 지시서 §0.1에서 전부 회신됐다.**
  1·2·3 승인 / 4 `9자 완성` 확정(지시서가 틀렸다) / 5 현행 유지 + **W-18 후보 확정** /
  6 이번 회차가 답이다. `Field` 계약 정리는 **S6로 예약**(위 「예약된 작업」).
- ~~W-03C 보고서 §7의 미회신 4건~~ → **W-07 지시서 §0.1에서 전부 회신됐다**(2회차 이월 종료).
  1 `fs.promises.rm` **현행 유지** · Node 다운그레이드 없음 · **OneDrive 오진 정정**(`reports/W-03B.md`에 기록) /
  2 시트 하이라이트는 W-04에서 이미 해소 / 3 시트 모션 `translateY(110%)` 유지(PRD §7.4 명시) /
  4 토스트 수동 닫기는 **W-18 이월 확정**.
- ~~W-06 보고서 §8의 확인 5건~~ → W-07 지시서 §0.2에서 전부 회신됐다.
  1 칩 X `aria-label` → **`거절된 계정 안내 닫기`**(`지우기`는 계정을 지운다고 읽힌다) /
  2 카드 하이라이트 `0.85` **유지** / 3 배너 겹침은 W-08 / 4 §0.1에서 회신 / 5 번들은 W-16.
- ~~S2-1 승인 감지 수단 부재~~ → **A안(`refresh()`) 확정·구현 완료**(W-07 §3).
- ~~W-05 보고서 §8의 확인 5건~~ → W-06 지시서 §0.1에서 전부 회신됐다.
  1 `permission-denied` → **`권한이 없습니다`**(EC-13·EC-42 원문, PRD v1.4에서 `ER-20`) /
  2 `DockRootContext` **승인** / 3 정책 3종은 **판정 대기를 건너뛴다**(W-05 DoD 9 개정) /
  4 번들은 W-16 / 5 W-03C 4건은 다음 회차.
- ~~`Footer`의 `onNavigate` 미연결(S1)~~ → W-06에서 연결. 전체 재적재 0건 실측.
  **S2-1·S10분은 아직이다**(W-07·W-10).
- ~~27px·13px 리터럴 승격 대기~~ → `--text-brand`·`--text-subtitle`로 승격(W-06 §3).
  로고 그림자도 `--shadow-logo`로 승격했다.
- ~~W-04 보고서 §7의 확인 4건~~ → W-05 지시서 §0.1에서 전부 회신됐다.
  1 `loading` 3초 타임아웃은 **W-05 소유**(스플래시와 함께) / 2 코드 분할은 W-16 /
  3 거절 계정 삭제 실패는 현행 유지 / 4 PRD v1.3은 PM이 한다.
- ~~`blurIn` 이식(W-03B가 W-05로 넘김)~~ → 완료. `.blur-in` + `@keyframes blurIn`(design 원문).
- ~~역할 전환 시 활성 탭 가드(W-03B §8-4)~~ → `RequireRole`이 처리한다(W-05 §4-5).
  강등된 계정이 `/admin`에 남아 있으면 다음 판정에서 `/` + TS-12로 밀려난다.
- ~~`tailwind-merge` 커스텀 스케일 충돌~~ → W-03A §1에서 해결. 실측 11케이스 통과.
- ~~`index.html`에 `viewport-fit=cover`·`theme-color` 부재~~ → W-03A §2에서 추가.
- ~~`.pill-fill` 그림자 확인 필요~~ → 의도된 차이로 확정. W-03B에서 실제 렌더 후에도 유지.
- ~~W-03A 확인 요청 5건~~ → W-03B 지시서 §0.1에서 전부 회신됐다.
- ~~W-03B 확인 요청 5건~~ → W-03C 지시서 §0.1에서 전부 회신됐다.
  1 빌드 스크립트 수정(§1) / 2 독 모션 reduce 0.01s(§2.2) / 3 **최소 지원 폭 360px 확정** /
  4 역할 전환 알약은 W-05 라우팅 가드 / 5 Footer 하단 여백 현행 유지.
- ~~빌드 크래시 원인 미상~~ → `fs.rmSync`로 특정하고 `build` 스크립트에서 막았다(W-03C §4-1).
- ~~DR-10 구현 여부~~ → **MVP 유보 확정**(W-04 회신 §1, 선택지 A). 근거는 `reports/W-04.md` §8.
- ~~DR-12 vs §9.6 자기 갱신 허용 키~~ → **충돌 아님.** 부록 B 전문이 정본이고 요약표가 좁았다.
  확인: `docs/SunDO_PRD.md:2866`(허용 키에 `name`·`memberStudentNo` 계열 포함) · `:2915`(사유 주석).
- ~~시트 내부 하이라이트 0.85 vs design 0.9~~ → **design(0.9)으로 되돌렸다**(W-04 §2).
  `--highlight-top` 토큰은 `.glass`·`.dock` 2곳이 계속 쓰므로 유지한다.
- ~~W-03B의 OneDrive 오진 기록~~ → `reports/W-03B.md` 2곳에 정정 1줄씩 덧붙였다(W-04 §1).
  1 끄기(§1 반영) / 2 승인 / 3 승인(`--color-sundo-tint-06` 신설) / 4 W-09으로 연기 / 5 승인.

## 참고
- **`src/App.tsx` 스토리 페이지는 W-05에서 사라졌다.** 필요하면 `git show 1e06962:src/App.tsx`.
  `App.tsx` 주석에도 같은 명령을 적어 뒀다. 되돌리기 비용은 한 줄이다.
- **`AuthProvider`는 `users/{uid}`를 `getDoc` 1회로 읽는다. 구독이 아니다.** (W-05 §6)
  역할 변경이 실시간 반영되지 않는다. R-06의 `getIdToken(true)` + 재조회는 **W-12**가 붙인다.
- **🟡 W-17 항목 — `getDoc` 캐시 폴백.** (W-05 §9 · W-05 지시서 §0.3)
  인증 판정 시 캐시 폴백 때문에 **조회 실패가 "문서 없음"으로 보일 수 있다.**
  `getDoc()`은 서버 실패 시 로컬 캐시로 떨어지고, 캐시가 "문서 없음"을 들고 있으면
  에러가 던져지지 않고 `exists() === false`가 반환된다. W-04 §4-3의 분기는 옳지만
  실제 `permission-denied`에서 안 탈 수 있다 — 활성 부원이 가입 신청 화면으로 튕긴다.
  **지금 없앨 수 없다.** 그 폴백이 곧 오프라인 지원(EC-01)이다.
  W-17에서 `getDocFromServer` 우선 + 실패 시 캐시 같은 전략을 판단한다.
  이 위험이 실현되면 W-05 §7의 `permission-denied` 문구 문제도 함께 사라진다. 두 항목을 같이 봐야 한다.
- **실측 방법(W-05).** `vite preview`가 SPA 폴백을 하므로 **모든 경로를 실제 URL 직접 진입**으로 쟀다.
  `status`·`role` 주입은 임시 `ProbeAuthProvider`가 맡았고(AuthContext를 임시 export),
  측정 후 4곳 전부 되돌렸다. **프로브 제거 후 빌드 산출물 해시가 도입 전과 같은 것이 제거의 증거다.**
- W-01 착수 시 원격 저장소가 비어 있지 않아(커밋 27개) 사용자 선택에 따라 히스토리를
  리셋하고 v0.0.1 단일 커밋으로 시작했다.
  기존 원격 히스토리 백업: `바탕 화면/SunDO_remote_backup_20260820.bundle`
  (복원: `git clone SunDO_remote_backup_20260820.bundle`)
- `design/index.html`은 번들 아티팩트라 마크업이 JSON 문자열로 인코딩돼 있다.
  CSS를 꺼내려면 이스케이프를 2단계 해제해야 한다. 방법은 `reports/W-02.md` §5 서두 참조.
- **검증 환경 주의.** W-02는 Chrome 확장으로 `getComputedStyle`을 읽었으나 W-03A 재개 세션에서는
  확장이 연결돼 있지 않았다. 헤드리스 Chrome을 CDP로 직접 구동해 같은 측정을 했다
  (의존성 추가 없음, node 전역 `WebSocket`/`fetch`만 사용). 방법은 `reports/W-03A.md` §1 참조.
  - 헤드리스는 창이 활성이 아니면 `focus`/`blur` 이벤트가 **발생하지 않는다.**
    `Emulation.setFocusEmulationEnabled`를 켜야 blur 검증을 실측할 수 있다.
- ~~`src/App.tsx`는 스토리 페이지다~~ → **W-05에서 라우터 루트로 대체됐다.**
- **순수 함수 실측 방법(W-04).** 테스트 러너가 없어 Node의 타입 스트리핑으로 `src/lib/*.ts`를
  직접 import해 실행했다. 확장자 없는 상대 import는 `module.register` 해석 훅으로 `.ts`를 붙였다.
  소스를 복사하지 않으므로 실측 대상과 저장소 파일이 어긋나지 않는다. 스크립트는 스크래치패드에 있다.
- **W-12 검증 방법.** 스텁 `firestore`가 `where(..., 'in', [...])`를 해석하고 질의 키를 기록한다.
  `setDoc`은 오프라인 모드에서 **로컬 반영 즉시 + promise 영원히 pending**으로 흉내 낸다 —
  화면이 `await`했다면 여기서 타임아웃 난다. 시각은 `Page.addScriptToEvaluateOnNewDocument`로
  앱보다 먼저 `Date`를 덮어 주입했다(**화면 코드에 시각 주입 훅을 만들지 않았다**).
  `clientRecordId`는 React fiber(`__reactFiber$…`)의 `useState` 체인에서 직접 읽었다.
  🔴 **히스토리 검증은 앱 내비게이션으로 화면에 들어가야 한다** — `Page.navigate`로 바로 꽂으면
  이전 엔트리가 `about:blank`라 뒤로가기 1회에 타깃이 detach된다.
  🔴 **쓰기 카운트는 `records/` 경로만 세라** — `useLastActiveAt`(DR-12)가 `users`를 상시로 쓴다.
- **최소 지원 폭은 360px이다.** (W-03C 지시서 §0.2) 320px에서 독 간격이 0이 되는 것은 지원 범위 밖이다.
- **토큰화 기준(W-03B 지시서 §3)**: 서로 다른 컴포넌트 2개 이상에서 쓰이면 토큰.
  같은 컴포넌트의 상태 변형 반복은 CSS 기계적 중복이지 토큰 수요가 아니다.
- W-09 승격 대기: `inset 0 1px 3px rgba(20,53,38,0.05)`(→ `--shadow-inset-soft`),
  `inset 0 1px 0 rgba(255,255,255,0.28)`(`.btnp`+모달 버튼 2종 3회),
  **`rgba(46,107,76,0.22)`**(스피너 트랙 — `.ff-spin`·`.gbtn-spin` 2개 컴포넌트, W-06에서 2번째 사용처 발생).
  `rgba(20,53,38,0.45)`·`inset 0 1px 0 rgba(255,255,255,0.85)`는 W-03C에서 승격 완료
  (`--color-sundo-ink-45` · `--highlight-top`). **§7.1 보완 토큰은 9개다**(W-08에서 `--color-sundo-tint-danger-10` 승격).

## 다음 세션이 먼저 읽어야 할 파일
- CLAUDE.md, docs/SunDO_PRD.md, prompts/{다음 작업}.md
- **reports/W-13.md §5(컴포넌트 API — `records.ts` 조회 계층 · `dateKeys` 2종 · 아이콘 2종) ·
  §8(넘길 전제 5건 — 🔴 §8-1 실시간 구독 규칙 7항) · §1-3(구독 계약 실측 P1~P9) ·
  §6(검증 환경 — 스텁 확장 4종과 측정 함정 8건)**
- **database_ToDo/W-13.md** — IX-01·IX-02 · `recordCount` 실행 순서 3단계 · `users` 읽기 재검증 2건 · 명부 시딩
- reports/W-12.md §5(컴포넌트 API — `RecordSheet` · `Segmented` · `records.ts` · 🔴 「쓰지 않는 5필드」 계약)
- database_ToDo/W-12.md — 규칙 재검증 시나리오 10건(W-13에서 변경분 0건)
- reports/W-11.md §5(컴포넌트 API — `roster` · `CenterNotice` · `.skel`/`.back` 승격) · §8(W-12 전제 5건)
- reports/W-10.md §5(`ScrollRootContext` · 신규 훅 3종 · `lib` 2종)
- reports/W-09.md §5(`AppShell` 신규 계약 · 스테이지 구조도)
- **reports/W-06.md §1(`AppShell` API 15항목 — W-09 §5가 1·2번을 갱신했다) · §9(W-07 전제)**
- reports/W-05.md §4(라우팅 판단 근거 10건) · §9(W-17 캐시 폴백)
- reports/W-03C.md §7(남은 확인 1~4번)
- 라우팅 구조가 궁금하면 `src/router.tsx` 하나면 된다 — 라우트 표·가드·전환·N-04가 전부 거기 있다.
- 화면 조립 예시는 `src/screens/Login.tsx`가 유일한 실물이다. 변형 분기·문구 상수 배치를 여기서 본다.
- 데이터 화면 조립 예시는 `src/screens/Home.tsx` + `src/lib/stats.ts`다.
  «화면은 Firestore를 직접 부르지 않고 `lib`이 판별 유니온을 돌려준다»는 계약을 여기서 본다.
- **목록 화면 + 4상태(로딩·정상·빈 상태·에러) 조립 예시는 `src/screens/ClassStudents.tsx` +
  `src/lib/roster.ts` + `src/components/CenterNotice.tsx`다.** `sticky` 헤더도 여기 있다.
- **실시간 구독 + 무한 스크롤 조립 예시는 `src/screens/Records.tsx` + `src/lib/records.ts`다.**
  구독 1개 · 정적 페이지 · ID Map 합집합 · 고정 커서 · `ScrollRootContext` root가 전부 거기 있다.

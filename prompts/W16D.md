# W-16D — 배포·시딩 실행 회차 (Rules 배포 + 전교생 명부 임포트 + 실기기 스모크)

목표 버전: **SunDO v0.9.1**
선행: W-16(`SunDO v0.9.0` · `06ad3f6`) 커밋·푸시 완료
기준 문서: 🔴 **`database_ToDo/W-16.md` (배포 실행 지시서 — 단일 입력)** · `reports/W-16.md` §8 ·
`database_ToDo/W-15A.md` §1~2 · `database_ToDo/W-15B.md` §1 · `database_ToDo/W-11.md` · `database_ToDo/W-12.md` §6 ·
`database_ToDo/W-14.md` §1 · `docs/SunDO_PRD.md` §9.3 · §9.5 · §14.1 · §14.5 · §18.4 · §20 · `CLAUDE.md`

> 🔴 **이 회차는 실 Firestore를 처음으로 건드린다.** W-16까지 접속은 0건이었고 검증은 전부 로컬 에뮬레이터였다.
> 여기서 **되돌리기 비싼 조작이 셋** 나온다 — ① 규칙 전량 교체(catch-all 제거) ② 학생 1,033건 임포트 ③ 부장 권한 양도.
> **앱 코드를 고치는 회차가 아니다.** `src/` `git diff` **0줄**이 원칙이다.
>
> 🔴 **스모크가 실패하면 규칙을 넓히지 마라.** `database_ToDo/W-16.md` §3으로 **1분 안에 되돌리고** §10으로 물어라.

---

## 0. 착수 전 확정 사항

### 0.1 §20.2 출력 규약 — 이 회차만 예외

이 회차는 **사용자의 손을 빌린다**(앱 조작·콘솔 확인은 Claude Code가 할 수 없다).
그래서 **단계별 지시와 확인 질문은 터미널에 출력한다.** 다만 배경 설명·근거 요약·완료 자축은 출력하지 않는다.

**출력 형식은 한 번에 한 단계다.**

```
[단계 N] {무엇을 하라}
확인할 것: {무엇이 보이면 통과인지}
→ 결과를 알려 달라
```

사용자의 답을 받기 전에 다음 단계로 넘어가지 마라. **여러 단계를 한꺼번에 쏟지 마라.**

### 0.2 계정 배치 (사용자 확정)

| 계정 | 지금 | 이 회차가 끝났을 때 |
| --- | --- | --- |
| `25_yij1029@dshs.kr` | 부장(`head`) | 🔴 **`dev`** |
| `25_kmc0311@dshs.kr` | 가입 완료 · 사용 가능 | 🔴 **부장(`head`)** |

🔴 **이 최종 배치는 §5의 스모크가 자연스럽게 만든다.** 콘솔에서 억지로 만들지 마라 —
그렇게 하면 양도 배치(OP-11)와 BR-23 복구 경로를 **한 번도 실물로 확인하지 못한 채** 넘어간다.
§5-5 → §5-8이 그 두 경로를 태우면서 배치를 완성하는 순서다.

### 0.3 순서 원칙 — 뒤집지 마라

| 순서 | 무엇 | 왜 |
| --- | --- | --- |
| ① | **시딩**(명부 · 일정) | catch-all이 살아 있을 때 넣는 편이 안전하다. Admin SDK는 규칙을 우회하지만, 규칙 배포 후 문제가 나면 원인이 둘로 갈린다 |
| ② | **인덱스** | 빌드에 **수 분**이 걸린다. `사용 설정됨`이 될 때까지 질의가 `failed-precondition`이다 |
| ③ | **규칙** | 여기서 W-04 임시 catch-all이 사라진다 |
| ④ | **스모크** | 🔴 실패하면 **즉시** §3 롤백 |
| ⑤ | 콘솔 확인 2건 · W-17 입력 수집 · 문서 갱신 · 커밋 1회 | — |

§18.4의 「Rules·인덱스 배포는 앱 배포보다 먼저」가 이 순서의 근거다.

### 0.4 이번 회차에 하지 마라

- 🔴 **앱 코드 수정** — `src/` `git diff` 0줄. 스모크가 앱 버그를 드러내면 **고치지 말고 §10으로 보고**하라(별도 회차다)
- 🔴 **`firestore.rules` 수정** — 스모크 실패라는 **실측 근거**가 나오기 전에는 한 글자도 고치지 마라
- 🔴 **서비스 계정 키 · 학생 명부 CSV/XLSX 커밋** — §2.4
- 🔴 **`auditLogs` 삭제·수정** — §9.6 필수 조건 6이 전면 금지한다. 되돌린 사실은 새 로그가 아니라 **보고서**에 남긴다
- 🔴 **§5 절차 밖에서 콘솔로 `users.role`·`departments.headUid`를 바꾸는 것** — 부장 2명/0명 상태가 조용히 만들어진다
- `firestore.rules.catchall`을 `firebase.json`이 가리키게 바꾸는 것 — 다음 배포가 조용히 catch-all을 올린다

---

## 1. 단계 0 — 현재 상태 인벤토리 (**코드를 쓰기 전에**)

🔴 **이 표가 §5 스모크 분기의 입력이다.** 사용자에게 물어 채우고, 채워지기 전에는 아무것도 실행하지 마라.

| # | 확인 대상 | 어디서 | 무엇을 적나 |
| --- | --- | --- | --- |
| 1 | `users` 전체 | 콘솔 Firestore | 문서마다 `email` · `role` · `status` (uid는 **끝 6자리만** 적어라) |
| 2 | `departments/dshs-jayul` | 같은 곳 | `headUid` · `activeInviteCodeId` · `memberCount` |
| 3 | `inviteCodes` | 같은 곳 | 문서 수 · `isActive: true`인 것의 ID |
| 4 | `students` | 같은 곳 | 문서 수 (0건이 정상이다 — `database_ToDo/W-11.md` §5) |
| 5 | `dutySchedules` | 같은 곳 | 문서 ID 목록 |
| 6 | 색인 | 콘솔 Firestore → 색인 | 이미 만들어 둔 복합 인덱스가 있는지 · 있으면 필드 순서 |
| 7 | Firebase CLI | 로컬 | `npx firebase login:list` · `npx firebase projects:list`에 `sundo-dev-8ef38`이 보이는지 |
| 8 | Java | 로컬 | `java -version` (규칙 재테스트에 필요하다 — 21이어야 한다) |

**이 표에서 반드시 갈라야 하는 것 2가지**

- 🔴 **미가입 `@dshs.kr` 계정이 하나라도 있는가.** §5-4(`inviteCodes` 단건 `get`)가 그것을 요구한다.
  `users` 문서가 없는 학교 계정이 없으면 §5-4를 돌릴 수 없다 → **§10-4로 물어라.**
- 🔴 **§0.2의 전제와 실제가 다른가.** 예를 들어 `kmc0311`이 아직 `pending`이면 §5-3(승인)이 먼저다.
  다르면 **§10-3으로 물어라.** 표에 맞춰 §5의 순서를 조정하되 **단계를 빼지는 마라.**

---

## 2. 단계 1 — 학생 명부 임포트 (1,033명 · 30개 반)

### 2.1 입력 파일

사용자가 저장소 루트의 `database_ToDo/2026_학생명부_임포트.csv`에 놓는다.

| 항목 | 값 |
| --- | --- |
| 헤더 | `grade,classNo,number,name` |
| 데이터 | **1,033행** |
| 학년별 | 1학년 **354** · 2학년 **346** · 3학년 **333** |
| 반 | **30개**(학년당 1~10반) |
| 학년도 | **2026** |

- 🔴 **번호가 비어 있는 자리가 있다**(예: 1학년 1반에 6번이 없다 · 2학년 2반에 2·8번이 없다). 전학·자퇴 자리다.
  **정상이다. 재번호를 매기지 마라.** 원본 `number`를 그대로 쓴다
- 🔴 **동명이인이 있다**(예: `박시후`가 학년을 넘어 3명). **이름으로 문서를 합치지 마라**
- 🔴 파일을 **읽어서 통계만 내라.** 이름 1,033건을 터미널·로그·보고서에 통째로 출력하지 마라(§14.1 PR-02)

### 2.2 🔴 스키마를 발명하지 마라 — 먼저 읽고 표로 보고한다

**코드를 쓰기 전에** 아래를 읽고 「필드 이름 → 값의 출처」 표를 터미널에 내고 사용자 확인을 받아라.

| 확인할 것 | 어디에 |
| --- | --- |
| `students` 문서 규격(필드 전량) | `docs/SunDO_PRD.md` §9.3 |
| 🔴 **문서 ID 형식** — `{academicYear}_{studentNo}` | `database_ToDo/W-12.md` §6 |
| 🔴 **`studentNo`의 정의** — §8.5.3 정렬 키와 IX-08의 `number`가 어떻게 대응하는지 | `database_ToDo/W-11.md` §2 · `reports/W-11.md` §7 |
| `isActive`의 기본값과 소비자(클라이언트 필터) | `database_ToDo/W-11.md` §2 |
| 앱이 실제로 읽는 필드 | `src/lib/roster.ts` — 🔴 **코드에서 직접 확인하라** |

🔴 **PRD와 CSV가 어긋나면 §10-2로 물어라.** 1,033건은 잘못 넣으면 되돌리기가 비싸다.

### 2.3 스크립트 — `scripts/seed-students.mjs`

| 항목 | 규정 |
| --- | --- |
| SDK | `firebase-admin` — 🔴 **`devDependencies`로만.** `dependencies` diff **0줄** |
| 자격 증명 | 환경변수 `GOOGLE_APPLICATION_CREDENTIALS`가 가리키는 경로. 🔴 **키 경로를 코드에 박지 마라** |
| 🔴 기본 동작 | **`--dry-run`이 기본값이다.** 실제 쓰기는 **`--commit`을 줬을 때만** 한다 |
| 배치 | 500건 단위 `writeBatch` · 실패한 배치의 인덱스를 남긴다 |
| 멱등 | 같은 문서 ID면 **덮어쓴다**(`set` + `merge: false`). 재실행해도 중복이 생기지 않는다 |
| 실행 전 출력 | 총 건수 · 학년별 · 반별 · 🔴 **중복 문서 ID 0건** · 필드 누락 0건 |
| 실행 후 출력 | 성공 건수 · 실패 건수 · 소요 시간. **이름은 출력하지 않는다** |

**절차** — ① `--dry-run`으로 돌려 요약을 사용자에게 보인다 → ② 사용자 확인 → ③ `--commit`.

### 2.4 🔴 개인정보 — 저장소에 들어가면 안 되는 것

`.gitignore`에 추가하고 **`git status`와 `git check-ignore -v`로 증명**하라.

```
# 학생 개인정보 · 서비스 계정 키 (커밋 금지)
database_ToDo/*.csv
database_ToDo/*.xlsx
*serviceAccount*.json
*-firebase-adminsdk-*.json
```

🔴 **커밋 직전에 `git show --stat`으로 이 셋이 0건임을 확인**하라(§11).

### 2.5 검증

| # | 확인 | 기대 |
| --- | --- | --- |
| 1 | 콘솔 `students` 문서 수 | **1,033** |
| 2 | 표본 3건(각 학년 1건)의 필드 | §2.2 표와 일치 |
| 3 | 앱 S4(반 선택) | 1~10반이 뜨고 **카운트가 회색이 아니다** |
| 4 | 앱 S5(학생 목록) | 목록이 뜬다. 🔴 `ER-03`이면 인덱스 문제다 — `database_ToDo/W-16.md` §8의 「IX-08은 등식 전용」 판단을 실측이 뒤집은 것이므로 **§10으로 보고** |

### 2.6 `dutySchedules` 시딩

- 필드 집합은 🔴 **`database_ToDo/W-14.md` §1 그대로.** 발명하지 마라
- 🔴 **문서 ID의 주차 키를 검산하라** — `src/lib/dateKeys.ts`의 주차 함수로 **오늘 날짜의 키를 계산**해서
  `2026-W35`가 맞는지 확인한다. 어긋나면 계산 결과를 정본으로 삼고 그 사실을 보고서에 적어라
- **이번 주 1건은 필수**, 다음 주 1건은 선택(T-04 「다음 주 미등록」 경로를 보고 싶으면 넣지 않는다)

---

## 3. 단계 2 — 배포 전 확인 3가지

`database_ToDo/W-16.md` §0 그대로다. **하나라도 실패하면 배포하지 마라.**

| # | 확인 | 명령 |
| --- | --- | --- |
| 1 | 커밋이 `SunDO v0.9.0`인가 | `git log -1 --format='%H %s'` |
| 2 | 🔴 규칙 테스트 **전량 통과** | `npm run rules:test` — **152건 / 실패 0** |
| 3 | `email_verified` 검사 0건 | `grep -n "token.email_verified" firestore.rules` |

⚠ 2번은 Java 21이 PATH 앞자리에 있어야 돈다(`reports/W-16.md` §6-1).

---

## 4. 단계 3 — 배포 (🔴 인덱스가 먼저다)

`database_ToDo/W-16.md` §2의 명령을 그대로 쓴다.

1. `npx firebase login` → `npx firebase projects:list` → `npx firebase use default`
   🔴 **`sundo-c5095`·`sundo-dev`를 고르지 마라.** 정본은 `sundo-dev-8ef38`이다
2. `npm run indexes:deploy`
3. 🔴 **콘솔 → Firestore → 색인**에서 IX-01·IX-02가 **`사용 설정됨`**이 될 때까지 대기.
   ⚠ **2건인지, 4건으로 늘지 않았는지 확인**하라(§8.1 — 늘었으면 필드 순서가 다른 것이고 파일이 정본이다)
4. `npm run rules:deploy`
5. 콘솔 → 규칙에서 게시 시각이 방금인지, 첫 줄이 `// SunDO — Firestore Security Rules`인지 확인

🔴 **4번을 실행한 시각을 기록하라.** 롤백 판단과 보고서 타임라인의 기준이다.

---

## 5. 단계 4 — 실기기 스모크 (🔴 **실행 순서는 의존 순이다**)

`database_ToDo/W-16.md` §4의 7건은 **위험 우선순위** 순서다. 실행은 **데이터 의존 순**으로 한다.
아래 순서대로 **한 번에 한 단계씩** 사용자에게 지시하고 결과를 받아 적어라.

| 실행 | §4의 # | 무엇을 누르나 | 통과 판정 | 실패하면 |
| --- | --- | --- | --- | --- |
| **5-1** | **1** 🔴 | 부장 계정 → 독 `관리` → ② `부원 · 권한 양도` | 부원 행이 **보인다**(아바타 + 이름 + 역할) | `users` **list**. 🔴 **앱에서 처음 쓰는 형태다** — 여기가 1순위 위험 |
| **5-2** | **3** | ① `코드 재발급` → 모달 확인 | 새 코드가 화면에 표시된다 | 재발급 배치 4연산(`inviteCodes` create/update · `departments` update · `auditLogs` create) |
| **5-3** | **4** 🔴 | **미가입 계정**으로 로그인 → S2 → 5-2의 코드 입력 | 코드가 **검증된다**. 🔴 `네트워크에 연결할 수 없습니다`면 실패 | `inviteCodes`의 `allow get`. **없으면 신입이 아무도 못 들어온다** |
| **5-4** | **3** | 부장 → `관리` → ③ 신청 행 `승인` | 행이 사라지고 토스트. ② 목록에 그 계정이 뜬다 | 승인 배치 4연산 |
| **5-5** | **2** 🔴 | 부장(`yij1029`) → ② → `kmc0311` 행의 `양도` → 모달 `양도` | 토스트 `부장 권한을 양도했습니다` → 홈 이동 → 독에서 `관리` 소멸. 🔴 **`접근 권한이 없습니다`가 뜨면 규칙이 아니라 앱 문제다** | `users` update의 자기 강등 · `departments` update |
| **5-6** | **5** | 부원 계정 → 홈 → 학년 → 반 → 학생 → 사유 → `저장` | 토스트 `기록 저장됨`. 🔴 콘솔에서 `users/{본인}.recordCount`가 **+1** | `records` create + `recordCount` 「+1만」. **이번에 앱이 2연산으로 바뀐 자리다** |
| **5-7** | **6** | 아무 계정 → 독 `일정` | 일정이 보인다. 🔴 `일정을 불러오지 못했습니다`(ER-05)면 실패 | `dutySchedules`의 `allow get` |
| **5-8** | **7** 🔴 | §5.1 절차 | 양도가 **성공한다** | 🔴 **여기가 막히면 부장 0명 사고에서 복구할 방법이 사라진다** |

**추가 회귀(순서 무관)** — S3 홈 통계 · S7 기록 목록(IX-01) · S7 사유 필터 3종(IX-02) · S5 학생 목록.

### 5.1 🔴 5-8 상세 — Dev 복구 경로 + 최종 계정 배치

5-5가 끝나면 `kmc0311 = head` · `yij1029 = member`다. 여기서 **부장 0명 상태를 일부러 만들고 Dev로 복구**한다.
이 절차가 BR-23을 실물로 닫으면서 §0.2의 최종 배치를 완성한다.

| # | 어디서 | 무엇을 |
| --- | --- | --- |
| 1 | 콘솔 | `users/{yij1029}.role` → **`dev`** |
| 2 | 콘솔 | `users/{kmc0311}.role` → **`member`** ← 🔴 **이 순간 부장 0명이다**(`headUid`는 여전히 `kmc0311`) |
| 3 | 앱 | `yij1029`(Dev)로 새로고침 → 독 `관리`가 **보이는지** 확인 → ② → `kmc0311` 행의 `양도` |
| 4 | 확인 | 🔴 **3연산으로 성공**한다(강등 대상이 `head`가 아니라 그 연산이 빠진다) |
| 5 | 콘솔 | `users/{kmc0311}.role == 'head'` · `departments.headUid == kmc0311` · `auditLogs`에 `HEAD_TRANSFER` |

⚠ **2번 상태에 오래 머물지 마라.** 부장 0명에서 앱이 도는 시간을 최소로 한다.
🔴 **3~4가 막히면 즉시 §3 롤백**하고, 무엇이 거부됐는지 콘솔 오류 문구를 그대로 받아 적어 §10-1로 보고하라.

**최종 확인** — `yij1029 = dev` · `kmc0311 = head` · 승인해 들인 계정 = `member`.

### 5.2 🔴 실패했을 때 — 1분 롤백

`database_ToDo/W-16.md` §3 그대로. **판단하지 말고 먼저 되돌린다.**

```bash
cp firestore.rules firestore.rules.broken
cp firestore.rules.catchall firestore.rules
npx firebase deploy --only firestore:rules
cp firestore.rules.broken firestore.rules && rm firestore.rules.broken
```

되돌린 뒤 **반드시**: ① 실패한 화면 문구를 그대로 기록 ② 그것을 `tests/rules/`에 **케이스로 옮긴다**
③ 🔴 **재배포 전에 §10-5로 보고한다.** catch-all 상태로 오래 두지 마라 — 역할 조건이 **하나도** 없다.

---

## 6. 단계 5 — 콘솔 확인 2건 (§14.5 13·14)

규칙으로 표현할 수 없어 **눈으로 봐야** 닫히는 항목이다.

| # | 어디 | 확인 | 어긋나면 |
| --- | --- | --- | --- |
| 13 | `Authentication > Sign-in method` | **이메일/비밀번호 Provider가 사용 중지**인가 | 사용자에게 알리고 **직접 끄게 하라** |
| 14 | `Authentication > Settings > Authorized domains` | 목록을 그대로 적는다 | 🔴 **배포 도메인이 아직 없으므로 `localhost`·`*.firebaseapp.com`만 있는 것이 정상.** 모르는 도메인이 있으면 **지우지 말고 §10으로 보고**하라 |

---

## 7. 단계 6 — 🔴 W-17 입력 수집 (결정 3건)

`reports/W-16.md` §8이 「규칙을 넓히지 말고 물어라」로 남긴 것들이다.
사용자에게 **하나씩** 묻고 답을 `database_ToDo/W-17-input.md`에 기록한다. 🔴 **답을 발명하지 마라.**

| # | 물음 | 배경(한 줄로만 설명하라) | 선택지 |
| --- | --- | --- | --- |
| 1 | 🔴 `departments.memberCount` 감소(BR-57)를 어디서 하나 | 탈퇴 배치에 넣으면 그 연산이 거부되고 **배치 전체가 죽는다** — 탈퇴하는 본인은 `head`가 아니다(§8-3) | ① 감소 포기(부정확 허용) ② 통계 시 실시간 count ③ Cloud Function(v1.2) |
| 2 | `dev` 계정의 탈퇴를 허용하나 | 규칙이 `role != 'head'`만 보므로 **Dev는 지금 탈퇴된다**(§8-2) | ① 그대로 둔다 ② 막는다(규칙 확장 — W-17에서 판단) |
| 3 | 부장 탈퇴 버튼의 안내 문구 | 규칙만 믿으면 사용자는 「눌렀는데 아무 일도 안 일어난다」를 본다. §8.10에 문구가 **없다** | ① 지금 문안을 확정 ② 규격 공백으로 W-17 §7에 올린다 |

사용자가 답하지 않으면 **「미결」로 기록하고 넘어가라.** 붙잡고 있지 마라.

---

## 8. 산출물

| 산출 | 파일 | 내용 |
| --- | --- | --- |
| 임포트 스크립트 | `scripts/seed-students.mjs` | §2.3 |
| 보고서 | `reports/W-16D.md` | §9 |
| 🔴 **배포 요청서 갱신** | `database_ToDo/W-16.md` | §9 병렬 진행 확인란의 `⬜`를 **실제 결과**로 바꾼다(`✅` 또는 `🔴 실패 + 이유`). §5 표의 13·14번도 채운다 |
| W-17 입력 | `database_ToDo/W-17-input.md` | §7의 답 3건 + §5-8의 최종 계정 배치표 |
| 진행 상황 | `docs/PROGRESS.md` | 현재 버전 `0.9.1` · 완료 목록에 W-16D · 🔴 **「막힌 지점」에서 닫히는 항목을 닫아라** — 「W-08 규칙 3건 미반영」 · 「`students` 컬렉션이 없다」 · 「학생 명부 시딩 미확인」 |
| 무시 목록 | `.gitignore` | §2.4 |

---

## 9. 보고서(`reports/W-16D.md`)가 담아야 할 것

1. **타임라인** — 시딩 · 인덱스 · 규칙 배포 · 스모크 각각의 시각과 명령, 출력 요약
2. 🔴 **스모크 8단계 결과표** — 통과/실패. **실패면 화면에 뜬 문구를 그대로** 적는다(추측한 원인이 아니라)
3. **명부 임포트 실측** — 총 건수 · 학년별 · 반별 · 실패 0건 · 소요 시간 · 🔴 **이름은 적지 마라**
4. **§14.5 보안 점검 14항목 최종 상태** — `database_ToDo/W-16.md` §5의 표를 **실제 확인 결과로** 갱신(자동 9 · 부분 3 · 콘솔 2)
5. **롤백 여부** — 했다면 무엇이 실패해서, 어떤 케이스를 테스트에 추가했는지
6. **최종 계정 배치표** + `departments/dshs-jayul` 최종 상태
7. 🔴 **W-17에 넘길 전제** — `reports/W-16.md` §8의 4항목을 **실물 확인 결과로 갱신**한다.
   특히 §8-4(「아직 아무것도 배포되지 않았다」)를 **무엇이 실물로 확인됐고 무엇이 남았는지**로 바꿔 써라
8. **PRD 반영 대기(st4R)** — W-16의 **83건**에서 이번에 닫히는 것과 신규분

---

## 10. 멈추고 물어야 하는 조건 (QA-01)

1. 🔴 **스모크가 실패했는데 원인이 규칙인지 앱인지 갈릴 때** — **규칙을 넓히지 마라.** 롤백부터 하고 물어라
2. `students` 스키마가 PRD와 CSV 사이에서 어긋날 때 — **1,033건은 되돌리기가 비싸다**
3. §1 인벤토리가 §0.2의 계정 배치 전제와 다를 때
4. **미가입 `@dshs.kr` 계정이 없어 5-3을 돌릴 수 없을 때**
5. 🔴 **롤백을 했을 때** — 재배포 전에 반드시 보고
6. 인덱스가 콘솔에서 4건으로 늘었을 때(필드 순서가 다른 중복)

**형식**

```
[확인 필요] {한 줄 요약}
상황: {무엇이 어떻게 막혔는지 — 화면 문구·오류 코드 그대로}
선택지
 A. {안} → 결과 / 되돌리기 비용
 B. {안} → 결과 / 되돌리기 비용
추천: {A 또는 B}와 이유
```

---

## 11. 커밋 — 측정이 전부 끝난 뒤 **한 번**

- 메시지 **`SunDO v0.9.1`** (GT-03) · `package.json`·`docs/PROGRESS.md` `0.9.1` (GT-07)
- 🔴 **`git show --stat`으로 증명할 것 3가지** — 서비스 계정 키 0건 · `*.csv` 0건 · `*.xlsx` 0건
- 🔴 **`git diff` `src/` 0줄**
- 🔴 **Contributors에 Claude를 포함하지 않는다** (GT-06)
- 터미널 마지막 출력은 두 줄:
  `작업이 완료되었습니다` / `Firebase에서 작업할 사항이 있습니다. Markdown File을 확인하세요`

# W-18 — 정책 화면 3종 + Footer 연결

목표 버전: **SunDO v0.11.0**
선행: W-17(S10 설정 + 계정 탈퇴 · `SunDO v0.10.0`) 커밋 완료
기준 문서: `docs/SunDO_PRD.md` §8.10 · §8.12(정책 화면 — §2.1에서 확인) · §14.1 · §14.2 · §14.3 · §17.1 ·
`design/index.html`(`16a`·`16b` 계열 — §2.1에서 확인) · 🔴 **`reports/W-17.md` §8** · `CLAUDE.md`

> 🔴 **이 회차의 산출물은 코드가 아니라 「사실」이다.**
> 지금까지의 회차는 화면이 규격대로 도는지를 물었다. 이번에는 **문서가 구현과 일치하는지**를 묻는다.
>
> 🔴 **개인정보 처리방침이 구현보다 앞서면 그것은 약속 위반이다.**
> §14.3 익명화는 **v1.2로 미뤄져 있다** — 지금 탈퇴해도 이름·이메일이 그대로 남는다
> (`reports/W-17.md` §5.1이 실측으로 확인했다: `anonymizedAt` 없음 · 17개 필드 불변).
> 「탈퇴 즉시 파기」라고 쓰면 **사실이 아닌 문장을 배포하는 것**이다.
>
> ⚠ **이 회차가 만드는 것은 법률 자문이 아니라 초안이다.** 최종 문안은 PM이 확정한다(§0.3 결정 3).

---

## 0. 착수 전 확정 사항

### 0.1 회차 로드맵 — 변화 없음

**W-18**(이번 · `v0.11.0`) → W-19 PWA → W-20 오프라인 → W-21 접근성 → W-22 배포(`v1.0.0`).

🔴 **W-17로 기능 화면 S1~S10이 전부 섰다.** 남은 회차는 화면이 아니라 품질·배포 레이어다.

### 0.2 W-17 보고서 회신 (10건)

| # | 항목 | 회신 |
| --- | --- | --- |
| 1 | 🔴 §4.1 CSS가 `@media (max-width:380px)`에 갇혔다 — 삽입 앵커 오독 | 🔴 **승인. 이 회차의 최고 산출이다.** 「소스·빌드·린트가 전부 통과하므로 번들을 봐야만 드러난다」와 「한 파일 안에서 어떤 것은 되고 어떤 것은 안 되는 상태가 신호다」 둘 다 정확하다. **규약 승격 승인** |
| 2 | 🔴 §4.2 결정 3을 코드 쓰기 전에 대조해 철회시켰다 | 🔴 **승인.** design과 PRD 3곳이 일치하고 결정만 달랐다면 고칠 것은 결정이다. **PROGRESS에 이력으로 남긴 것까지** 옳다 |
| 3 | §4.3 BR-57은 4연산이 아니라 2연산 · `B-17`이 증명 | **승인.** 「4연산 중 3개는 통과한다는 관측이 무의미하다」가 Q-3의 연장이다 |
| 4 | 🔴 §4.4 `USER_WITHDRAW`는 발명이 아니라 PRD 내부 어긋남의 보정 · `B-18` | **승인.** 규칙이 `action`을 보지 않는다는 실측으로 재배포 0회를 만든 것이 좋다. **익명화를 넘어 살아남는 유일한 기록**이라는 근거도 정확하다 |
| 5 | §4.5 `appendAudit`을 재사용 못 하고 두 벌이 됐다 | **승인 — 다만 부채로 명확히 남긴 것이 옳다.** DoD 15와 규약이 충돌할 때 하드 조건을 택하고 §7에 올린 판단을 지지한다 |
| 6 | 🔬 §4.6 `affectedKeys()`는 「값이 바뀐 키」다 | 🔴 **승인.** 「규칙이 느슨하다로 읽으면 틀린다」가 핵심이고, `B-20`으로 성질 자체를 문서화한 것이 최선이다 |
| 7 | §4.7 낙관적 UI를 `useEffect` 없이 | **승인.** 기준선 18을 지키면서 렌더도 줄였다 |
| 8 | §6.1 시크릿 세션 단위 격리 + W-16D 재검토 | **승인.** 「창이 아니라 데이터가 신원을 증명했다」가 정확하다 |
| 9 | §5.3 온라인 ✅ / 오프라인 ⬜ 를 갈라 적었다 | 🔴 **승인.** 「생기지 않도록 막았다」와 「생겨도 괜찮다」는 다른 문장이다 — 그 구분이 이 프로젝트의 규율이다. **W-20 이관 유지** |
| 10 | §7 st4R 신규 9건 | 확인. PM 소유로 이월. 🔴 **신규-9(`vice`·`teacher` 실계정 부재)는 이번 회차에도 걸린다** — §6.2 |

**보정 회차(W-17F)** — 행 우측 정렬과 `htm0303` 재가입 진단은 그 문서가 닫는다.
🔴 **재가입 진단이 「앱 버그」로 판정되면 이 회차보다 그것이 먼저다.** 착수 전에 확인하라.

### 0.3 🔴 사용자 확정 결정 3건

**결정 1 — 문서는 구현에 맞춘다. 미구현 기능을 약속하지 않는다.**

🔴 **이것이 이 회차의 제1원칙이다.** 「~할 예정입니다」도 쓰지 마라 — 일정을 약속하는 문장이 된다.
현재 상태를 **있는 그대로** 쓰고, 바뀌면 그때 문서를 고친다.

특히 §2.2의 인벤토리와 한 줄이라도 어긋나면 **그 문장을 지워라.**

**결정 2 — 오픈소스 라이선스는 프로덕션 번들에 들어가는 것만 열거한다.**

`dependencies`가 대상이고 `devDependencies`(`vite`·`oxlint`·`firebase-tools`·
`@firebase/rules-unit-testing`·`firebase-admin` 등)는 **사용자에게 배포되지 않으므로 넣지 않는다.**
🔴 **`package.json`에서 실제로 읽어라.** 기억으로 목록을 만들지 마라.

**결정 3 — 🔴 법적 검토는 PM 소유다.**

이 회차는 **사실에 근거한 초안**을 만든다. 법률 자문이 아니다.
🔴 **학교·교육청의 개인정보 처리 지침과 대조하는 것은 PM의 몫**이고, 배포 전에 그 확인이 필요하다.
보고서 §8에 「PM 확인이 필요한 문장」을 **목록으로** 뽑아라.

### 0.4 승계되는 계약 — 깨지 마라

| 계약 | 이번 회차에서의 의미 |
| --- | --- |
| 🔴 **「이미 있는 메커니즘이 지시서와 다르면, 있는 것을 쓰고 보고한다」** | 일곱 회차 연속 옳았다. `/policy/*` 라우트는 **이미 등록돼 있다**(`router.tsx:177~179`) |
| 🔴 **CSS는 소스가 아니라 번들을 본다** | W-17 §4.1의 규약. 새 CSS를 넣으면 **번들에서 선택자 직전에 `@media`가 없는지 확인**하라 |
| 🔴 **시크릿 창은 세션 단위로 격리된다** | 계정을 바꾸려면 전부 닫는다 |
| **의존성 추가 0** | |
| **`AppShell`·`router.tsx`·기존 화면 9종·`lib` 기존 6종 불가침** | `git diff` 0줄 |
| **문구는 §8.10 사전** | ⚠ **정책 본문은 예외다** — 사전이 담을 성격이 아니다. §3.2 참조 |
| **터미널 출력 두 줄 · 커밋 1회 · Claude 트레일러 금지** | |

### 0.5 이번 회차가 만들지 않는 것

- 🔴 **§14.3 익명화 구현** — v1.2다. **문서가 그것을 있다고 쓰면 안 된다**
- 🔴 **`firestore.rules` 수정** — 정책 화면은 Firestore를 읽지 않는다. diff 0줄
- **개인정보 열람·정정·삭제 요청 처리 화면** — 규격에 없다. §9-3
- **동의 획득 흐름(가입 시 약관 동의 체크박스)** — §2.1에서 규격을 확인하고, 없으면 §9-2
- **PWA · 오프라인 · 접근성 전면 점검** — W-19~W-21
- **App Check · BR-30** — W-22

---

## 1. 산출물

| 산출 | 파일 | 성격 |
| --- | --- | --- |
| 정책 화면 3종 | `src/screens/policy/*`(경로는 §2.1에서 확인) | 자리표시자 → 본체 |
| 본문 데이터 | 위치는 §3.1에서 판단 | 🔴 **긴 본문을 JSX에 직접 박을지 별도 모듈로 뺄지 근거와 함께** |
| 공용 셸 | 필요하면 신규 1종 | 3화면이 같은 구조를 쓴다면 |
| 보고서 | `reports/W-18.md` | §8 |
| 콘솔 요청서 | `database_ToDo/W-18.md` | 🔴 **필요 없으면 만들지 마라.** 이 회차는 Firestore를 건드리지 않는다 |

---

## 2. 사전 조사 — 코드를 쓰기 전에 표로 보고한다

### 2.1 규격과 시안

| 확인 대상 | 무엇을 봐야 하나 |
| --- | --- |
| PRD 정책 화면 조항 | 🔴 **절 번호를 직접 찾아라.** §8.12 부근일 것이나 확정하지 마라. 요소 표·문구·목차 구조 |
| §14.1 · §14.2 · §14.3 | 개인정보 통제(PR-**) · 보관 · 익명화. **처리방침의 근거 조항이다** |
| `design/index.html` | 정책 화면 시안. Footer가 `#16a`를 가리키므로 `16a`·`16b` 계열부터 본다. 🔴 **없으면 WF-05대로 물어라** |
| `router.tsx:177~179` | `/policy/privacy` · `/policy/terms` · `/policy/oss` — **이미 있다.** 화면 파일명·자리표시자 형태 확인 |
| `Footer.tsx` | `variant="full"`의 링크 3개가 위 3경로를 가리키는지 |
| S10 `약관·정책` 카드 | 같은 3경로인지(W-17 §8-1) |
| 뒤로 가기 | 정책 화면의 헤더·뒤로 버튼이 어느 부품인지(S2·S5가 쓰는 것) |

### 2.2 🔴 **구현 사실 인벤토리 — 이 회차의 핵심 산출**

처리방침 본문을 쓰기 **전에** 아래를 코드·규칙·실측에서 확인해 표로 보고하고 사용자 확인을 받아라.
🔴 **한 칸이라도 「아마도」가 있으면 그 문장을 본문에 쓰지 마라.**

| # | 물음 | 어디서 확인 |
| --- | --- | --- |
| 1 | **무엇을 수집하나** — 사용자(부원) | `users` 문서의 전 필드. Google 계정의 `name`·`email`·`displayNameRaw` |
| 2 | 🔴 **무엇을 수집하나 — 학생** | `students`의 전 필드(**실명 1,033건**) · `records`의 `studentName`·`studentNo` **비정규화 보관** · `reasonText`(자유 서술) |
| 3 | 🔴 **정보주체와 사용자가 다르다** | 학생은 이 앱을 쓰지 않는데 이름이 저장된다. **처리방침이 이것을 다루지 않으면 핵심이 빠진 것이다** — §9-1 |
| 4 | **누가 볼 수 있나** | `firestore.rules`의 각 컬렉션 `allow get`/`list`. 🔴 **「부원은 이름·역할만」은 규칙이 아니라 클라이언트 규율이다**(W-14 결정 3 · W-16 §4-2) |
| 5 | 🔴 **얼마나 보관하나** | ① `users` — 탈퇴해도 **이름·이메일 잔존**(익명화 v1.2 · W-17 §5.1 실측) ② `auditLogs` — 🔴 **수정·삭제 전면 금지 = 영구** ③ `records` — 소프트 삭제(`status:'deleted'`), 실제 파기 경로 없음 ④ `students` — `--prune`은 `isActive:false`일 뿐 삭제가 아니다 |
| 6 | **어디에 저장되나** | Firebase Firestore(Google Cloud). 🔴 **리전을 콘솔에서 확인하라** — 국외 이전 여부가 갈린다 |
| 7 | **처리 위탁** | Google(Firebase Authentication · Firestore · Hosting) |
| 8 | **제3자 제공** | 코드에서 외부 전송이 0건인지 확인 |
| 9 | **자동 수집** | `lastActiveAt` 등 접속 기록이 있는지 |
| 10 | **알림** | `notificationPrefs`는 **값만 저장**하고 Web Push 발송은 **v1.1 미구현**(W-17 §9) |
| 11 | **이용자 권리 행사 경로** | 🔴 앱에 열람·정정·삭제 요청 수단이 **있는가**. 이름은 Google 계정이라 앱에서 못 바꾼다(§8.11 T-02) |
| 12 | **재가입** | 재신청 6조건 · BR-62(역할 초기화) · BR-63 — `database_ToDo/W-17.md` §3 |

### 2.3 오픈소스 라이선스 수집

- 🔴 `package.json`의 **`dependencies`만** (결정 2). 현재 6개로 알고 있으나 **직접 읽어 확인하라**
- 각 패키지의 **라이선스 종류 · 저작권 표기 · 원문 링크**를 `node_modules/*/LICENSE`에서 읽어라
- ⚠ **전이 의존성(transitive)을 어디까지 넣을지 판단하고 근거를 남겨라.** 전부 넣으면 수백 개다
- ⚠ **Spoqa Han Sans Neo 폰트**도 포함 대상인지 판단하라 — CDN으로 로드하는 서드파티 자산이다

---

## 3. 구현 지시

### 3.1 화면 3종 — 공통 셸

- 🔴 **세 화면이 같은 구조라면 셸을 하나로 뽑아라.** 세 벌 복사는 이 프로젝트가 계속 피해 온 형태다
- 본문이 길다 — **JSX에 직접 박을지 데이터 모듈로 뺄지 판단하고 근거를 남겨라**.
  ⚠ 번들 크기 영향을 실측하라
- 목차·앵커·접힘이 규격에 **있는지 확인하고, 없으면 만들지 마라**
- 뒤로 가기는 기존 부품을 쓴다. 🔴 **`AppShell` 여백 계약을 건드리지 마라**
- 200% 확대·루트 32px에서 가로 스크롤 0. 긴 URL·라이선스 원문이 **넘치지 않게** `overflow-wrap`

### 3.2 🔴 개인정보 처리방침 — **사실 대조표를 함께 만든다**

본문의 **모든 사실 주장**에 대해 근거를 1:1로 대는 표를 보고서 §3에 남겨라.

| 본문 문장 | 근거(코드·규칙·실측) | 판정 |
| --- | --- | --- |

**절대 쓰지 마라 — 지금 사실이 아닌 문장들**

- 🔴 「탈퇴 시 개인정보를 **즉시 파기**합니다」 → **익명화는 v1.2다.** 이름·이메일이 남는다
- 🔴 「탈퇴 시 개인정보를 **익명화**합니다」 → 같은 이유
- 🔴 「보관 기간 경과 후 **자동 파기**됩니다」 → 자동 파기 경로가 **없다**
- 🔴 「이용자는 앱에서 개인정보를 **열람·정정·삭제**할 수 있습니다」 → §2.2-11에서 확인되기 전에는 쓰지 마라
- 「알림을 발송합니다」 → **Web Push는 v1.1 미구현**이다

**반드시 다뤄야 할 것**

- 🔴 **학생 개인정보** — 정보주체(학생)가 이용자(부원)와 다르다. §2.2-3
- 🔴 **감사 로그가 영구 보존된다** — 규칙이 수정·삭제를 전면 금지한다. 그 사실과 이유(위·변조 방지)
- **기록의 비정규화 보관** — 작성자가 탈퇴해도 기록의 학생 이름은 남는다
- **`reasonText`는 검색 대상이 아니다**(§14.1 PR-02 · 인덱스 면제) — 통제의 근거로 쓸 수 있다

### 3.3 서비스 이용약관

- 🔴 **동의 시점이 언제인지 규격에서 확인하라.** 가입(S2)에 동의 절차가 없다면 그것이 규격 공백이다 — §9-2
- 이용 자격(`@dshs.kr` 학교 계정) · 역할과 권한 · 금지 행위 · 계정 정지·탈퇴 · 면책
- 🔴 **부장 권한 양도가 되돌릴 수 없다**는 사실을 다뤄라(OP-11 · W-15B)
- ⚠ **미성년자가 이용자다.** 그 사실이 약관 구조에 영향을 주는지는 **PM 확인 항목**으로 §8에 올려라

### 3.4 오픈소스 라이선스

- 결정 2대로 `dependencies`만. 패키지명 · 버전 · 라이선스 · 저작권 표기
- 🔴 **MIT·Apache-2.0은 저작권 고지 유지가 조건이다.** 요약만 쓰지 말고 고지 문구를 넣어라
- Apache-2.0(Firebase)은 **NOTICE 파일 유무**를 확인하라

### 3.5 Footer · S10 연결 확인

- 🔴 **새로 만들지 마라.** 링크는 이미 있다(W-17 §8-1). **실제로 이동하는지 확인**하고 끝이다
- Footer `variant="full"`(S10)과 `compact`(S1·S2-1)에서 각각 확인
- 정책 화면 안의 Footer가 **재귀 링크**가 되는지 확인 — 규격에 없으면 자기 화면 링크는 비활성

### 3.6 만들지 마라

동의 체크박스 · 열람·정정 요청 폼 · 문의 이메일 발송 기능 · 다국어 · 인쇄용 스타일 ·
버전 이력 테이블(규격에 없으면) · `firestore.rules` 수정 · 여섯 번째 `clear*Cache`.

---

## 4. 규약

| ID | 규칙 |
| --- | --- |
| 4-1 | **의존성 추가 0** |
| 4-2 | 🔴 **`firestore.rules` diff 0줄** |
| 4-3 | 🔴 **본문의 모든 사실 주장에 근거를 댄다**(§3.2 대조표). 근거 없는 문장은 쓰지 않는다 |
| 4-4 | 🔴 **CSS를 추가하면 번들에서 선택자 직전에 `@media`가 없는지 확인**(W-17 §4.1 규약) |
| 4-5 | 린트 규칙을 끄지 마라 · 경고 18(기준선) |
| 4-6 | 값이 §7.1~7.4에 없으면 승격 여부를 먼저 판단한다 |
| 4-7 | 🔴 **법률 문언을 발명하지 마라.** 다른 앱의 처리방침을 베끼지도 마라 — 이 앱의 사실과 다르다 |

---

## 5. `database_ToDo/W-18.md`

🔴 **Firestore를 건드리지 않으므로 필요 없을 가능성이 높다.** 다음이 나오면 그때 만들어라.

- Firestore **리전** 확인(§2.2-6) — 국외 이전 문언이 갈린다
- 개인정보 보호책임자·문의처를 문서에 넣어야 한다면 그 정보(PM이 제공)

---

## 6. 검증 환경

### 6.1 화면

- 🔴 **시크릿 창.** 계정을 바꿀 때는 전부 닫는다(W-17 §6.1)
- S10 → 3화면 각각 이동 → 뒤로 가기 → Footer에서도 이동
- 200% 확대 · 루트 32px · reduce에서 가로 스크롤 0
- 🔴 **번들 크기 증가분을 실측하라.** 본문이 길다

### 6.2 역할

- 🔴 **정책 화면은 로그인 전(S1)에서도 열려야 하는지 규격에서 확인하라.** 가입 전 동의가 있다면 필수다
- ⚠ `vice`·`teacher` 실계정이 없다(W-17 §7 신규-9). 정책 화면이 역할에 무관하다면 문제없고,
  무관함을 **코드로 증명**하라

---

## 7. 완료 조건 (DoD)

| # | 조건 |
| --- | --- |
| 1 | `npm run build` exit 0 · `npm run lint` 경고 18 · 의존성 diff 0줄 |
| 2 | §2 사전 조사 3건(규격·시안 / 🔴 **구현 사실 인벤토리 12항** / 라이선스 수집)이 표로 |
| 3 | 🔴 **`firestore.rules` git diff 0줄** |
| 4 | 🔴 **§3.2 사실 대조표** — 본문의 사실 주장 전량에 근거가 1:1로 달렸다 |
| 5 | 🔴 **금지 문장 0건** — §3.2의 5종을 `grep`으로 증명 |
| 6 | 처리방침이 §2.2-3(학생 정보주체) · 감사 로그 영구 보존 · 비정규화 보관을 다룬다 |
| 7 | OSS 목록이 `package.json`의 `dependencies`와 **1:1로 일치**한다 |
| 8 | 3화면이 S10과 Footer 양쪽에서 열리고 뒤로 가기가 동작한다 |
| 9 | 200% 확대 · 루트 32px · reduce에서 가로 스크롤 0 |
| 10 | 🔴 **번들 크기 증가분 실측값** |
| 11 | 🔴 **`AppShell`·`router.tsx`·기존 화면 9종·`lib` 기존 6종 git diff 0줄** |
| 12 | `npm run rules:test` 전량 통과(회귀 확인) |
| 13 | 🔴 **PM 확인이 필요한 문장 목록**이 보고서 §8에 있다 |
| 14 | 커밋 `SunDO v0.11.0` · `package.json`·`PROGRESS.md`·S10 버전 표시 `0.11.0` · Claude 트레일러 없음 |

---

## 8. 보고서(`reports/W-18.md`)가 담아야 할 것

- **§2 구현 사실 인벤토리 12항** — 이 회차의 정본 산출
- **§3 사실 대조표** — 본문 문장 ↔ 근거
- **§4 판단과 이탈** — 특히 본문 배치(JSX vs 데이터 모듈) · 전이 의존성 범위 · 폰트 포함 여부
- 🔴 **§8 PM 확인이 필요한 문장 목록** — 결정 3. 법적 판단이 걸린 문장을 전부 뽑는다.
  최소: 국외 이전 · 미성년자 · 보관 기간 · 학생 정보주체의 권리 행사 경로
- **§7 st4R** — W-17의 목록에서 이어받는다. 신규 후보:
  ① 정책 화면 규격에 동의 시점이 없다면 그것 ② 열람·정정 요청 경로 부재
  ③ §14.3 익명화 미구현이 처리방침과 충돌하는 지점
- **§9 다음 회차(W-19 PWA)에 넘길 전제** — 최소: manifest에 들어갈 이름·아이콘 자산이 어디 있는지 ·
  🔴 **`public/fonts/`의 Spoqa TTF 5종을 로컬 `@font-face`로 전환할지**(W-01 §5-2 미결) ·
  COOP 경고(W-16D)가 PWA 설치 흐름에 영향을 주는지

---

## 9. 멈추고 물어야 하는 조건 (QA-01)

1. 🔴 **학생 개인정보의 정보주체(학생 본인)가 권리를 행사할 경로가 규격에 없을 때** —
   문언을 발명하지 마라. 이것은 제품 결정이다
2. 🔴 **약관 동의 시점이 규격에 없을 때** — 가입(S2)에 동의 절차가 없다면 그 자체가 결정 대상이다
3. 열람·정정·삭제 요청 처리 방법을 본문에 써야 하는데 앱에 경로가 없을 때
4. `design/`에 정책 화면 시안이 없을 때(WF-05)
5. Firestore 리전이 국외이고 그 문언이 필요할 때
6. 🔴 **처리방침에 써야 할 사실이 §2.2 인벤토리와 어긋날 때** — 문서를 맞추지 말고 멈춰라

**형식**

```
[확인 필요] {한 줄 요약}
상황: {무엇이 어떻게 막혔는지}
선택지
 A. {안} → 결과 / 되돌리기 비용
 B. {안} → 결과 / 되돌리기 비용
추천: {A 또는 B}와 이유
```

---

## 10. 커밋

- 메시지 **`SunDO v0.11.0`**(GT-03) · `package.json`·`docs/PROGRESS.md`·**S10 버전 표시** `0.11.0`(GT-07)
- 🔴 **Contributors에 Claude를 포함하지 않는다**(GT-06)
- 터미널 출력 두 줄:
  `작업이 완료되었습니다` / `Firebase에서 작업할 사항이 있습니다. Markdown File을 확인하세요`
  ⚠ 콘솔 요청서를 만들지 않았다면 **첫 줄만** 출력한다
